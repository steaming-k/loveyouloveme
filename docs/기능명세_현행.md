# 럽유럽미 (Love U Love Me) — 현행 기능명세

> **이 문서는 '지금 제품이 무엇인가'만 쓴다.**
>
> [`기능명세서.md`](./기능명세서.md)는 v1.0부터 버전이 쌓인 문서라 같은 화면의 사양이
> §5(본문)와 §11~§19(버전별 절)에 흩어져 있다. 이 문서는 그 조각들을 **화면·로직 기준으로
> 합쳐** 위에서 아래로 한 번에 읽히게 만든 **현행(v1.39) 통합본**이다.
>
> - **변경 이력·판단 근거·철회된 시도**를 알고 싶으면 → [`기능명세서.md`](./기능명세서.md) 부록 A
> - **이전 버전의 이 문서**가 필요하면 → [`versions/`](./versions/README.md) (동결본)
> - **기획 의도·가치 가설·성공지표**를 알고 싶으면 → [`럽유럽미_기획서_v1.md`](./럽유럽미_기획서_v1.md)
> - **톤·컬러·컴포넌트 규칙**을 알고 싶으면 → [`design-guide.md`](./design-guide.md)
> - **실행 방법·아키텍처 개요**를 알고 싶으면 → [`../README.md`](../README.md)

| 항목 | 내용 |
|---|---|
| 문서 종류 | 현행 기능명세 (Consolidated Functional Spec) |
| 제품 버전 | **v1.39** |
| 최종 수정 | 2026-09-07 |
| 이전 버전 | [v1.22 동결본](./versions/기능명세_현행_v1.22.md) — **이 문서의 동결본은 v1.22 하나뿐이다**(v1.23~v1.38은 동결하지 않았다. 사후 생성하지 않는 이유: [versions/README](./versions/README.md)) |
| 기술 스택 | Next.js 15 App Router · React 19 · TypeScript 5.7 · Tailwind CSS 4 · framer-motion |
| 코드 기준 | `src/app/**/page.tsx` (화면) · `src/lib/logic/**` (계산) · `src/services/ai/**` (AI) |
| 대상 독자 | PM · 디자이너 · 신규 합류 개발자 · 포트폴리오 리뷰어 |
| 상태 표기 | **FACT**(구현 확인) · **VERIFIED**(실측 확인) · **NOT VERIFIED**(미검증) · **NOT VALIDATED**(사용자 가치 미검증) |

---

## 목차

0. [Product Principle — HOOK → SURPRISE → VALUE → RETENTION → PREMIUM](#0-product-principle--hook--surprise--value--retention--premium)
1. [제품 정의와 데이터 위계](#1-제품-정의와-데이터-위계)
2. [전체 사용자 흐름 (Funnel)](#2-전체-사용자-흐름-funnel)
3. [화면 인벤토리 (Route 전체)](#3-화면-인벤토리-route-전체)
4. [전역 사양](#4-전역-사양)
5. [데이터 모델](#5-데이터-모델)
6. [화면별 상세 명세](#6-화면별-상세-명세)
7. [핵심 계산 로직](#7-핵심-계산-로직)
8. [AI 분석 파이프라인](#8-ai-분석-파이프라인)
9. [Premium](#9-premium)
10. [Analytics](#10-analytics)
11. [Validation 규칙](#11-validation-규칙)
12. [접근성](#12-접근성)
13. [참조표 (컴포넌트 ↔ 로직)](#13-참조표-컴포넌트--로직)
14. [검증 상태](#14-검증-상태)

---

## 0. Product Principle — HOOK → SURPRISE → VALUE → RETENTION → PREMIUM

> **사용자는 기능의 개수를 사는 것이 아니라,
> 자신의 관계를 더 잘 이해하게 되는 깊이를 산다.**

이 다섯 단계가 이 제품의 가치 구조다. 새 기능을 넣을지 판단할 때 **어느 단계를
강화하는가**를 먼저 답하고, 어느 단계도 강화하지 않으면 넣지 않는다.

| 단계 | 사용자의 말 | 담당 화면 | 재료 |
|---|---|---|---|
| **HOOK** | "궁합이나 MBTI 보러 왔는데" | S21R 동기화율 · X1 MBTI Lens · (향후) 사주·점성술 | 익숙한 소재 |
| **SURPRISE** | "어? 여기서는 실제 내가 답한 관계 신호까지 보네" | `잠깐. 숫자만 보면…` · YOUR SIGNAL · Mirror Teaser · First Contact | 사용자 자신의 답 |
| **VALUE** | "나한테 실제로 의미가 있네" | Mirror 판정 · 근거 목록 · 다가가는 힌트 · 대화 질문 | 개인화된 해석 + 행동 |
| **RETENTION** | "러비가 내가 예전에 어떻게 답했는지도 기억하네" | F1 History · WHAT CHANGED · Home 재진입 행 | 시간축 |
| **PREMIUM** | "무료를 더 길게 쓴 게 아니라 따로 보던 걸 연결해서 보여주는 거구나" | P2 정밀 관찰 리포트 | Cross-source 연결 |

### 단계별 규칙

**HOOK은 익숙하게, 해석은 다르게.**
궁합 점수·MBTI를 약화하지 않는다. `MBTI는 틀렸다`를 먼저 말하지 않고, 기대한 것을
먼저 주고 그 **다음에** 다르게 해석한다. 동기화율은 첫 viewport에 그대로 있고,
`잠깐.`은 점수를 본 **뒤에** 온다.

**SURPRISE는 설명이 아니라 사용자 자신의 결과에서 나온다.**
`MBTI가 전부가 아니야` 같은 문장은 Surprise가 아니다. Surprise는
`네가 말한 기준(5/5)과 관계에서 나타난 신호가 다른 방향을 가리켰어`처럼
**그 사용자의 데이터**에서만 만들어진다. 그래서 이 단계의 품질은 카피가 아니라
**근거 정확도**에 달려 있다 — v1.36이 고친 것이 정확히 이 지점이다.

**VALUE는 판정이 아니라 확인할 것을 준다.**
`이렇게 하면 된다`가 아니라 `이걸 물어보면 알 수 있다`까지다.

**RETENTION은 습관이 아니라 계기다.**
`streak` · `연속 기록` · `오늘도 기록` · `매일 체크`를 쓰지 않는다. 다시 오는 이유는
**기준이 달라졌을 때**이고, 그건 매일 일어나지 않는다.

**PREMIUM은 길이가 아니라 연결이다.**
```
FREE     Depth within      한 Lens / 한 Signal을 깊게
PREMIUM  Depth through     서로 다른 Signal 사이의 연결
```
Premium section이 무료 문장을 반복하면 그 section은 삭제 대상이다 — 개수를 늘리는
방향으로 가치를 만들지 않는다.

### 이 구조가 실제로 정한 것

- **사진은 입장권이 아니다.** Observed는 보강 근거이고, 없으면 그 섹션만 없다.
- **비교할 것이 없다는 말이 관찰할 것이 없다는 말은 아니다.** 상대가 없으면
  First Contact Report가 SURPRISE와 VALUE를 담당한다.
- **없는 것은 자리도 만들지 않는다.** 빈 섹션·빈 카드·`정보 없음` 자리를 두지 않는다.
- **근거 없이 말하지 않는다.** 이 원칙이 깨지면 SURPRISE가 그대로 불신으로 바뀐다.

---

## 1. 제품 정의와 데이터 위계

### 1.1 한 문장

> 상대를 사랑하는 과정에서 **나에 대해 더 깊게 이해하고**, 나를 사랑하는 방법을 알아가는 서비스.
> 궁합은 시작점이고, **관계 속의 나를 보는 것**이 핵심이다.

궁합 점수는 **입구이지 결론이 아니다.** 제품의 Aha Moment는 "이 사람과 잘 맞는다/안 맞는다"가
아니라 **"내가 말한 나와 관계에서 나타난 내가 다르다"**는 발견이다.

### 1.2 캐릭터 — 러비(Lovy)

인간의 사랑을 관찰하러 온 외계인. 역할은 **관찰자**이며 상담가·점쟁이·전문가가 아니다.

| 쓴다 | 쓰지 않는다 |
|---|---|
| `~인가 봐` · `~일 수도 있겠네` · `~구나` | `당신은 ~한 사람입니다` |
| `실제로 그런지는 확인해봐` | `이 관계는 성공할 확률이 78%입니다` |
| `없는 건 없다고 할게` | `심리학적으로 ~` · `천생연분` · `상극` |
| `이건 아직 잘 모르겠네`(혼잣말 자리) | `헤어져야 해` · `너는 회피형이야` |

### 1.3 데이터 위계 — **점수에 들어가는 것은 단 하나**

```
CORE          실제 관계 행동 신호 4축        →  동기화율 계산에 들어간다
              (연락 · 갈등 해결 · 개인 시간 · 애정 표현)
   ↓
SUPPORTING    MBTI                          →  대화 포인트만. 점수 미반영
   ↓
ENTERTAINMENT 사주 · Astrology               →  참고 렌즈. 점수 미반영
   ↓
RETENTION     Relationship History           →  변화 기록. 점수 미반영
   ↓
PREMIUM       Deep Report                    →  관찰 사이의 연결. 점수 미반영
```

> **동기화율(Sync Rate)은 관계 행동 신호 4축만으로 계산한다.**
> MBTI·사주·별자리·History·Premium은 **어느 것도** 점수·`comparedCount`·`TARGET_MIN_KNOWN`
> 판정에 합산되지 않는다. 이건 구현 디테일이 아니라 **제품 원칙**이다 — 근거 없이 판정하지
> 않기 위해서다.

### 1.4 정보 위계 — 4단계 읽기 깊이 (v1.23)

결과 화면의 모든 정보는 **사용자가 그 답을 원하는 시점**으로 분류한다.
같은 정보를 여러 Level에서 반복하지 않는다.

| Level | 시간 | 사용자의 질문 | 담는 것 |
|---|---|---|---|
| **LEVEL 1** | 5초 | 그래서 우리 관계는 어떤데? | 동기화율 · **핵심 한 문장** · 러비의 의문 · YOUR SIGNAL |
| **LEVEL 2** | 30초 | 왜 그렇게 나왔는데? | 잘 맞는 신호 · 확인이 필요한 신호 · 근거 · LOVY OBSERVATION |
| **LEVEL 3** | 1~3분 | 그래서 우리가 뭘 해볼 수 있는데? | 다가갈 때 · 이야기해볼 질문 |
| **LEVEL 4** | Deep | 이 신호들이 서로 왜 연결되는 건데? | Premium Deep Report |
| (보조) | — | 다른 관점으로는? | Lens — Core·Action 뒤 |

이 구조는 **Compatibility Result(S21R)에 실제로 적용돼 있다**(§6 S21R).
Typography도 이 4단계에 대응한다(§4.13).

### 1.5 절대 규칙 (전 버전 공통)

| # | 규칙 |
|---|---|
| 1 | **근거 없이 판정하지 않는다.** 수집하지 않은 데이터를 계산에 넣지 않는다 |
| 2 | **AI는 판정을 만들지 못한다.** 규칙이 판정하고, AI는 그 판정을 근거와 함께 설명만 한다 |
| 3 | **못 한 것을 숨기지 않는다.** `limitations`를 화면에 그대로 나열한다 |
| 4 | **실패를 성공인 척하지 않는다.** demo/mock/fallback/real을 화면에서 구분해 말한다 |
| 5 | **상대의 마음을 읽지 않는다.** 사용자가 알려준 상대 정보만 쓰고, 성격·감정을 단정하지 않는다 |
| 6 | **무료에서 Core Value가 완성된다.** Premium은 제품 중심을 대체하지 않는다 |
| 7 | **연출 때문에 실제 처리를 늦추지 않는다.** motion은 presentation layer 전용 |

### 1.6 러비의 두 루프 — 4단계와 5단계는 다른 것이다 (v1.39)

러비를 설명하는 단계 목록이 두 개 있고, **둘은 층이 다르다.** 문서·코드·화면에서 이 둘을
섞어 쓰면 "왜 로딩이 4단계인데 세계관은 5단계지?"라는 질문이 반복된다.

| | ANALYSIS SEQUENCE | PRODUCT LEARNING LOOP |
|---|---|---|
| 단계 | `OBSERVE → COLLECT → CONNECT → REPORT` (**4**) | `OBSERVE → COLLECT → CONNECT → REPORT → REMEMBER` (**5**) |
| 층 | 한 번의 분석 **안**의 처리 흐름 | 분석과 분석 **사이**, 시간축에 걸친 제품 행동 |
| 시간 척도 | 2.6초 (첫 관찰 · §4.7) | 일 · 주 · 달 |
| 담당 | `LovyObservation` (S08 사진 · S20 궁합) | S27R 저장 → F3 → F1 → F1-a → F2 |
| 코드 | `components/lovy/LovyObservation.tsx` · `lib/motion.ts` | `historyRepository.ts` · `logic/history.ts` · `logic/soloHistory.ts` |
| 사용자가 보는 것 | 러비가 지금 무엇을 하는 중인지 | 러비가 예전 내 답을 **기억하고 있다**는 사실 |

**`REMEMBER`가 무엇인가** — History 저장(`RelationshipHistoryEntry`) · Snapshot 동결 ·
F3 Change Moment · STABLE/SHIFT/NEW 판정 · 과거와 현재의 비교. 즉 §2.3의 `CURRENT` vs
`HISTORY` 구분과 §0의 RETENTION 단계가 이 이름을 갖는다.

> ⚠️ **`REMEMBER`는 loading 연출의 다섯 번째 stage가 아니다.** 다음 세 가지 이유로
> `LovyObservation`에 추가하지 않는다.
>
> 1. **아직 일어나지 않은 일이다.** 분석 로딩 시점에는 저장 버튼을 누르지도 않았다.
>    없는 것을 진행 중이라고 말하지 않는다(§1.5 규칙 3·4).
> 2. **Time-to-Value와 정면으로 충돌한다.** v1.38이 6,158ms → 2,633ms로 줄인 근거가
>    "궁합은 순수 계산이고 대기의 전부가 연출이었다"였다. 단계를 하나 더 얹으면
>    `stageMs × 5 + tailMs`로 되돌아간다(§4.7).
> 3. **REMEMBER는 화면이 이미 담당한다.** F3 Change Moment가 저장 **직후** 이 단계를
>    사용자에게 보여준다 — 로딩 애니메이션으로 예고할 대상이 아니다.
>
> 세계관은 5단계, loading은 4단계다. 이 문서에서 5단계를 쓰는 곳은 이 절과 §0(RETENTION)
> 뿐이고, 4단계를 쓰는 곳은 §4.7·§6 S08·§6 S20뿐이다.

---

## 2. 전체 사용자 흐름 (Funnel)

```
새로운 사람에 대한 궁금증
  │
  ├─ S01 스플래시 ─→ (재방문자면 S29 홈으로 분기)
  ├─ S02~S04 온보딩 3단
  ├─ S05 관계 상태 선택
  │
  ├─ ══ AI Profile Building (S06~S18) ═══════════════════
  │   S06 인트로 → S07 사진 → S08 분석 → S09 Observed Me ⭐
  │                                   → S10~S13 Declared Me (+ 내 MBTI)
  │                                   → S14~S17 Relationship Me (+ S16a Adaptive)
  │                                   → S18 Relationship Profile ⭐
  │   ⚠️ S18은 세 Source를 **나열만** 한다. 아직 비교하지 않는다.
  │
  ├─ S19 상대 정보 입력 (4축 + 상대 MBTI + 좋아하는 것)
  ├─ S20 궁합 관찰 (LovyObservation 4단계)
  │
  ├─ S21R Compatibility Result ⭐  ── "우리 둘은 어떻게 맞는가?"
  │      #summary #why #good #friction #lenses #approach #questions
  │      ↑ 여기서 FIRST SURPRISE + YOUR SIGNAL (무료 Aha)
  │
  ├─ S26 Mirror Teaser ⭐  ←── 제품의 시선 전환점 (Primary KPI)
  │      "너와 그 사람"에서 "말한 나 vs 관계 속 나"로
  │
  ├─ S27R Relationship Mirror Result ⭐ ── "나는 관계에서 어떤 사람인가?"
  │      → 내 관찰 기록에 저장 → F3 Change Moment
  │
  └─ S29 분석 후 홈  ── Current Result Revisit 진입점
       │
       ├─ F1 관찰 기록 → F1-a 기록 상세 → F2 변화 리포트   (Retention)
       ├─ X1 러비의 다른 관측 렌즈 → X1-a MBTI / X1-b Astrology / 사주
       └─ P1 Premium Paywall → (현재 Fake Door) → Deep Report
```

### 2.1 두 Mental Model의 분리

제품에는 서로 다른 두 질문이 있고, **화면 전환은 질문이 바뀔 때만** 일어난다.

| Mental Model | 질문 | 화면 |
|---|---|---|
| **Compatibility** | 우리 둘은 어떻게 맞는가? | S21R (구 S21~S25 통합) |
| **Mirror** | 나는 관계에서 어떤 사람인가? | S27R (구 S27~S28 통합) |

같은 질문의 답을 읽는 동안에는 **한 화면에 머문다**(anchor 이동). 질문 자체가 바뀌는
순간(Compatibility → Mirror)에만 새 화면으로 전환하고, 그 사이에 S26 Teaser를 둔다.

### 2.2 자기 발견은 딱 한 번만 일어난다

S18(Relationship Profile)과 S26/S27R(Mirror)은 **서로 다른 코드 경로**를 쓰고 어느 쪽도
서로를 호출하지 않는다.

| 화면 | 함수 | 하는 일 |
|---|---|---|
| S18 | `buildRelationshipProfile()` | 세 Source를 **나열**. 판정 없음 |
| S27R | `buildMirrorReport()` | MATCH / GAP / CHANGE **판정** |

S18에서 미리 "말한 것과 실제가 다르다"를 말해버리면 S26의 Aha가 '이미 본 내용의 반복'이 된다.

### 2.3 Current Result vs History

| | Current Result | Relationship History |
|---|---|---|
| 데이터 | 세션에서 **매번 다시 계산**되는 순수 함수 | 저장된 **불변 Snapshot** |
| 저장소 | `localStorage['lym.session.v1']` | `localStorage['lym.history.v1']` |
| 진입 | `?view=revisit&source=...` | `/history` |
| 답을 주는 질문 | "지금의 나는?" | "그때의 나는 어땠지?" |

수정(Declared·Experience·Target)은 Current Result에만 반영되고, **History Snapshot은 다시
쓰지 않는다.** 새 Entry는 사용자가 Mirror를 다시 저장했을 때만 생긴다.

---

## 3. 화면 인벤토리 (Route 전체)

### 3.1 Core Funnel

| WF | 화면명 | Route | 파일 |
|---|---|---|---|
| S01 | 스플래시 | `/` | `src/app/page.tsx` |
| S02~04 | 온보딩 3단 | `/onboarding` | `src/app/onboarding/page.tsx` |
| S05 | 관계 상태 | `/status` | `src/app/status/page.tsx` |
| S06 | 프로필 빌딩 인트로 | `/profile/intro` | `src/app/profile/intro/page.tsx` |
| S07 | 사진 입력 | `/profile/photos` | `src/app/profile/photos/page.tsx` |
| S08 | 사진 분석 관찰 | `/profile/analyzing` | `src/app/profile/analyzing/page.tsx` |
| **S09** | **Observed Me 결과** ⭐ | `/profile/observed` | `src/app/profile/observed/page.tsx` |
| S10~13 | Declared Me 1~4 | `/profile/declared/[1-4]` | `src/app/profile/declared/[step]/` |
| S14 | 과거 관계 인트로 | `/profile/past/intro` | `src/app/profile/past/intro/page.tsx` |
| S15~17 | Relationship Me 1~3 | `/profile/past/[1-3]` | `src/app/profile/past/[step]/` |
| S16a | Adaptive Follow-up (조건부) | `/profile/past/adaptive` | `src/app/profile/past/adaptive/page.tsx` |
| **S18** | **Relationship Profile** ⭐ | `/profile/result` | `src/app/profile/result/page.tsx` |
| S19 | 상대 정보 입력 | `/target` | `src/app/target/page.tsx` |
| S20 | 궁합 관찰 | `/compatibility/analyzing` | `src/app/compatibility/analyzing/page.tsx` |
| **S21R** | **Compatibility Result** ⭐ | `/compatibility` | `src/app/compatibility/page.tsx` |
| **S26** | **Mirror Teaser** ⭐ | `/mirror/teaser` | `src/app/mirror/teaser/page.tsx` |
| **S27R** | **Mirror Result** ⭐ | `/mirror` | `src/app/mirror/page.tsx` |
| S29 | 분석 후 홈 | `/home` | `src/app/home/page.tsx` |

### 3.2 Retention (Relationship History)

| WF | 화면명 | Route | 파일 |
|---|---|---|---|
| F1 | 관찰 기록 | `/history` | `src/app/history/page.tsx` |
| F1-a | 기록 상세 ('이때의 관찰') | `/history/[id]` | `src/app/history/[id]/page.tsx` |
| F2 | 변화 리포트 | `/history/report` | `src/app/history/report/page.tsx` |
| F3 | 저장 직후 Change Moment | `/history/saved` | `src/app/history/saved/page.tsx` |

### 3.3 Lens (Add-on)

| WF | 화면명 | Route | 위계 |
|---|---|---|---|
| X1 | 러비의 다른 관측 렌즈 (목록) | `/lens` | — |
| X1-a | MBTI 렌즈 상세 | `/lens/mbti` | **Supporting** |
| X1-b | Astrology 렌즈 | `/lens/zodiac`, `/lens/astrology` | Entertainment |
| — | 사주 렌즈 | `/lens/saju` | Entertainment (**엔진 미연결**) |
| X2 | Birth Profile 입력 | `/lens/birth` | 공용 입력 |
| — | 궁합용 렌즈 묶음 | `/compatibility/lenses` | — |

### 3.4 Premium

| 화면명 | Route | 상태 |
|---|---|---|
| Premium Paywall → Success → Report | `/premium` | **Fake Door** (실제 PG 미연결) |
| First Contact Report (v1.29 P4) | `/first-contact` | 상대를 비교할 수 없는 사용자의 관찰 리포트. `couple`이면 `/compatibility`로 보낸다 |
| Deep Report 미리보기 (dev·UT) | `/premium-preview/[feature]` | `PREMIUM_PREVIEW=true`일 때만 |
| Deep Question | `/premium-preview/deep-questions` | 〃 |

### 3.5 기타

| 화면명 | Route | 그룹 |
|---|---|---|
| 궁합 공유 카드 | `/share/compatibility` | Share |
| Mirror 공유 카드 | `/share/mirror` | Share |
| 프라이버시 안내 | `/privacy` | Add-on |
| AI Contract Test (dev 전용) | `/api/ai/contract-test` | dev (**Production 404**) |
| History Fixture 실행기 (dev 전용) | `/api/dev/history-test` | dev (**Production 404**) |

> **두 dev Route는 같은 방식으로 막혀 있다** — 핸들러 첫 줄에서
> `process.env.NODE_ENV === 'production'`이면 404를 반환한다(§13.1). 그래서
> `npm run test:ai`·`npm run test:history`는 로컬 dev 서버에서만 돌아간다
> (`LYM_BASE_URL`을 배포 URL로 바꿔도 404다).

### 3.6 Edge (별도 Route 없이 조건부 렌더)

| WF | 상태 | 나타나는 곳 |
|---|---|---|
| E1 | 데이터 부족 | `/mirror` 내부 상태 |
| E2 | AI 오류 | `?error=1` — `/profile/analyzing`, `/compatibility/analyzing` |
| E3 | 관측 정보 부족 (궁합) | `/compatibility` 내부 상태 (`score === null`) |
| E4 | 연애 경험 없음 | `/profile/past/none` (실제 Route) |
| E5 | Mirror 사용 불가 | `/mirror/teaser` 내부 상태 |

### 3.7 Legacy Route — 삭제하지 않고 `redirect()`

기존 링크·북마크가 깨지지 않도록 남겨둔 5개다. 전부 Canonical Route의 anchor로 보낸다.

| 구 WF | Route | 목적지 |
|---|---|---|
| S22 | `/compatibility/why` | `/compatibility#why` |
| S23 | `/compatibility/good` | `/compatibility#good` |
| S24 | `/compatibility/friction` | `/compatibility#friction` |
| S25 | `/compatibility/questions` | `/compatibility#questions` |
| S28 | `/mirror/insight` | `/mirror#core-insight` |

---

## 4. 전역 사양

### 4.1 레이아웃 골격 — `ScreenLayout`

모든 화면이 공유하는 3단 구조다.

| 영역 | 규칙 |
|---|---|
| **Header** (flex-none) | `ScreenHeader` — 뒤로가기(←) · progress bar(선택) · 카운터(`질문 1/4`) · 우측 액션(공유/수정) |
| **Body** (flex-1, scroll) | 좌우 여백 20px(`px-gutter`) 고정. `centered` 옵션이면 세로 중앙(스플래시·로딩·Empty·Error) |
| **Footer** (flex-none, sticky) | Primary CTA + 보조 액션. 하단 safe-area 26px |
| **Nav** (선택) | `BottomNavigation` — Post-analysis 화면에서만. footer와 겹치지 않고 아래에 쌓인다 |

> ⚠️ **스크롤 주체는 `document`가 아니라 Body의 `overflow-y-auto` 컨테이너다.**
> 이 컨테이너에는 `data-screen-scroll` 표식이 붙어 있고, 스크롤 위치 복원이 이 표식으로
> 컨테이너를 찾는다(§6 S21R · §4.9). 브라우저·Next.js의 native scroll restoration은
> document 스크롤만 되돌리므로 이 앱에서는 **원리적으로 동작하지 않는다.**

스크롤바는 전역 CSS(`* { scrollbar-width: none }`)로 숨기되 스크롤 동작은 유지한다.

### 4.2 앱 셸 — `AppShell`

Mobile First. 기준 프레임 **393×852**, 360px에서도 깨지지 않아야 한다.
데스크톱(`lg:`)에서는 프레임을 가운데 띄우고 옆에 dev용 `PrototypePanel`을 둔다
(`NODE_ENV === 'production'`에서는 렌더되지 않는다).

### 4.3 진행률(Progress)

AI Profile Building 전 구간(S05~S19)에서 **하나로 이어진다.**

| 화면 | % | 화면 | % |
|---|---|---|---|
| S05 관계 상태 | 8 | S14 과거 관계 인트로 | 60 |
| S06 프로필 인트로 | 14 | S15 과거 Q1 | 66 |
| S07 사진 입력 | 22 | S16 과거 Q2 | 72 |
| S09 Observed 결과 | 30 | S16a Adaptive (조건부) | 75 |
| S10~S13 Declared 1~4 | 36 / 42 / 48 / 54 | S17 과거 Q3 | 78 |
| | | S19 상대 정보 | 88 |

S18·S21R 이후는 progress bar 없이 `title`/`centerLabel`만 쓴다.

### 4.4 검증 / 에러 처리

`alert()`를 쓰지 않는다. 네 가지 패턴만 쓴다.

| 패턴 | 컴포넌트 | 쓰는 곳 |
|---|---|---|
| 인라인 에러 | `InlineError` | 폼 검증 실패 (footer 위) |
| 토스트 | `useToast()` | 성공·완료·비차단 안내 |
| 바텀시트 | `BottomSheet` | 수정 입력 · Fake Door |
| 확인 모달 | `ConfirmModal` | 삭제 등 되돌릴 수 없는 동작 |

### 4.5 데이터 영속성

| 키 | 내용 | 관리 주체 |
|---|---|---|
| `localStorage['lym.session.v1']` | 세션 답변 (`SessionAnswers`) | `SessionProvider` |
| `localStorage['lym.history.v1']` | 영구 관찰 기록 | `historyRepository.ts` (**유일 접근점**) |
| `localStorage['lym.analytics.v1']` | 분석 이벤트 로그 | `analytics.ts` |
| `localStorage['lym.premium-intent.v1']` | Premium 관심 표시 | `premiumIntentStore.ts` |
| `localStorage['lym.consent.v1']` | GA4 동의 상태 | `analyticsConsent.ts` |
| `localStorage['lym.ut.deep.v1']` | UT 평가 응답 | `deepReportUtStore.ts` |
| `sessionStorage['lym.session_fired.*']` | KPI 중복 방지 가드 | `analytics.ts` |
| `sessionStorage['lym.scroll.v1:<key>']` | 스크롤 위치 (숫자만) | `scrollRestore.ts` |

**업로드 사진은 새로고침 후 세션에서 자동 제거된다.** `deserialize()`가 복원 시점에
`source === 'upload'` 사진을 필터링한다 — `objectUrl`은 새로고침 후 무효라서, 남겨두면
'보이지 않는 사진'이 유효한 개수로 잡힌다. 샘플 사진은 tone만으로 다시 그릴 수 있어 유지한다.

### 4.6 Hydration 안전장치 — `HydrationGate`

계산 결과를 보여주는 화면(`/compatibility`·`/mirror`·`/mirror/teaser`·`/profile/result`·
`/history`)은 `HydrationGate`로 감싼다. `localStorage` 복원 전에 렌더하면 "답변이 하나도
없는 상태"로 계산된 빈 결과가 한 프레임 스쳐 보인다.

### 4.7 관찰 시퀀스 — `LovyObservation`

분석 화면은 스피너가 아니라 **러비의 관찰 과정**이다.

```
OBSERVE  →  COLLECT  →  CONNECT  →  REPORT
관계 신호를   서로 다르게   신호들이      관찰 기록을
살펴보는 중   나타난 신호를  어디에서      정리하는 중
             모으는 중     이어지는지
```

> ⚠️ **이 시퀀스는 4단계로 고정한다.** 제품 학습 루프의 5번째 단계 `REMEMBER`는 분석
> **이후** 시간축에서 일어나는 행동이라 여기에 들어오지 않는다 — 이유와 담당 화면은
> **§1.6**에 있다. 새 단계를 추가하는 변경은 §1.6을 먼저 읽고 판단한다.

| 항목 | 값 |
|---|---|
| 표시 방식 (v1.38) | **누적(stack)**. 지나온 단계가 사라지지 않고 쌓인다 — 앞 줄은 낮추고 마지막 줄만 강조 |
| 단계 간격 | **520ms** (`OBSERVATION.stageMs` · `lib/motion.ts`) |
| 마무리 | **420ms** (`OBSERVATION.tailMs`) |
| 총 시간 | `520 × 4 + 420 = **2.5초**` (Release Gate 5회 median **2,633ms** · min 2,612 · max 3,045) |
| 재관찰 배속 | **×0.55** (`OBSERVATION.revisitScale`) → 약 1.4초. `completed.compatibility`로 판정 |
| 시각 표현 | 인라인 SVG `ObservationField` — 신호 토큰이 모여 기록지가 된다 |
| 상태 텍스트 | 단계 코드 + 한국어 문장. **색·모션만으로 진행을 전달하지 않는다** |
| Reduced Motion | 4단계를 **목록으로 한 번에** 보여주고 **700ms** 뒤 이동 (`OBSERVATION.reducedMs`, 실측 median 776ms) |
| 대기 중 문구 (v1.38) | 연출이 끝났는데 실제 처리가 안 끝났으면 `아직 보고 있어. 조금만 더.`(`OBSERVATION_PENDING`). **완성되지 않은 것을 완성됐다고 말하지 않는다** |
| 실패 시 | E2 에러 화면 (`?error=1`로 재현 가능) |

> ⚠️ **연출 때문에 분석을 늦추지 않는다.**
> S08(사진)은 `연출 완료 && 실제 분석 완료` **둘 다** 만족해야 이동한다 — 실제 응답이
> 더 늦으면 마지막 단계에서 기다린다. S20(궁합)도 v1.38부터 **계산 완료를 함께 기다린다**
> (없는 값을 `score: 0`으로 채우지 않는다).

> **v1.38 — 왜 6.1초에서 2.5초가 됐나.**
> v1.22가 단계 간격을 1100 → 1400ms로 올린 이유는 *"각 단계 문구가 읽히기 전에 다음
> 단계로 넘어간다"* 였다. 그건 단계가 **교체**됐기 때문이고, 그러면 한 줄을 읽는 데
> 필요한 시간이 그대로 대기가 된다. v1.38은 줄을 **쌓는다** — 앞 줄이 사라지지 않으므로
> 사용자가 자기 속도로 읽고, 간격은 '순서대로 일어난다'만 전달하면 된다.
> `OBSERVE → COLLECT → CONNECT → REPORT` 4단계와 문구는 **하나도 지우지 않았다.**
>
> ⚠️ `ObservationField`의 CSS duration/JS stagger는 이 간격에 **맞춰져 있다**.
> 1400ms 시절 값(CONNECT 선 620ms + 210ms stagger = 830ms)을 그대로 두면 선이
> 목적지에 닿기 전에 다음 단계가 시작된다. `stageMs`를 바꾸면 `globals.css`의 `.obs-*`와
> `ObservationField`의 `transitionDelay`를 함께 본다.

### 4.8 승인된 줄바꿈 — `Lines`

한국어는 자동 줄바꿈 위치가 화면 폭마다 달라진다. 기획에서 "이 두 문장은 각각 한 줄"로
정한 카피는 CSS 흐름에 맡기지 않고 `Lines` 컴포넌트가 각 줄을 `block` `span`으로 그린다.

⚠️ `<br>`을 쓰지 않는다 — 스크린리더가 한 문단을 억지로 끊어 읽지 않도록 문장 단위
`span`으로만 나눈다.

| 화면 | 줄 1 | 줄 2 |
|---|---|---|
| F1 caption | 누구와 몇 번 만났는지는 기록하지 않아. | 네 기준이 어떻게 움직였는지만 남겨. |
| F1 변화 요약 / F2 Empty | 아직 비교할 과거 기록이 하나뿐이야. | 다음 관찰이 쌓이면 변화를 알려줄게. |
| F1 · F2 러비 한 줄 | 인간의 사랑은 아직도 잘 모르겠어. | 근데 너에 대해서는 전보다 조금 알 것 같아. |
| F1-a '이때의 관찰' | 이건 가장 최근 관찰이야. | 다음 관찰이 쌓이면 이 기록과도 비교할 수 있어. |

### 4.9 스크롤 위치 복원 — `useScrollRestore`

하위 화면(Lens 상세 등)을 보고 Back으로 돌아왔을 때 읽던 위치로 되돌린다.

| 항목 | 규칙 |
|---|---|
| 저장소 | `sessionStorage['lym.scroll.v1:<key>']` — **스크롤 오프셋 숫자만**. 답변·사진·분석 내용 미포함 |
| 키 | `compat:<funnelAnalysisId>` — **새 상대는 새 키** |
| 새 분석 | `/compatibility/analyzing` 진입 시 해당 키를 **삭제** → 같은 상대 재분석도 맨 위에서 시작 |
| hash 진입 | **복원하지 않는다.** Legacy Redirect·anchor 이동(`useAnchorScroll`)이 이긴다 |
| 첫 복원 시도 | **동기 실행.** 탭이 가려지면 `requestAnimationFrame`이 실행되지 않아 복원이 아예 안 걸린다 |
| 내용 지연 로드 | 높이가 모자라면 rAF로 최대 20프레임 재시도 (Report 섹션·AI Narrative가 늦게 채워진다) |
| 저장 시점 | 스크롤 이벤트에서 **동기 변수**에 기록, sessionStorage 쓰기만 rAF로 묶음 |
| 언마운트 | 마지막 관측값을 저장. ⚠️ 컨테이너 `scrollTop`을 **다시 읽지 않는다** — 라우트 전환 시점에는 노드가 이미 분리돼 `0`으로 읽히고, 저장값을 0으로 덮어써 복원이 깨진다 |
| 접근성 | **포커스를 옮기지 않는다.** `smooth`도 쓰지 않는다 |
| 실패 내성 | 읽기·쓰기 전부 `try/catch` — sessionStorage가 막힌 환경(프라이빗 모드)에서도 앱이 죽지 않는다 |

적용 화면: `/compatibility` (현재 1곳)

### 4.10 러비 사용 규칙

| 크기 | 쓰는 곳 |
|---|---|
| 96~200px | Splash · Onboarding · 로딩 · Empty/Error · Mirror Teaser |
| 38~46px | 일반 입력 화면 아바타 |

**에셋** — `LOVY_ASSETS`(`src/data/lovy.ts`)에 14개 포즈. `hero`(509×558, Splash 전용)를 빼면
13개는 **291×298 균일 캔버스**다.

> ⚠️ `LOVY_ASSETS`의 width/height는 **실제 파일 픽셀과 반드시 일치해야 한다.**
> `Lovy`가 이 값으로 표시 높이를 계산하므로(`height = renderWidth × asset.height / asset.width`)
> 어긋나면 이미지가 눌리거나 늘어난다.

**시각 보정** — `LOVY_VISUAL_SCALE`. 균일 캔버스는 여백이 있어 같은 `size`에서 몸통이 작게
보인다. 초록 픽셀(캐릭터 색)만 골라 몸통 높이를 실측해 포즈별 배율을 구했다.

| 배율 | 포즈 |
|---|---|
| 1.00 | laptop |
| 1.05~1.06 | calendar · hero · observe · record |
| 1.13 | question · heart · chart · mug · cool |
| 1.17 | book |
| 1.27 | movie |
| 1.30 | wand |
| 1.35 | crystal |

배율은 **컴포넌트 내부에서만** 적용한다 — 40여 개 호출부 중 상당수가 `pose`를 동적으로 받는
공유 컴포넌트를 거치므로, 개별 화면을 고치면 동적 호출부를 놓친다.

> ⚠️ **공유 카드 PNG는 `Lovy`를 거치지 않는다.** `shareCard.ts`가 Canvas 2D로 직접 그리므로
> `ShareCardSpec`이 경로 문자열이 아니라 `lovyPose: LovyPose`를 받아 같은 레지스트리에서
> 경로·배율을 가져온다 — 문자열 경로를 따로 들고 있으면 다시 어긋난다(실제 있었던 버그).

### 4.11 디자인 토큰

| 역할 | 토큰 | 값 |
|---|---|---|
| **페이지 배경** | `--color-canvas` | **`#ffffff`** |
| 카드 표면 | `--color-surface` | `#ffffff` |
| 보조 표면 (파선 카드·AI Narrative) | `--color-canvas-warm` | `#fcfbf8` |
| 눌린 표면 · NoticeBox | `--color-sunken` | `#f6f5f1` |
| Brand (분석·Insight) | `--color-brand` | `#8f74f0` |
| Secondary (러비·관찰) | `--color-mint` | `#a9e3d0` |
| Friction (차이 신호 전용) | `--color-friction` | `#e29bc0` |
| 데스크톱 프레임 뒤 벽면 | (AppShell `lg:`) | `#F1F1F1` |

**Purple(분석)과 Mint(러비)는 역할을 섞지 않으며 gradient로 쓰지 않는다.**
비율 원칙: Neutral 70~75% / Primary Purple 15~20% / Mint 10~15%.

> **'모든 표면을 흰색으로'가 아니다.** 페이지 배경만 순백이고, 카드·Note·러비 말풍선·
> Evidence block의 tonal distinction은 유지한다.

| Radius | 값 | Type scale | 값 |
|---|---|---|---|
| tag / chip / row | 8 / 12 / 14px | display / hero / question | 26 / 25 / 24px |
| button / card / hero | 15 / 18 / 20px | title / section / insight | 23 / 19 / 17px |
| | | lead / body / sub | 16 / 15 / 14px |
| Motion | fast 150 · normal 300 · slow 400ms | caption / meta / label | 13 / 12 / 10px |

### 4.12 Card Density 정책 (v1.23)

카드는 **정보가 독립된 객체일 때만** 쓴다. 문단을 구분하려고 카드를 만들지 않는다.

| 표현 | 쓰는 곳 |
|---|---|
| **CARD** (`rounded-card` + border) | 그 자체로 완결된 단위 — PRIMARY 신호 1개, PRIMARY 힌트 1개, 대화 질문 |
| **DIVIDER LIST** (1px `border-t`) | 같은 종류가 반복되는 목록 — compact 신호·힌트, Lens index |
| **SECTION** (`ReportSection`) | 번호 + hairline divider + 제목. 카드가 아니다 |
| **ANNOTATION** (`LovyNote`) | 러비의 생각. mint 좌측 rule |
| **EVIDENCE BLOCK** (`ReportEvidenceBlock`) | 근거·메타. neutral 좌측 rule |
| **INLINE TEXT** | 나머지 전부 |

**PRIMARY / compact 두 단계** — 같은 규격 카드를 3~4개 쌓으면 무엇을 먼저 볼지 사라진다.
대표 1개만 카드로 두고 나머지는 divider 행 + 한 단계 낮은 typography로 내린다.
⚠️ **정보를 빼는 것이 아니다** — 상황·근거·AI 설명은 compact에도 그대로 붙어 있다.

### 4.13 Typography — 읽기 깊이와 연결한다 (v1.23)

| Level | 크기 | 쓰는 곳 |
|---|---|---|
| Hero | 76px | 동기화율 숫자 (**한 화면에 하나만**) |
| LEVEL 1 | 24px | 보고서 제목(`ReportHeader` h1) |
| LEVEL 1 | 17px semibold | **핵심 한 문장** |
| LEVEL 2 | 19px | 섹션 제목(`ReportSection` h2) |
| LEVEL 2 | 15px | PRIMARY 카드 제목 · 본문 |
| LEVEL 3 | 13~14px | compact 항목 · action 본문 |
| Metadata | 10~12px | 섹션 번호 · code · 근거 라벨 · 면책 |

**모바일에서 32px 이상 제목을 남발하지 않는다.** 76px 점수는 이 화면의 **유일한** hero다.

### 4.14 AI 모드 정직성

**결과의 `meta.mode`가 진실이고, 화면 배지는 그걸 그대로 따른다.**

| mode | S09 배지 | 안내 | trait |
|---|---|---|---|
| `real` | `AI OBSERVATION` + `근거: 사진 N/M장` | 사진 전송 사실 안내 | 실제 관찰 |
| `mock` | `MOCK AI` | 개발용이며 Provider를 부르지 않았다 | mock 관찰 |
| `demo` | `DEMO AI` | 규칙 기반이며 **전송조차 없다** | **0개** |
| `legacy-demo` | `DEMO AI` | 〃 (v1.5 이전 세션 복원) | **0개** |
| `fallback` | `규칙 기반 대체` | 사진 분석에 실패했다 | **0개** |

- **S07(사진 입력) 안내도 모드를 따른다.** demo에서 "사진이 전송된다"고 고지하면 그 안내
  자체가 거짓이다 — `AI_MODE_HINT`에 따라 `photoAiNotice` / `photoDemoNotice`를 분기한다
- `AI_MODE=real`인데 서버에 키가 없으면 **real이라고 표시하지 않는다** → `CONFIG_ERROR`
- `NEXT_PUBLIC_AI_MODE`는 첫 호출 이전 **표시용 힌트**일 뿐이고 실제 동작은 서버 `AI_MODE`가 정한다

> ⚠️ `NEXT_PUBLIC_*`는 **빌드 타임에 번들로 인라인**된다. 서버 `AI_MODE`만 `real`로 켜고
> 재빌드하지 않으면 "서버는 전송하는데 화면은 전송하지 않는다고 말하는" 거짓 고지가 생긴다.
> 환경변수 변경 후에는 **빌드 캐시 없이 재배포**한다.

---

## 5. 데이터 모델

### 5.1 `SessionAnswers` — 진행 중인 분석 (세션)

저장소: `localStorage['lym.session.v1']` · 관리: `SessionProvider`

```
SessionAnswers
├─ status              RelationshipStatus | null              (S05)
├─ photos              PhotoAsset[]                           (S07)
├─ observedAnalysis    ObservedProfileResult | null           (S08 결과)
├─ observations        Record<traitId, ObservationFeedback>   (S09 사용자 확인/수정)
├─ declared            DeclaredPreference                     (S10~S13)
│   ├─ contact    1~5 | null                    연락 중요도
│   ├─ conflict   'now'|'soon'|'space' | null   갈등 해결 방식
│   ├─ alone      1~5 | null                    개인 시간 중요도
│   ├─ affection  'a1'|'a2'|'a3' | null         애정 표현
│   └─ hobby      'h1'|'h2'|'h3' | null         취미 공유
├─ experience          RelationshipExperience                 (S15~S17, S16a, E4)
│   ├─ important  PastFactor[] (최대 4개)
│   ├─ hardest    HardestMoment | null
│   ├─ selfGap    'yes'|'some'|'no' | null
│   ├─ note       string (선택, 최대 300자)
│   ├─ skipped    boolean                        E4 경로
│   └─ adaptive   { axis, optionId } | null      S16a (조건부)
├─ target              TargetProfile                          (S19)
│   ├─ relation      TargetRelation | null       ← 맥락 전용. 점수 미반영
│   ├─ contact/conflict/alone/affection: 'l'|'m'|'h'|'x'
│   ├─ mbti          MbtiType | null             ← 선택. 점수 미반영
│   ├─ preferences   { interests: string[] }     ← 최대 5개 (Approach Hints용)
│   └─ birthProfile  BirthProfile                ← 선택. 점수 미반영
├─ savedQuestions      ConversationQuestionId[]               (S21R)
├─ coreVerdict         'ok'|'no'|null                         (S27R)
├─ coreCorrection      string                                 (S27R)
├─ mbti                MbtiType | null           ← 내 MBTI. 선택 (S13)
├─ birthProfile        BirthProfile              ← 선택. 사주·Astrology 공용
├─ legacyZodiac        ZodiacSign | null         ← v1.3 잔재. 읽기 전용
├─ deepAnswers         DeepAnalysisAnswer[]                   (Premium)
├─ share               { includeTargetInfo, includeDimensionScores }
├─ currentAnalysisMeta { funnelAnalysisId, updatedAt, ... }
└─ completed           { onboarding, observed, declared, experience,
                         profile, compatibility, mirror }
```

#### `TargetRelation` — 관계 맥락 (v1.22에서 2개 추가)

```ts
type TargetRelation = 'crush' | 'talking' | 'friend' | 'work' | 'intro' | 'unsure';
```

| 값 | 라벨 |
|---|---|
| `crush` | 알아가는 중 |
| `talking` | **썸 타는 중** |
| `friend` | 친구 |
| `work` | 같이 일하는 사람 |
| `intro` | 소개로 만남 |
| `unsure` | **잘 모름** |

> ⚠️ **계산 근거가 아니다.** 동기화율·4축 similarity·`comparedCount`·`TARGET_MIN_KNOWN`·
> Mirror·History 판정 어디에도 들어가지 않는다. 실사용처는 세 곳뿐이다.
>
> 1. `RelationshipHistoryEntry.context.targetRelation` (기록 metadata)
> 2. `target_profile_complete` 이벤트의 `relation` 속성 (**새 이벤트 아님**)
> 3. `buildCompatibilityContext`의 `targetRelation` — 지금도 항상 `null`을 보낸다
>    (상대 관계 맥락을 Provider에 넘기지 않는다)
>
> 그래서 옵션을 늘려도 점수가 바뀌지 않는다.
> `Record<TargetRelation, string>`(`TARGET_RELATION_LABEL`)이 라벨 누락을 컴파일에서 막는다.

**`잘 모름`의 의미 경계** — `unsure`는 '이 사람과 나의 **관계 이름**을 모르겠다'는 뜻이다.
'상대의 관계 행동을 모른다'와 혼동하지 않는다 — 그건 4축의 `x`(모름)가 담당하고, `x`만
`comparedCount`에서 빠진다. 관계 이름을 모른다고 점수에서 빠지는 항목은 없다.

**Migration** — 값을 추가만 했다. 기존 세션의 `crush`/`friend`/`work`/`intro`는 그대로
유효하고, `resetTargetContext()`는 `relation: null`로 초기화한다.

#### 왜 MBTI를 `declared` 안에 넣지 않았는가

`DeclaredPreference`는 사용자가 직접 표현한 **관계 기준**(연락 중요도·갈등 해결 방식)이고,
MBTI는 별도의 **Self-described Personality Lens**다. 두 데이터를 섞으면 MBTI가 관계 기준의
하나처럼 취급되기 쉬우므로 `answers.mbti`로 분리했다.

#### 제거된 축 — `talk` · `rhythm`

`TargetProfile`/`TargetAxisKey`/`AXIS_DEFINITIONS`에서 `talk`(대화 방식)·`rhythm`(생활 리듬)
축이 **완전히 제거**돼 있다. 예전에는 이 두 축의 '나의 값'을 사진 관찰 고정값(`talk=4`,
`rhythm=3`)으로 하드코딩해서 계산에 넣었는데 **사용자가 실제로 준 데이터가 아니었다.**
질문을 늘리는 대신 축 자체를 뺐다 — 그래서 S19도 6개가 아니라 **4개 필드**만 받는다.

### 5.2 `BirthProfile` — Entertainment Lens 공용

사주와 Astrology가 **같은 정보를 두 번 묻지 않도록** 공용 구조로 만들었다.

```
BirthProfile
├─ date          string | null   'YYYY-MM-DD'. 없으면 어떤 Lens도 계산하지 않는다
├─ time          string | null   'HH:MM'. 없어도 Sun Sign은 가능, 시주·Natal은 불가
├─ timeUnknown   boolean         '태어난 시간을 몰라'를 명시 선택했는지 (미입력과 구분)
├─ calendarType  'solar' | 'lunar'
└─ location      { country?, city?, timezone? } | null
```

| 항목 | 내용 |
|---|---|
| 위치 | `answers.birthProfile`(나) · `answers.target.birthProfile`(상대) |
| 필수 여부 | **Optional.** Main Funnel의 질문이 아니다 — CORE에 쓰이지 않는 정보로 입력 부담을 늘리지 않는다 |
| CORE 영향 | **없음.** 동기화율·Mirror·History 계산 어디에도 들어가지 않는다 |
| Progressive Input | LEVEL 1 생년월일 → LEVEL 2 출생시간 → LEVEL 3 출생지역. 각 Lens가 필요한 수준까지만 요청 |

**Migration** — v1.3 이전 세션에는 `zodiac`(직접 고른 별자리)만 있다. `deserialize()`가 그
값을 `legacyZodiac`으로 옮기고 **`birthProfile`은 비워둔 채로 복원한다** — 별자리에서
생년월일을 역산해 임의로 만들어내지 않는다.

### 5.3 `ObservedProfileResult` — 사진 분석 결과

```
PhotoObservation                     ← LEVEL 1. 사진 1장 = Provider 1회
├─ photoId
├─ scenes · activities · objects · environment   ObservedLabel[]
├─ evidenceSummary
└─ usable                            ← 흐리거나 판단 불가면 false
     ⚠️ 반복·occurrenceCount 필드가 **애초에 없다** — Provider는 사진 1장만 봤다

ObservedSignal                       ← LEVEL 3. 규칙이 집계한 결과
├─ category        'sports'|'outdoor'|'travel'|'food'|'cafe'
│                  |'culture'|'reading'|'pet'|'social'|'other'
├─ label · evidence[]
├─ photoIds        string[]          ← Evidence Provenance
├─ occurrenceCount number            ← **사진 장수가 아니라 서로 다른 장면 그룹 수**
├─ strength        'single'|'repeated'|'strong_repeated'
└─ hasDuplicateLikePhotos

ObservedProfileResult
├─ traits            AiObservedTrait[]   (최대 6개. 0개도 정상)
│   ├─ id · observation · confidence('low'|'medium'|'high')
│   ├─ evidence      ImageEvidence[]   ← 어느 사진에서 봤는지
│   ├─ evidenceText  string?           ← 샘플 세션용 서술
│   └─ signal        ObservedSignal?   ← **실제 분석 결과에만** 있다
├─ evidenceCoverage  { imageCount, usableImageCount, level }  ← **코드가 판정**
├─ observedState     ObservedAnalysisState?
├─ limitations       string[]          ← 못 한 것을 숨기지 않는다
└─ meta              AiAnalysisMeta
    ├─ mode          'real'|'mock'|'demo'|'fallback'|'legacy-demo'
    ├─ promptVersion · analysisVersion
    └─ inputFingerprint                ← 사진이 바뀌었는지 판단
```

#### `ObservedAnalysisState` — 7가지 상태를 구분한다

| 값 | 뜻 |
|---|---|
| `repeated_found` | Vision 성공 + 반복 신호 있음 |
| `single_only` | Vision 성공 + 단일 관찰만 (**No Pattern ≠ No Information**) |
| `no_observation` | Vision 성공했지만 쓸 만한 관찰이 안 나옴 |
| `insufficient_photos` | 분석에 쓸 사진이 부족 |
| `provider_failed` | Provider 실패 |
| `mock` | 개발용 mock |
| `demo` | Provider 미연결 |

**'분석 실패'와 '반복 없음'과 '데모 모드'가 같은 말을 하지 않는다.**

| 항목 | 내용 |
|---|---|
| 사진 원본 | **저장하지 않는다.** 분석 후 메모리에서 사라지고 관찰 결과와 `imageId`만 남는다 |
| 지문 변경 시 | 사용자 확인 상태(`observations`)를 **초기화한다** — 다른 사진의 확인을 재사용하면 거짓이 된다 |
| 구 세션 복원 | `mode: 'legacy-demo'`로 복원하되 **trait은 만들지 않는다**(v1.22) |

### 5.4 `RelationshipHistoryEntry` — 영구 기록

`SessionAnswers`와 **완전히 분리된** 영구 데이터다. 저장소도 다르다.

```
RelationshipHistoryEntry
├─ id                     string            (crypto.randomUUID)
├─ analysisId             string            분석 입력 지문 — 중복 저장 방지 키
├─ createdAt              string            ISO
├─ context
│   ├─ relationshipStatus RelationshipStatus | null
│   └─ targetRelation     TargetRelation | null   ← 관계 '맥락'만. 상대 식별 정보 아님
├─ profileSnapshot
│   └─ mbti               MbtiType | null   ← metadata 전용. 변화 판정에 쓰지 않는다
├─ declaredSnapshot       DeclaredPreference
├─ relationshipEvidence   { important, hardest, selfGap, adaptive }
├─ mirrorSnapshot
│   ├─ insights[]         { axis, state, declaredText, relationshipSignal }
│   └─ focusAxis          MirrorAxisKey | null
├─ coreInsight            { original, userCorrection, verdict }
├─ evidenceCoverage       Confidence
├─ audience?              'couple' | 'solo'   ← 없으면 couple. 기존 기록을 다시 쓰지 않는다
└─ soloSnapshot?                              ← Solo 관찰에만 있다
    ├─ signals[]          { axis, level }      단계 키만. 문장은 표시할 때 복원한다
    ├─ headline           string               그때 화면 맨 위에 보인 한 문장
    ├─ pairIds[]          string               규칙 id만 (문장 아님)
    ├─ sources[]          'declared'|'mbti'|'observed'|'experience'
    └─ observed?[]        { category, occurrences, strength }   ← v1.35 · 활동 범주만
```

⚠️ **`audience`와 `soloSnapshot`은 optional이다** — v1.33 이전 기록에는 없고, 그건 전부
커플 관찰이다. **파괴적 migration을 하지 않는다**(자동 backfill 금지). `soloSnapshot.observed`도
같은 원칙이라 `undefined`(그때는 저장하지 않았음)와 `[]`(사진 근거가 없었음)를 구분한다.

**저장하지 않는 것** — 사진 원본 · base64 · AI 서술 원문 · 상대 이름 · 상대 사진 ·
상대 자유서술 원문 · 민감정보.
History의 주체는 '상대'가 아니라 **'나'**다. 그래서 `target`에서도 `relation`만 가져오고,
`experience.note`(자유서술)와 `photos`는 아예 스냅샷에 넣지 않는다.

| 항목 | 값 |
|---|---|
| 저장 키 | `localStorage['lym.history.v1']` (세션과 분리) |
| 접근 경계 | `historyRepository.ts`가 localStorage를 만지는 **유일한 지점**. 화면은 `useHistory()`만 쓴다 |
| 정렬 | 항상 `createdAt` 오름차순 (오래된 것 → 최신) |
| 손상 데이터 | 스키마가 안 맞는 항목은 복원 시 **조용히 버린다** — 깨진 기록으로 화면을 터뜨리지 않는다 |
| 항목 식별 | **분석 시점(날짜)으로만** 표시. 상대 이름·연애 기간·'몇 번째 관계'로 라벨링하지 않는다 |
| MBTI 변화 | **해석하지 않는다.** INFP → ENFP가 되어도 '관계를 통해 외향적으로 변했다' 같은 Insight를 만들지 않는다 |

---

## 6. 화면별 상세 명세

### S01 · 스플래시 — `/`

| 항목 | 내용 |
|---|---|
| 목적 | 브랜드 첫인상 + 신규/재방문 분기 |
| 구성 | `LOVE RESEARCH : EARTH` 라벨 → 러비 `hero`(196px, float) → 서비스명 → `인간의 사랑을 관찰 중` |
| 분기 | `hydrated && completed.profile === true` → `/home` (재방문자) · 그 외 → `/onboarding` |
| Analytics | 없음 |

---

### S02~S04 · 온보딩 3단 — `/onboarding`

| 슬라이드 | 헤드라인 | 비주얼 | CTA |
|---|---|---|---|
| 1 | "이 사람이랑 나, 잘 맞을까?" | 나/그 사람 신호 바 4줄 비교 | 다음 |
| 2 | "근데 잠깐. 너는 연애할 때 어떤 사람이야?" | Declared Me vs Relationship Me GAP 예고 카드 | 다음 |
| 3 | "상대를 보다 보면 의외로 네가 더 잘 보일지도 몰라." | 러비 `crystal` | **나부터 관찰하기** |

슬라이드 2가 제품의 핵심 가설을 미리 보여준다 — `"연락은 별로 중요하지 않아."`(DECLARED) vs
`"연락이 줄어드는 게 가장 힘들었어."`(RELATIONSHIP).

- **쓰는 상태:** `completed.onboarding = true`
- **Analytics:** `onboarding_complete` `{ skipped, last_step }`

---

### S05 · 관계 상태 — `/status`

| 항목 | 내용 |
|---|---|
| 목적 | 현재 관계 상태 확인 + **지원 범위를 정직하게 밝힌다** |
| 선택지 | `solo_none` · `solo_exp` · `crush` · `dating` · `married` · `ended` (6개) |
| 지원 범위 | `STATUS_SUPPORTED`에서 **`solo_exp`(솔로·경험 있음)와 `crush`(관심 가는 사람 있음)만 `true`.** 나머지 행에는 `준비 중` 표시 |
| 캡션 | "지금은 '연애 경험 있음'과 '관심 가는 사람 있음' 상태에 맞춰져 있어. 다른 상태는 준비 중이야." |
| 미지원 선택 시 | 흐름을 **막지 않는다.** 토스트로 "이 상태에 맞는 분석은 아직 준비 중이야. 우선 지금 흐름으로 안내할게."를 띄우고 같은 Main Flow로 진행 |
| 원칙 | **'상태에 따라 다르다'고 말해놓고 몰래 같은 흐름을 주지 않는다** — 다르지 않다는 걸 명시한다 |
| Analytics | `relationship_status_select` `{ status, supported }` |

---

### S06 · 프로필 빌딩 인트로 — `/profile/intro`

3-Layer 구조(Observed Me / Declared Me / Relationship Me)를 먼저 설명한다.

| 항목 | 내용 |
|---|---|
| 구성 | 러비 `record`(52px) 말풍선 → 설명 → `DATA_LAYERS` 3단 타임라인 → 프라이버시 안내 |
| Primary CTA | **관찰 시작** → `/profile/photos` · 캡션 `약 3분 · 중간에 저장돼` |
| Secondary CTA | **샘플 답변으로 결과부터 볼게** → `loadSampleSession()` → `/profile/result` |
| Analytics | `profile_building_start` |

> ⚠️ **샘플 세션 진입점은 Production에도 노출된다.** 이 버튼은 게이팅이 없다.
> 누르면 `createSampleAnswers()`가 dev 샘플 데이터(한사랑 페르소나)를 채우고,
> `observedAnalysis`에는 `buildSampleObservedResult()`의 **고정 관찰 4개**가 들어간다
> (`DEMO AI` 배지 + "이건 화면 확인용 샘플 세션이야" 안내 동반).
> 사용자가 직접 '샘플로 보겠다'고 선택한 경로이며, **사진을 업로드한 사용자의 결과 경로와는
> 완전히 분리돼 있다**(§8.4). 다만 그 evidence 문장이 '반복적으로 관찰됐어'라고 반복을
> 주장하는 것은 §8.5 원칙과 어긋나며 **미해결 항목**이다.

---

### S07 · 사진 입력 — `/profile/photos`

| 항목 | 내용 |
|---|---|
| 목적 | 관찰에 쓸 사진 선택 (업로드 또는 샘플 타일) |
| **입장권이 아니다 (v1.37)** | 사진이 없어도 퍼널이 진행된다. `사진 없이 질문부터 시작`이 **분석을 돌릴 수 없는 동안 항상** 보이고, 누르면 고른 타일을 지운 뒤 `/profile/declared/1`(Revisit이면 원래 자리)로 간다 |
| 최소/최대 | **업로드한 사진** `PHOTO_MIN_COUNT`(3)장 이상 · 최대 9장 |
| **샘플 타일은 세지 않는다 (v1.37)** | `isPhotoSelectionValid()`가 `source === 'upload'`만 센다. 샘플 타일은 색 타일이라 `prepareImagesForAnalysis()`가 전부 걸러내고 서버가 `NO_USABLE_IMAGE`를 돌려준다 — 셀 수 없는 것을 조건에 넣지 않는다 |
| 허용 타입 | `image/jpeg` · `image/png` · `image/webp` |
| **HEIC 제외** | `image/heic`는 accept에서 뺐다 — 미리보기 변환 로직이 없는 채로 넣어두면 브라우저에 따라 깨진다. 선택 시 "이미지 파일이 아니에요" 토스트로 **정직하게 거절**한다 |
| 샘플 배지 | 샘플 타일을 하나라도 고르면 `샘플 타일 · 화면 확인용` 배지. 선택 수 줄에도 `샘플 타일 N개는 분석 제외`가 함께 붙는다 |
| 샘플 타일 안내 (v1.37) | 버튼 이름은 `샘플 타일 채우기`다(구 `샘플 사진으로 체험`). 샘플만 골라 진행하면 **여기서** `샘플 타일은 색 타일이라 러비가 볼 수 있는 게 없어…`로 멈춘다 — 6.1초를 기다리게 한 뒤 실패 화면으로 보내지 않는다 |
| Primary 라벨 | `러비에게 보여주기 · N장`의 N은 **업로드 수**다. 전송되지 않을 타일을 보여줄 장수로 세지 않는다 |
| 안내 문구 | **모드를 따른다.** real → `photoAiNotice`("서버로 전송될 수 있어… AI 제공사의 데이터 보관 정책은 우리가 통제하지 않아") · demo → `photoDemoNotice`("지금은 데모 모드라 사진을 전송하지 않아") |
| 프라이버시 원칙 | '즉시 삭제됩니다' 같은 보장을 **하지 않는다.** Provider의 보관 정책을 우리가 확인하지 않았으므로 **우리가 하는 일만** 말한다 |
| 다음 | `/profile/analyzing` (항상 재분석) · 또는 `사진 없이 질문부터 시작` → `/profile/declared/1` |
| Analytics | `photo_input_complete` `{ count, uploads, skipped }` — **새 이벤트를 만들지 않는다**(§10.5). `skipped: true`가 사진 없이 진행한 경우다 |

---

### S08 · 사진 분석 관찰 — `/profile/analyzing` (+ E2)

| 항목 | 내용 |
|---|---|
| 연출 | `LovyObservation` 4단계 (§4.7) |
| **이동 조건** | **`연출 완료 && 실제 분석 완료` 둘 다 만족해야** 이동한다. 분석이 늦으면 마지막 단계에서 기다린다 |
| 중복 호출 방지 | `aiClient`의 in-flight 병합 + 세션 캐시 |
| 실패 시 | E2가 아니라 이 화면에서 선택지를 준다 — **다시 분석 / 사진 다시 고르기 / 질문으로 계속하기** |
| 실패해도 막지 않는 이유 | **사진 분석 실패가 Core Funnel을 막지 않는다.** 질문으로 계속할 길을 항상 준다 |
| 오류 표시 | 오류 코드를 그대로 보여주지 않고 `AiFailureReason`별 사람 말로 바꾼다 |
| Analytics | `photo_analysis_start` `{ photo_count }` · `photo_analysis_complete` / `_failure` |

> **StrictMode 이중 마운트 대응** — 예전에는 `retryCount + 사진 id` 조합을 `useRef`로 기억해
> **effect 자체를 건너뛰었다.** StrictMode는 mount → cleanup → mount를 연달아 실행하므로,
> 첫 mount의 cleanup이 `cancelled = true`를 세팅한 뒤 두 번째 mount가 가드에 걸려 **아무
> 실행도 남지 않았다** — 분석은 200 성공인데 화면이 로딩에서 멈췄다.
> 지금은 두 mount 모두 실행하고, `useRef`는 **Analytics를 한 번만 보내는 용도**로만 쓴다.

---

### S09 · Observed Me 결과 ⭐ — `/profile/observed`

러비의 관찰은 **초안**이고, 사용자가 고칠 수 있다.

#### 정상 결과 화면

| 요소 | 규칙 |
|---|---|
| 배지 | `OBSERVED ME` + 근거 배지 + 모드 배지 (§4.12) |
| 근거 배지 | 사진 장수가 아니라 **실제 사용된 근거 수** — `근거: 사진 2/8장` |
| 헤드라인 | `repeated_found` → "사진에서 반복해서 보인 활동이야" · `single_only` → "사진에서 이런 장면이 보였어" · 그 외 → "사진에서 이런 모습이 보였어" |
| `single_only` 안내 | 목록 맨 위에 "같은 활동이 여러 번 반복해서 보이지는 않았어"를 먼저 말하고 **단일 관찰은 그대로 보여준다** — No Pattern ≠ No Information |
| 카드 구성 | **활동 이름**(`스포츠 관람·운동`)과 **반복 근거**(`사진 3장에서 비슷한 장면이 반복해서 보였어`)를 분리. 반복 횟수는 규칙이 센 값이라 활동 이름과 한 줄에 섞으면 단정처럼 읽힌다 |
| 사용자 검증 | 맞아 / 조금 달라(수정) / 분석 제외 |
| 수정 시트 | 러비의 **원래 관찰 문장을 함께 보여준다** — AI Original을 덮어쓰지 않고 `original`에 보존 |
| `limitations` | 분석이 못 한 것을 하단에 그대로 나열한다 |
| 금지 표현 | **'생활 패턴'을 쓰지 않는다.** 사진 몇 장으로 판정할 수 있는 것이 아니다 |

#### 빈 상태 — 상태별로 다른 말을 한다

| 조건 | 제목 | 본문 |
|---|---|---|
| 분석 없음 | 아직 사진을 관찰하지 않았어. | 사진을 고르고 관찰을 시작하면 여기에 결과가 보여. |
| **사진 0장** (`photosGone`) | 지금은 볼 수 있는 사진이 없어. | 이전 관찰 기록은 남겨뒀어. 사진을 다시 고르면 새로 관찰할 수 있어. |
| **사진 바뀜** (`photosChanged`) | 사진이 바뀌어서 이 관찰은 더 못 써. | 지금 사진으로 다시 관찰해야 해. 예전 결과를 현재 관찰인 것처럼 보여주진 않을게. |
| `insufficient_photos` | 관찰에 쓸 사진이 조금 부족했어. | 사진이 조금 더 있으면 반복되는 모습을 확인하기 쉬워. 없는 걸 있다고 하진 않을게. |
| `provider_failed` | 사진 분석을 완료하지 못했어. | 내 쪽 문제라 사진 내용을 읽지 못했어. 다시 시도하거나 질문으로 계속해도 괜찮아. |
| `demo` | 지금은 데모 분석을 사용 중이야. | 실제 사진 내용을 분석하지 않았어. 그래서 관찰 결과도 만들지 않았어. |
| `mock` | 지금은 개발용 MOCK 분석이야. | 〃 |
| 그 외 | 사진은 봤는데, 관찰로 쓸 만한 장면을 못 찾았어. | 무엇을 하고 있는 장면인지 읽히지 않았어. 없는 걸 있다고 하진 않을게. |

빈 상태 CTA는 항상 두 갈래 — **사진 더 고르기** / **질문으로 계속하기**.

#### Stale 판정 (v1.22)

저장된 `analysis.meta.inputFingerprint`를 현재 `photoFingerprint(answers.photos)`와 대조한다.
지문은 사진 id와 순서를 모두 반영하므로 **한 장만 바꿔도** 값이 달라진다.

```
photosChanged =
  !photosGone
  && analysisFingerprint !== null
  && analysisFingerprint !== 'legacy'   // v1.5 이전 세션 복원
  && analysisFingerprint !== 'sample'   // 샘플 세션
  && analysisFingerprint !== photoFingerprint(answers.photos)
```

다르면 trait과 **예전 `limitations`까지 모두 숨긴다** — 예전 한계 문구가 현재 상태처럼
읽히면 안 된다.

**재현 경로:** 새로고침 → `deserialize()`가 `source:'upload'` 사진을 제거 → 샘플 사진이
남아 있으면 `photos.length > 0`이라 `photosGone`에 걸리지 않음 → Profile Revisit이나
Declared 1단계 back으로 이 화면에 재진입.

| 항목 | 내용 |
|---|---|
| Validation | 최소 1개 항목에 확인 또는 수정을 남겨야 다음으로 진행 |
| Analytics | `photo_observation_confirm` `{ category, strength, ai_mode }` · `observed_profile_complete` `{ confirmed, corrected, excluded }` |
| 다음 | `/profile/declared/1` (또는 `from=profile-revisit`이면 `/profile/result?view=revisit`) |

---

### S10~S13 · Declared Me — `/profile/declared/[1-4]`

한 화면에 질문 1개 원칙. S13만 2개(애정 표현 + 취미).

| WF | 필드 | 질문 |
|---|---|---|
| S10 | `contact` | 연락 중요도 (1~5) |
| S11 | `conflict` | 갈등 해결 방식 (`now`/`soon`/`space`) |
| S12 | `alone` | 개인 시간 중요도 (1~5) |
| S13 | `affection` + `hobby` | 애정 표현 · 취미 공유 |

#### S13 Optional Section — 내 MBTI

| 항목 | 내용 |
|---|---|
| 위치 | S13 하단 `PERSONALITY LENS · 선택` 섹션 |
| 제목 | **`네 MBTI를 안다면 알려줘`** *(v1.22)* |
| 캡션 | "MBTI는 관계를 판단하는 기준은 아니지만, 서로 다른 성향을 이야기해보는 참고 정보로 사용할게." |
| 구성 | 16개 유형 칩 + **`잘 모르겠어`** 칩(= `null`) |
| **진행을 막지 않는다** | `isDeclaredStepComplete()`는 `affection`/`hobby`만 검사한다. MBTI 미선택으로 **다음**을 눌러도 정상 진행 |
| 별도 화면을 만들지 않은 이유 | 프로필 빌딩 흐름이 이미 길다. S13a를 새로 만들면 이탈 지점이 하나 늘어난다 |
| Analytics | `self_mbti_select` `{ mbti }` · `declared_me_complete`에 `mbti` 속성 동반(완료 조건 아님, 입력률 관찰용) |

---

### S14~S17 · Relationship Me — `/profile/past/[1-3]`

| WF | 필드 | 질문 | 비고 |
|---|---|---|---|
| S14 | — | 인트로 | **이전 관계 돌아보기** → S15 · **연애 경험이 없어** → E4 |
| S15 | `important` | "이전 관계에서 생각보다 중요했던 건 뭐였어?" | 최대 4개 다중 선택 |
| S16 | `hardest` | "가장 힘들었던 순간은 어떤 쪽에 가까웠어?" | **여기서 Adaptive 트리거 계산** |
| S17 | `selfGap` + `note` | "연애 전 생각했던 너와 실제 연애 속 너는 달랐어?" | 서술은 선택(300자) |

**Analytics:** `relationship_experience_complete` `{ important_count, hardest, self_gap, has_note }`

---

### S16a · Adaptive Follow-up (조건부) — `/profile/past/adaptive`

| 항목 | 내용 |
|---|---|
| 진입 조건 | S16에서 답한 `hardest`(+`important`)와 Declared를 조합해 Mirror 판정을 미리 계산했을 때 **GAP인 축이 있고**, 아직 이 세션에서 그 질문을 안 물어봤을 때만 |
| 목적 | **모든 사용자에게 질문을 늘리지 않는다.** 모순 후보가 발견된 딱 하나의 축에 대해서만 "왜 그랬는지"를 하나 더 묻는다 — Progressive Profiling |
| 구성 | 러비 `question` + "여기서 하나만 더 물어볼게." → 축별 질문 → 5지선다(4개 이유 + '잘 모르겠어') |
| 데이터 | `src/data/adaptive.ts` — 5개 축(`contact`/`conflict`/`alone`/`affection`/`hobby`) 전부에 질문·보기 세트를 미리 정의 |
| Validation | **없음 — 완전히 선택.** 아무것도 고르지 않고 "다음"을 눌러도 진행 |
| 방어 처리 | back으로 돌아와 `hardest`를 바꿔 더는 모순 후보가 아니면 `useEffect`가 조용히 S17로 리다이렉트 |
| 활용처 | S27R Core Insight의 근거 03번 줄(`이렇게 생각한 이유`). **숫자로 환산하지 않는다** |
| Analytics | `relationship_adaptive_answer` `{ axis, option }` (답했을 때만) |

트리거 함수는 `pickAdaptiveTriggerAxis()`(`src/lib/logic/mirror.ts`). S17의 `backHref`는
Adaptive가 실제로 보였으면 S16a로, 아니면 S16으로 동적으로 계산된다.

---

### E4 · 연애 경험 없음 — `/profile/past/none`

Relationship Profile(S18)을 건너뛰고 바로 `/target`으로 이동. `experience.skipped = true`.
이후 Mirror는 E5(사용 불가) 상태가 된다.

---

### S18 · Relationship Profile ⭐ — `/profile/result`

| 항목 | 내용 |
|---|---|
| 목적 | Observed/Declared/Relationship 세 Source를 **나열**한다. **모순이나 Gap을 판정하지 않는다** |
| 로직 | `buildRelationshipProfile()`. **`buildMirrorReport()`를 호출하지 않는다** |
| Summary 생성 | `declaredHighlight()`가 Declared에서 가장 두드러진 특징 1개, `relationshipHighlight()`가 관계 경험에서 신호 1개를 고르고 **판정 없이 나란히** 붙인다 |
| Summary 예시 | `"개인 시간을 중요하게 여기고, 관계에서는 연락이 줄어드는 순간에 민감하게 반응했어."` |
| 금지 | `"~라고 했지만 실제로는"`처럼 두 값을 견주는 표현 |
| 구성 | `Tag`(관측 완료) + `ConfidenceLabel`(관측 정보 충분/보통/부족) → 헤드라인 "러비가 관찰한 현재의 너" → `ProfileLayerStack`(Layer 칩, **MATCH/GAP/CHANGE 태그 없음**) → 자기 확인 UI → 프라이버시 안내 |
| **빈 레이어를 만들지 않는다 (v1.37)** | 관찰이 0개면 `OBSERVED ME` 레이어 **자체가 없다**(§0 '없으면 그 섹션만 없다'). 예전에는 사진 0장에도 레이어가 남아 `OBSERVED ME · 사진 관찰 / 아직 기록이 없어`가 결과 맨 위에 떴다 |
| **합친 관찰 수 (v1.37)** | 요약 라벨이 실제 레이어 수를 따라간다 — `세 관찰을 합친 결과` / `두 관찰을 합친 결과`. 둘을 보여주면서 셋을 합쳤다고 말하지 않는다 |
| Personality Lens 표시 | `answers.mbti`가 있으면 `ProfileLayerStack` 맨 아래 `PERSONALITY LENS · 참고` 행으로 유형만 작게. **3-Layer 타임라인 안에는 넣지 않는다** |
| Personality Lens 제약 | Summary의 근거로 쓰지 않는다. `buildProfileSummary()`는 `answers.mbti`를 **아예 읽지 않는다** |
| OBSERVED ME 칩 | `observedAnalysis.traits` 기준(고정 목록이 아니다) — 사진을 다시 분석하면 칩도 바뀐다 |
| CTA (기본) | **이제 상대를 관찰하기** → `/target` |
| CTA (Revisit) | **정보 수정** + **최근 Mirror 보기** → `/mirror?view=revisit` — 상대 입력 Funnel로 다시 강제로 보내지 않는다 |
| Analytics | `profile_complete` `{ confidence, observed_items }` · `profile_result_revisit` `{ source }` · `result_edit_entry` `{ section }`. **v1.37 — `observed_items`는 `layers`를 id로 찾는다**(인덱스 0이 아니다). observed 레이어가 빠지면 Declared 개수를 Observed 개수라고 보내게 된다 |

#### Edit Return — 수정하고 제자리로 돌아오기

Revisit에서 편집 시트(사진 관찰 다시 보기 / 관계 성향 답변 고치기 / 이전 관계 경험 고치기)를
열면 각 링크에 `?from=profile-revisit`가 붙는다(`src/lib/returnTo.ts`).

- 대상 Funnel(Observed / Declared 1~4 / Past 1~3 + Adaptive)은 **화면·질문 UI를 전혀 바꾸지 않는다**
- **그 세그먼트의 마지막 스텝이 완료됐을 때만** 원래 다음 Funnel 화면 대신
  `/profile/result?view=revisit`로 돌아온다 (`resolveReturnDestination()`)
- 중간 스텝은 `withReturnTo()`로 `from`을 그대로 들고 다닌다

#### 수정의 파급 범위

Profile·Compatibility·Mirror는 모두 세션의 **순수 함수**(`useMemo`)라 새로운 무효화 로직이
필요 없다 — 다음 렌더에 자동으로 최신 값이 된다.

| 수정 대상 | 영향 |
|---|---|
| Declared | Profile · Compatibility · Mirror 전부 갱신 |
| Relationship Experience | Profile · Mirror 갱신, Compatibility의 Past Observation만 갱신 |
| Target | **Compatibility만** 갱신, Mirror 불변 (`useMirror()`가 `target`을 아예 읽지 않는다) |
| Observed Correction | Profile과 관련 Cross-source Insight만 갱신 |
| MBTI | Supporting Lens만 갱신 — Main Score/Mirror는 `answers.mbti`를 읽지 않으므로 불변 |
| Birth Profile | Entertainment Lens만 갱신 — Core는 `birthProfile`을 읽지 않으므로 불변 |
| **History Snapshot** | **수정해도 다시 쓰지 않는다.** 그 당시 저장된 그대로 남는다 |

---

### S19 · 상대 정보 입력 — `/target`

| 항목 | 내용 |
|---|---|
| 헤드라인 | "이번엔 그 지구인을 알려줘." · 캡션 "네가 아는 만큼만 골라도 돼. 모르는 건 비워둬." |
| 관계 맥락 | `이 사람과 나는` — **6개 옵션**(§5.1): 알아가는 중 / **썸 타는 중** / 친구 / 같이 일하는 사람 / 소개로 만남 / **잘 모름** |
| 관계 행동 4축 | 연락 방식 · 갈등 방식 · 개인 시간 · 애정 표현 — 각 `뜸한 편`/`보통`/`자주` 형태 3단 + 미선택(`x` = 모름) |
| **6개→4개로 줄인 이유** | `talk`·`rhythm`은 Compatibility 계산 자체에서 빠졌다 — **어차피 안 쓰일 데이터를 상대에게 물어보지 않는다** |
| UI 자동 반영 | 화면이 `TARGET_FIELDS.map(...)`으로 렌더하므로 데이터 파일만 바꿔도 화면이 따라간다 |
| 상대 MBTI (선택) | `PERSONALITY LENS · 선택` 접이식. 16유형 + `모름`. **`TARGET_FIELDS`/`known_count`/동기화율 어디에도 미포함** |
| MBTI Copy 규칙 | "점수에는 넣지 않지만, 둘 사이의 성향 차이를 보는 참고 렌즈로 사용할게." ❌ '궁합 점수를 더 정확하게 만들기 위해 입력해주세요' 금지 |
| 좋아하는 것 (선택) | `다가가는 힌트 · 선택` 접이식, 최대 5개. Approach Hints 생성용 |
| 렌즈 링크 | 하단 `러비의 다른 관측 렌즈` → `/lens` |
| Validation | **아는 항목 최소 1개.** 3개 미만이면 "N개 더 알려주면 동기화율을 계산할 수 있어" 안내 |
| Analytics | `target_profile_complete` `{ relation, known_count }` · `target_mbti_select` · `target_preference_add` |

---

### S20 · 궁합 관찰 — `/compatibility/analyzing` (+ E2)

| 항목 | 내용 |
|---|---|
| 연출 | `LovyObservation` 4단계 **누적** · 토큰 = **실제로 비교하는 4개 관계 축** (화면에 없는 신호를 그리지 않는다) |
| 총 시간 | **2.5초** (`520 × 4 + 420`) — Release Gate 실측 median: 첫 관찰 **2,633ms** · 재관찰 **1,443ms** · Reduced Motion **776ms** |
| 성격 (v1.38) | **기다리는 화면이 아니라 전환이다.** 궁합은 deterministic 계산이라 Provider를 기다리지 않는다 — 연출이 결과보다 늦으면 그건 연출이 사용자를 막고 있는 것이다 |
| 재관찰 (v1.38) | `completed.compatibility`가 true면 ×0.55로 짧아진다. 새 상대는 v1.35 reset이 이 값을 false로 되돌리므로 **자동으로 전체 연출**이 된다 |
| 계산 시점 | 진입 즉시 `calculateCompatibility()`를 **연출과 병렬로** 실행. 이동은 `연출 완료 && 계산 완료`일 때만 — 값이 없으면 0으로 채우지 않고 기다린다 (v1.38) |
| Caveat | "입력에 없는 건 추측하지 않을게." |
| Footer | "입력된 정보 기준으로만 비교 중" |
| **v1.22 부수 동작** | 진입 시 `clearScrollPosition('compat:<funnelAnalysisId>')` — **새 분석은 맨 위에서 시작**(§4.9) |
| E3 분기 | `comparedCount < TARGET_MIN_KNOWN(3)`이면 `score === null` → 결과 화면이 `LowConfidenceView` |
| Analytics | `compatibility_complete` `{ score, compared }` 또는 `compatibility_low_confidence` `{ compared }` |

---

### S21R · Compatibility Result ⭐ — `/compatibility`

> 구 S21(Hero)~S25(Questions) **5개 화면을 하나로 합친** 화면이다. Mental Model이 하나
> ("우리 둘은 어떻게 맞는가?")인 결과를 5번 이동해야 다 볼 수 있던 문제를 고쳤다.

#### 화면 구성 순서 (v1.23 — 4단계 읽기 구조)

```
ReportHeader           LOVY OBSERVATION REPORT · 이번 관계에서 발견한 신호
                       관찰한 신호 N개 · 비교한 신호 M개 · YYYY.MM.DD 작성
╔═ LEVEL 1 · 5초 ════════════════════════════════════════════════════════╗
 01 SUMMARY            SYNC RATE · 동기화율 (76px)
                       '연애 성공확률이 아니야'
                       ▸ 핵심 한 문장          ← 결과 요약 (17px semibold)
                       ▸ 근거 한 줄 (#why)     ← 구 02 METHOD 흡수
 ── FIRST SURPRISE ──  러비의 의문 + YOUR SIGNAL + '실제 관계 신호 보기 ↓'
 ResultSectionNav      앵커 칩 5개 (flex-wrap · 시각 32px / 터치 44px)
╚════════════════════════════════════════════════════════════════════════╝
╔═ LEVEL 2 · 30초 ═══════════════════════════════════════════════════════╗
 02 WHERE IT CONNECTS  잘 맞는 신호 — PRIMARY 1개(카드) + 'N개 더 보기'(compact)
 03 DIFFERENCE         확인이 필요한 신호 — PRIMARY 1개 + LOVY OBSERVATION + compact
 ── PREMIUM ENTRY ──   (friction이 있을 때만)
 ── 과거 관찰 ──        (반복 신호가 있을 때만)
╚════════════════════════════════════════════════════════════════════════╝
╔═ LEVEL 3 · 1~3분 ══════════════════════════════════════════════════════╗
 04 NOW WHAT           그래서 뭘 해볼까
   ├ 이 사람에게 다가갈 때 (#approach)  PRIMARY 1개 + compact 나머지
   └ 이야기해볼 질문      (#questions)  추천/저장 Segmented Control
╚════════════════════════════════════════════════════════════════════════╝
 05 OTHER LENSES       editorial index — MBTI · 사주 별자리 (divider 행)
 [sticky CTA]          다음 관찰 보기 → /mirror/teaser
```

섹션 번호는 **실제로 렌더된 것만** 센다 — 조건부로 빠지는 섹션이 있어도 번호가 건너뛰지 않는다.

#### v1.23에서 무엇을 바꿨고 무엇을 바꾸지 않았나

| 바꾼 것 | 근거 |
|---|---|
| 점수 아래에 **핵심 한 문장** 추가 | 첫 viewport에 결과 요약이 없고 면책 문장만 3개였다 |
| 구 `02 METHOD` 섹션 → SUMMARY의 근거 한 줄로 흡수 | 145px을 쓰면서 새 정보 0, SUMMARY와 문장 중복 |
| `SyncScore` 96px → **76px**, generic 설명 2줄 제거 | LEVEL 1을 첫 viewport 안에 넣기 위해 |
| `ResultSectionNav`를 FIRST SURPRISE **뒤로** 이동 + 라벨 재작성 | 점수 바로 아래 72px이 결과 묶음을 밀어냈다 |
| FIRST SURPRISE body 축약 | 방법론 설명('축을 하나씩 본다')이 핵심 한 문장과 중복 |
| 신호/힌트 카드에 `density="compact"` 도입 | 동일 규격 카드 나열로 무엇이 중요한지 사라졌다 |
| 구 `06 APPROACH` + `07 NEXT QUESTION` → **04 NOW WHAT** 병합 | 같은 Mental Model인데 별개 기능처럼 보였다 |
| `05 OTHER LENSES`를 Core·Action **뒤로** 이동 + editorial index화 | Lens(510px)가 Core 신호(427px)보다 무거웠다 |
| `SectionLabel`에 `as` 슬롯, Approach 제목 h4 | heading 계층이 평평했다 |

| 바꾸지 않은 것 |
|---|
| **anchor id 7개 전부** — `#summary` `#why` `#good` `#friction` `#lenses` `#approach` `#questions` |
| Legacy Redirect 5개 · `RESULT_ANCHORS` · `useAnchorScroll` · `useScrollRestore` |
| **Analytics 이벤트 0개 추가/삭제** — `compatibility_result_view`(Primary KPI) 포함 전부 그대로 |
| Compatibility Score · tone · Good/Friction 판정 · Approach Hints · 대화 질문 생성 |
| Premium Entry **위치**(friction 직후)와 `premium_entry_view` attribution |
| Mirror Teaser로 가는 sticky CTA |

#### 실측 (375×812, friction 1 + good 1 + MBTI 양쪽 + interests 2 + Premium Hook)

| 지표 | v1.22 | v1.23 |
|---|---|---|
| 총 높이 | 4,307px | **3,755px** (−13%) |
| 화면 수 (620px 기준) | 6.9 | **6.1** |
| `rounded-card` 개수 | 12 | **7** (−42%) |
| 01 SUMMARY 높이 | 451px | **300px** |
| FIRST SURPRISE 시작 위치 | 589px | **434px** |
| OTHER LENSES 높이 | 510px | **294px** (−42%) |
| APPROACH 높이 | 767px | 588px (−23%) |

#### Section Anchor

`#summary` `#why` `#good` `#friction` `#lenses` `#approach` `#questions`
(`RESULT_ANCHORS`, `src/lib/routes.ts`). Legacy Redirect · Home 카드 · `ResultSectionNav` 칩이
모두 이 id로만 이동한다.

#### 핵심 한 문장 — `selectResultHeadline()`

LEVEL 1에서 '그래서 우리 관계는 어떤데?'에 답하는 **한 문장**이다.

```
score === null(E3)                 → null (이 화면은 LowConfidenceView로 빠진다)
friction[0] && good[0]             → "{good}에 대한 기대는 비슷해 보이는데, {friction}에서는 차이가 보여."
friction[0]만                       → "{friction}에서 두 사람의 답이 서로 다르게 나왔어."
good[0]만                           → "{good 상위 2개}에 대한 기대가 비슷해 보여."
전부 neutral                        → "아주 다르지도, 아주 비슷하지도 않은 신호들이야."
```

- **새 계산 0 · 새 AI 호출 0 · 랜덤 0.** 이미 규칙이 판정한 tone 결과의 **라벨만** 읽는다
- friction을 먼저 말한다 — 사용자가 먼저 알아야 할 것은 차이다
- good이 여러 개여도 **상위 2개까지만.** 4개를 나열하면 문장이 목록이 된다
- 모든 축이 neutral이면 **판정을 만들지 않는다**

#### FIRST SURPRISE + YOUR SIGNAL

무료 결과를 이해한 **직후**, 근거를 읽기 전에 한 번 나온다. Summary와 본문 사이.

| 요소 | 내용 |
|---|---|
| 목적 | "궁합 점수를 주는 서비스인 줄 알았는데, 관계를 보는 방식이 다르네" |
| variant `gap` | hook `잠깐. 숫자만 보면 놓치는 게 하나 있어.` (friction이 있을 때) |
| variant `alignment` | hook `궁합 점수가 높아도 확인은 필요해.` *(v1.22)* |
| **YOUR SIGNAL** | 그 관찰이 **이 사용자의 어떤 답에서 나왔는지** 한 줄 |
| CTA | `실제 관계 신호 보기 ↓` — **무료 본문**으로만 내려간다 |
| 금지 | Premium 광고가 아니다. 가격·잠금·결제 CTA를 넣지 않는다 |
| E3에서는 | **표시하지 않는다** — 관측 정보가 부족한데 사람에 대한 생각을 지어내지 않는다 |

**YOUR SIGNAL 생성 규칙** — 이미 계산된 `CompatibilityResult`에서만 파생된다.

```
근거 축 = frictionSignals[0] ?? goodSignals[0] ?? 비교 가능한 첫 축
tone 'watch' → "{label}에서는 두 사람의 답이 서로 다르게 나왔어. (나: X · 상대: Y)"
tone 'good'  → "{label}에 대해서는 둘이 비슷하게 답했어. (나: X · 상대: Y)"
그 외        → "{label}은 아주 다르지도, 아주 비슷하지도 않게 나왔어. (나: X · 상대: Y)"
비교 가능한 축이 없으면 → null (렌더하지 않는다)
```

**새 계산 0 · 새 AI 호출 0 · 랜덤 0.** 같은 결과면 언제나 같은 문장이다.

#### 신호 카드 (03 / 04)

| 항목 | 규칙 |
|---|---|
| 표시 | 대표 1개를 펼쳐 보여주고 나머지는 'N개 더 보기' |
| 카드 내용 | 축 라벨 · 나/상대 값 · 차이 게이지 · `이런 점이 편할 수 있어` / `일어날 수 있는 상황` · `이 신호를 본 근거` |
| **문장은 tone을 따른다** | `evidence`/`scene`이 `good`/`neutral`/`watch`/`unknown`별로 따로 있다 — 판정과 반대되는 문장이 나가지 않게 |
| friction이 없을 때 | "지금 입력으로는 큰 차이를 못 찾았어. 차이가 없다는 결론은 아니야 — 아직 내가 못 본 것일 수도 있어." (**과장하지 않는다**) |
| LOVY NOTE | 신호 목록 **직후**(더 보기 버튼 위)에 배치 *(v1.22 — 위계 조정)* |
| Analytics | `result_section_expand` `{ section }` |

#### 스크롤 위치 복원 (v1.22)

```ts
useScrollRestore(`compat:${funnelAnalysisId}`, result.score !== null)
```

Lens 상세·MBTI 상세를 보고 Back으로 돌아오면 읽던 위치로 되돌아온다. 상세 규칙은 §4.9.

#### Revisit 모드

| 항목 | 내용 |
|---|---|
| 진입 | `?view=revisit&source=home\|history\|direct\|share` |
| 헤더 | 타이틀 "최근 궁합 결과" |
| CTA | Mirror가 완료됐으면 **Relationship Mirror 다시 보기** → `/mirror?view=revisit` |
| Bottom Nav | Revisit일 때만 표시 |
| Analytics | `compatibility_result_revisit` `{ funnel_analysis_id, source }` (mount 1회, StrictMode 가드) |

#### E3 · 관측 정보 부족

`comparedCount < 3`이면 점수 대신 `LowConfidenceView`. "이 지구인에 대해서는 아직 아는 게
별로 없네." — 점수를 만들어내지 않는다.

#### Analytics (Primary KPI 포함)

| 이벤트 | 시점 |
|---|---|
| **`compatibility_result_view`** | 마운트 시 `trackOnce` — **Primary KPI 분모.** Revisit이어도 같은 정책 |
| `compatibility_analysis_result_view` | `funnelAnalysisId` 기준 dedup — Analysis Funnel 분모 |
| `lovy_surprise_view` | FIRST SURPRISE 표시 (opaque `surprise_variant`만, **문구 원문 미전송**) |
| `result_anchor_navigation` · `result_section_expand` | 섹션 이동·펼침 |
| `both_mbti_available` · `mbti_conversation_question_view` | MBTI Lens (v1.24 — 유형 원문 대신 축 개수만 전송) |
| `approach_hint_view` · `approach_hint_expand` | Approach Hints |

---

### S26 · Mirror Teaser ⭐ — `/mirror/teaser`

> **제품의 시선 전환점.** '상대 분석'에서 '자기 분석'으로 넘어가는 핵심 Aha Moment다.
> Compatibility와 Mirror를 각각 통합했어도 **이 Transition은 삭제하지 않는다.**

| 항목 | 내용 |
|---|---|
| 카피 | "근데 잠깐." → "지금까지는 너와 그 사람을 비교했잖아. 이번엔 너 안에서 조금 다른 신호가 보여." → `TeaserComparison` → "이 둘이 왜 다른지 조금 더 볼까?" |
| E5 (관계 경험 없음) | `mirror.available === false`면 전용 화면 — "아직 관계 경험 데이터는 없어서 생각한 나와 실제 관계 속 나를 비교하긴 어려워." CTA는 홈으로 하나뿐 |
| Revisit에서는 거치지 않는다 | Home의 '최근 Mirror 다시 보기'는 이 화면을 건너뛰고 `/mirror?view=revisit`로 직접 간다 |
| Analytics | `relationship_mirror_teaser_view` · **`relationship_mirror_entry_click`**(Primary KPI **분자** — 이 CTA 버튼에만 있다) · `relationship_mirror_postpone` |

---

### S27R · Relationship Mirror Result ⭐ — `/mirror` (+ E1, E5)

> 구 S27(Map)과 S28(Core Insight)을 합쳤다. 둘은 처음부터 같은 Mental Model이었고 S28은
> S27이 이미 계산한 focus 축의 상세일 뿐이었다.

#### 화면 구성 순서

```
헤더 '관계 속의 나'
 ① Mirror Overview      MirrorLegend + MirrorComparisonRow 리스트
 ② Repeated Signal Notice (과거 기록에 같은 신호가 있으면)
 ③ Focus Insight        "러비가 가장 눈여겨본 부분" headline / explanation / evidence
 ④ Evidence List        "이렇게 생각한 이유"
 ⑤ Feedback             맞는 것 같아 / 조금 달라 (+ 수정 BottomSheet)
 ⑥ 내 관찰 기록에 저장   ← History Entry 생성 Trigger
 ⑦ UT Rating Card       (UT_MODE에서만)
 ⑧ Premium Entry        (Secondary)
```

**Focus Insight를 Overview 바로 아래 둔 이유** — 사용자가 '전체 지도 → 그래서 가장 중요한 게
뭔데?'를 **같은 스크롤 안에서** 해결하게 한다. 별도 Route 이동이 필요 없다.

#### Mirror Gap Map

| 항목 | 규칙 |
|---|---|
| 표현 | **레이더 차트를 쓰지 않는다.** `MirrorComparisonRow` 리스트 |
| '말한 나' | 정확한 위치(1~5)로 표시 |
| '관계 속 나' | **방향으로만** — ▲GAP / ▼CHANGE / ✓MATCH |
| 레이더를 버린 이유 | 5개 축을 같은 방식으로 겹쳐 그리면 Relationship Me가 **실제로는 1~5 척도로 직접 수집된 값이 아니라는 사실**이 가려지고, 실제보다 정밀해 보이는 착시가 생긴다 |

#### 저장 (History Entry 생성)

| 항목 | 내용 |
|---|---|
| Trigger | **내 관찰 기록에 저장** 버튼 |
| 순서 | ① 세션 읽기 ② `buildHistoryEntry()` ③ `saveEntry()` ④ `relationship_history_entry_created` ⑤ `relationship_mirror_complete` ⑥ `/history/saved` |
| 중복 방지 | `analysisId`(status+declared+experience 지문)가 같으면 **새 항목을 만들지 않고 기존 항목을 갱신한다.** 저장을 두 번 눌러도 '관계가 2개'가 되지 않는다 |
| 저장 금지 | `mirror.available === false` 또는 `insights.length === 0` — 가짜 History를 쌓지 않는다 |

> ⚠️ **`relationship_mirror_view`(mount) ≠ `relationship_mirror_complete`(저장).**
> 화면을 합쳤다고 mount에서 complete를 발생시키지 않는다.

| 항목 | 내용 |
|---|---|
| AI ORIGINAL vs USER CORRECTION | `mirror.core.headline`(AI) vs `answers.coreCorrection`(사용자) — **분리 보관** |
| headline 우선순위 | 사용자 수정 → AI headline → 규칙 headline |
| E1 (데이터 부족) | 화면 내부 상태. 결론을 내리지 않는다. **v1.37 — 사진 없는 사용자도 본문에 도달한다**: 예전에는 `isLowData`가 사진 3장을 요구해 Declared·관계 경험을 다 답해도 Primary KPI 화면이 영영 막혔다. E1 안내에서 `사진 3장 더 고르기`도 뺐다 — 사진으로는 이 화면이 열리지 않는다 |
| Analytics | `relationship_mirror_view` · **`relationship_mirror_complete`** · `mirror_feedback_positive`/`_edit` · `mirror_result_revisit` |

---

### S29 · 분석 후 홈 — `/home`

| 섹션 | 내용 |
|---|---|
| Hero | `현재 러비가 알고 있는 나` + Core Insight 요약 (`mirror.core?.summary ?? fallbackProfile`) |
| 최근 관찰 | highlights 3줄 |
| **최근 분석** (Current Result Revisit) | 최근 궁합 / 최근 Mirror / 내 관계 프로필 카드 |
| RELATIONSHIP HISTORY | 저장된 관찰 기록 예고 |
| 새 분석 | **새로운 사람과 궁합 보기** — Revisit이 생겼다고 없애지 않는다 (서로 다른 의도) |
| 데이터 삭제 | 하단, 가장 눈에 안 띄는 자리(작은 회색 링크) — 파괴적 동작이라 우선순위를 낮췄다 |

#### 최근 분석 카드 규칙

| 항목 | 내용 |
|---|---|
| 노출 조건 | `completed.compatibility` / `.mirror`가 각각 true일 때만 — 실제로 그 결과 화면에 도달했을 때만. **가짜 Current Result를 만들지 않는다** |
| 데이터 출처 | **새 저장소를 만들지 않는다.** `useCompatibility()`/`useMirror()`를 Home에서도 그대로 호출 — 세션의 순수 함수라 가능하다 |
| History와 구분 | 이 섹션은 **지금 세션의 분석**, 'RELATIONSHIP HISTORY'는 **저장된 과거 Snapshot** |

#### 데이터 삭제

| 항목 | 내용 |
|---|---|
| 인터랙션 | `ConfirmModal` → **전체 삭제** → `deleteAllData()` → 세션 초기화 + objectUrl 해제 → 토스트 → `/` |
| History 동시 삭제 | 모달의 체크박스(**기본 켜짐**). 기록이 0개면 체크박스를 숨긴다 |
| `deleteAllData()` vs `reset()` | 전자는 `session_data_deleted`, 후자(dev 패널)는 `session_reset` — 실제 사용자 삭제와 개발용 초기화를 analytics에서 구분 |
| Analytics 로그 | 삭제해도 `lym.analytics.v1`은 지우지 않는다 — 운영 지표는 별도 관리 대상 |

#### Bottom Navigation (4탭)

| 탭 | 목적지 | 게이팅 |
|---|---|---|
| 홈 | `/home` | 항상 |
| 나 | `/profile/result?view=revisit` | `completed.profile` — 미완료면 토스트만 |
| 분석 | `/compatibility` | `completed.compatibility` — 미완료면 토스트만 |
| 관찰기록 | `/history` | **없음.** History가 이미 Empty State를 직접 안내한다 |

**표시 조건** — 새 persisted boolean을 만들지 않고 기존 `completed.*`/Revisit 여부만 쓴다.

| 화면 | 표시 |
|---|---|
| `/home` · `/history` | 항상 |
| `/compatibility` · `/mirror` · `/profile/result` | **`?view=revisit`일 때만** — 최초 방문은 Funnel의 다음 단계로 이어지는 흐름이라 붙이지 않는다 |
| `/compatibility/lenses` · `/lens/**` · `/premium*` | 붙이지 않음 — 하위 화면이고 자체 back으로 돌아간다 |

'분석' 탭은 `/compatibility`와 `/mirror` 둘 다에서 active로 켜진다 — 사용자에게는 둘 다 같은
Analysis Mental Model이다. **새 `/analysis` Route를 만들지 않았다** — `/compatibility`가 이미
Mirror·Lens·질문·Premium 진입점을 전부 갖고 있어 Hub를 새로 만들면 복제·이중 유지가 된다.

---

### F1 · 관찰 기록 — `/history`

> 목적은 '누구와 언제 만났는지 기록'이 **아니다.** 관계를 거치며 내 기준·반응·Gap·Core
> Insight가 어떻게 변해왔는지 축적한다.
> Relationship Diary ❌ / **Personal Relationship Memory** ⭕
> 제품 질문: **"나는 관계를 거치면서 어떻게 달라지고 있지?"**

**만들지 않은 것** — 전 애인 리스트 · 상대 이름 중심 Timeline · 연애 기간 · 매일 감정 일기 ·
SNS Feed · 관계 횟수 카운트 · '몇 번째 연애' · 성장 점수 · 건강한 연애 점수 ·
'과거보다 좋아졌다'는 자동 판정 · MBTI 변화를 성격 변화로 해석하는 문장.

시각적 우선순위: ① 현재 Insight ② 의미 있는 변화 ③ 과거 관찰(반복 신호) ④ Timeline.
Dashboard·Chart·CRM·Calendar처럼 만들지 않는다 — **Editorial Personal Archive**다.

| 요소 | 내용 |
|---|---|
| 제목 | **`러비가 기억하고 있는 나`** — **한 줄** *(v1.22)*. 360px에서 23px `text-title` + `keep-all`로 들어간다(실측 312px) |
| 캡션 | `누구와 몇 번 만났는지는 기록하지 않아.` / `네 기준이 어떻게 움직였는지만 남겨.` (**승인된 2줄**) |
| Hero | `NOW · 가장 최근 관찰` + 최신 Core Insight(사용자 수정본 우선) + 날짜 |
| 변화 요약 | `buildHistorySummary()` 한 줄. 기록 1개면 `아직 비교할 과거 기록이 하나뿐이야.` / `다음 관찰이 쌓이면 변화를 알려줄게.` (**승인된 2줄**) |
| Meaningful Changes | SHIFT/NEW만 최대 3개 |
| Timeline | **Insight 중심.** 각 행은 `날짜 · Relationship Mirror` + 그때의 Core Insight → `/history/[id]` |
| 러비 한 줄 | `인간의 사랑은 아직도 잘 모르겠어.` / `근데 너에 대해서는 전보다 조금 알 것 같아.` (**승인된 2줄**) |
| Empty (0개) | "아직 너를 오래 관찰하진 못했어." → **[나 관찰 시작하기]** |
| Analytics | `relationship_history_view` `{ entry_count }` |

---

### F1-a · 기록 상세 ('이때의 관찰') — `/history/[id]`

① 당시 Core Insight → ② **이때와 지금의 차이** → ③ 당시 Declared Me → ④ 당시 Relationship
Evidence → ⑤ 당시 Mirror 판정 → ⑥ 당시 참고 정보 → ⑦ 이 관찰 기록 삭제.

| 항목 | 내용 |
|---|---|
| 최신 기록일 때 | `이건 가장 최근 관찰이야.` / `다음 관찰이 쌓이면 이 기록과도 비교할 수 있어.` (**승인된 2줄**, v1.22) |
| 변화 없을 때 | "이때와 지금 사이에 크게 달라진 기준은 없었어." |
| MBTI 표시 | `당시 MBTI · INFP`. 아래에 **항상** "MBTI는 당시 참고 정보로만 남겨둔 값이야. 유형이 달라졌다고 성격이 변했다고 보진 않아." |
| 삭제 | 해당 항목만 삭제 → latest/변화/반복 신호 **자동 재계산**(모두 `entries` 파생값) |

---

### F2 · 변화 리포트 — `/history/report`

| 기록 수 | 화면 |
|---|---|
| 0~1개 | **변화를 판정하지 않는다.** `아직 비교할 과거 기록이 하나뿐이야.` / `다음 관찰이 쌓이면 변화를 알려줄게.` |
| 2개 이상 | `previous` vs `latest` → 가장 큰 변화 1개 → 달라진 기준 → 새롭게 보인 신호 → 유지된 기준 → 반복 신호 → 아직 비교하기 어려운 기준 |

캡션은 두 기록의 날짜가 같으면 `{날짜}에 저장한 두 관찰을 비교했어.`로 바꿔 쓴다 — 같은 날짜를
두 번 적으면 구분이 안 된다.

---

### F3 · 저장 직후 Change Moment — `/history/saved`

| 기록 수 | 화면 |
|---|---|
| 1개 | "첫 관찰 기록을 저장했어." + "아직 비교할 과거의 너는 없네. 다음에 다시 관찰하면 그때 달라진 것도 알려줄게." → **[내 기록 보기]**. **가짜 변화 결과를 만들지 않는다** |
| 2개 이상 | "지난 관찰과 비교할 수 있게 됐어." + "어느 쪽이 맞다고 판정하진 않을게. 달라진 지점만 보여줄게." + 가장 의미 있는 변화 1개 미리보기 → **[뭐가 달라졌는지 보기]** |
| 중복 저장 | "이미 저장된 관찰이야. 같은 분석은 기록에 한 번만 남겨." |

---

### X1 · 러비의 다른 관측 렌즈 — `/lens`

**화면 순서 자체가 위계다.**

| 순서 | 계층 | 대상 | 동기화율 |
|---|---|---|---|
| ① | **CORE** | 실제 관계 신호 4축 | ✅ |
| ② | **SUPPORTING** | MBTI | ❌ |
| ③ | **ENTERTAINMENT** | 사주 · Astrology | ❌ |

MBTI도 과학적 궁합 예측이 아니라 동기화율에 안 들어간다. 다만 사용자가 스스로 알고 있는
Personality Preference를 **대화의 출발점**으로 쓴다는 점에서 사주·점성술보다 Relationship
Analysis에 가까워 Supporting으로 위계를 구분한다.

| 상태 | 목록 설명 |
|---|---|
| SELF 없음 | 입력 유도 (`내 정보 입력하기` / `출생정보 입력하기`) |
| SELF만 있음 | 내 결과 + Optional 안내 (`내 MBTI 보기 · 상대 정보를 추가하면 둘도 비교할 수 있어`) |
| 둘 다 있음 | `우리 둘 보기` / `나 × 상대 보기` |

`/lens/zodiac`(v1.3)은 삭제하지 않고 `/lens/astrology`로 `redirect()`한다.

---

### X1-a · MBTI 렌즈 — `/lens/mbti` (Supporting)

**입력 화면이 아니라 비교 상세 화면이다.** 내 MBTI 입력은 S13에 있다.

> **v1.24 P3-1 — 'Hook은 익숙하게, 해석은 다르게.'**
> 익숙한 MBTI 조합으로 들어오게 하되, 읽어 내려가면 **사용자가 직접 답한 관계 신호**와
> 나란히 놓여 '성향만으로는 설명되지 않는 지점'이 드러난다. MBTI = Hook,
> Relationship Evidence = 차별점, 러비 = 해석, 보고서 = 신뢰.

#### 화면 구성 (두 유형 모두 있을 때)

```
 ReportHeader     러비 관찰 기록 · 렌즈 / 성향 렌즈로 본 두 사람
                  meta: 렌즈 · MBTI · 동기화율에는 반영하지 않음
 01 MBTI LENS     나 ISFJ × 상대 ISTJ  (26px · 큰 숫자/궁합%/별점 없음)
                  결정론적 한 문장 (selectMbtiLensHeadline — sameCount만 읽는다)
                  MbtiAxisSummary — 축 이름 + 같음/다름만 4행 (카드 없음)
 02 PATTERN       (v1.25) 무료가 반드시 주는 세 가지
                  ① 조합 패턴(label · body · 눈여겨볼 지점)
                  ② LOVY OBSERVATION — 러비의 심리/철학적 관찰
                  ③ 확인해볼 질문 1개
 03 BUT IN REAL   MbtiBridgeSection — 성향 렌즈 vs 네가 답한 관계 신호
    LIFE          가장 눈에 띄는 지점(러비) → 근거 블록 → 나머지 → 비교 안 한 축
 LENS → CORE      LensCoreBridge (재사용) — '실제 관계 신호로 보기'
 04 4 AXES        MbtiAxisField — **기본 접힘.** 토글 1개로 전체 펼침
                  ENERGY  I [나][상대] ──── E   + 극별 짧은 라벨(MBTI_AXES.poles)
 입력 수정 2행     내 MBTI → S13 · 상대 MBTI → S19
 PremiumEntryRow · 점수 미반영 안내
```

첫 viewport(375×812 실측) 안에 **조합 · 한 문장 · 4축 같음/다름 · 패턴 이름**이 들어간다.

#### 정보 위계 — Core보다 낮다 (v1.38)

> **MBTI Lens는 Hook이자 Supporting Lens이고, Core Relationship Report보다 낮은
> 정보 위계를 갖는다.** 무료 가치를 줄이는 것이 아니라 **읽는 순서**를 정한다.

v1.37까지 375px 실측에서 이 화면은 Core의 순서를 따르지 않았다.

| 지점 | v1.37 | v1.38 | 변화 |
|---|---|---|---|
| 첫 핵심 문장 | 234px | **201px** | -14% |
| PATTERN 시작 | 867px (1.39 화면) | **382px (0.61 화면)** | -56% |
| **Bridge(SURPRISE)** | 1,480px (**2.37 화면**) | **1,056px (1.69 화면)** | **-29%** |
| **Core CTA** | 2,438px (**3.91 화면**) | **2,014px (3.23 화면)** | **-17%** |
| 전체 높이 | 2,903px (4.65 화면) | **2,626px (4.21 화면)** | -9.5% |
| 카드 수 | 7 | **6** | -1 |

**무엇을 지웠나 — 아무것도 지우지 않았다.** 4축 관찰표는 판정의 **근거**이므로
Surprise 뒤로 옮기고 기본 접힘으로 바꿨다(Core S21R도 `점수 → 핵심 한 문장 →
Surprise → 근거` 순서다). 대신 축별 같음/다름 요약을 첫 viewport에 두어, 아래
PATTERN이 근거 없이 읽히지 않게 한다.

| 항목 | 규칙 |
|---|---|
| 토글 | **1개만.** 축마다 accordion을 만들지 않는다 — 목표는 클릭 횟수가 아니라 초기 정보량이다 |
| 토글 접근성 | `<button type="button">` · `min-h-11`(44px) · `aria-expanded` · `aria-controls` · 전역 `:focus-visible` |
| 접힘 상태 | 렌더하지 않는다. 숨겨둔 채 스크린리더에만 남기면 위계가 무의미해진다 |
| 상태 보존 | **하지 않는다.** 펼침은 읽는 동안의 선택이지 기억할 설정이 아니다 |
| Analytics | 기존 `result_section_expand` `{ section: 'mbti_axes' }` 재사용 — **새 이벤트를 만들지 않는다**(§10.5) |
| 삭제한 것 | `4개 축 중 N개 비슷 · M개 다름` 한 줄. 바로 아래 문장(`비슷한 축이 더 많아`)과 같은 사실을 숫자로 한 번·문장으로 한 번 말하고 있었다. 요약이 같은 개수를 담으면서 **어느 축인지**까지 말한다 |
| Core와의 대비 | Core 점수 **76px·brand**, MBTI 조합 **26px·중립** — Lens가 Core보다 크게 외치지 않는다 |

#### 조합 패턴 (`lib/logic/mbtiPattern.ts`) — v1.25 P3-2

**왜 만들었나** — P3-1 배포 후 실사용 피드백이 **"구조는 좋아졌는데 정보가 적다"** 였다.
4축 표는 글자 위치만 보여줬고, 러비의 한 줄 관찰은 그 표를 문장으로 옮긴 것이라 새로
얻는 게 없었다. **축 설명을 늘려서 채우지 않고**(MBTI 교육 페이지가 되면 실패다),
무료가 반드시 세 가지를 주도록 블록을 신설했다.

⚠️ 이 모듈은 `CompatibilityResult`를 **import조차 하지 않는다** — 관계 답변을 억지로
MBTI와 연결하지 않는다. 관계 신호와의 대조는 아래 Bridge의 일이다.

**① 조합 패턴 — 궁합표가 아니다**

유형쌍(INFP × ESTJ)마다 문장을 두면 그 순간 궁합 사전이 된다. 그래서 **4축 같음/다름이
만드는 구조**만 읽는다. 축 묶음은 MBTI 자체의 표준 구분을 그대로 쓴다:

- **INNER** 정보를 받아들이고(S/N) 판단하는(T/F) 과정
- **OUTER** 에너지를 회복하고(E/I) 생활을 조직하는(J/P) 리듬

| 패턴 | 조건 | 이름 |
|---|---|---|
| `all-same` | 4개 같음 | 네 축이 모두 같은 쪽 |
| `all-different` | 0개 같음 | 네 축이 모두 다른 쪽 |
| `inner-same` | INNER 둘 다 같음 + OUTER는 아님 | 판단은 비슷한 쪽, 리듬은 갈리는 쪽 |
| `outer-same` | OUTER 둘 다 같음 + INNER는 아님 | 리듬은 비슷한 쪽, 판단은 갈리는 쪽 |
| `mixed` | 그 외 | 축마다 갈리는 쪽 |

**상호 배타적이고 256개 조합 전부를 덮는다** (같음 3개는 구조상 `mixed`가 될 수 없고
INNER/OUTER 중 한쪽으로 반드시 떨어진다). 각 패턴은 `body`(2~3문장) + `watchFor`(한 줄)를 갖는다.

**② 러비의 심리/철학적 관찰** — 패턴당 하나, `MBTI_PATTERN_OBSERVATION`.
관찰 대상은 **인간 일반**이지 이 사용자가 아니다. 진단·애착유형·트라우마·감정조절 추론 금지.
문장은 늘 러비가 아직 모른다는 데서 끝난다.

**③ 확인해볼 질문 1개** — 새 질문 데이터를 만들지 않고 `MBTI_AXES[].question`을 쓴다.
고르는 규칙(고정): 갈린 축이 있으면 축 정의 순서상 첫 번째 갈린 축, 없으면 첫 번째 축.
`why`가 갈린다 — 갈린 축은 *"실제로도 그런지는 물어봐야"*, 같은 축은
*"같은 글자가 같은 뜻인지는 물어봐야"*. **답을 예측해서 말하지 않는다.**

> **v1.25 Audit — 축 질문의 층위 교정**
> `MBTI_AXES[].question`은 두 사람의 글자가 **다를 때만** 노출된다. 그래서 질문이 특정
> 연애 행동을 지목하면 "너희가 이 축에서 다르니 그 행동도 다를 것"이라는 **예측이 질문의
> 형태로** 숨어든다. 두 개를 축 층위로 되돌렸다.
> - `lifestyle`: "데이트 계획은 미리 정하는 게 좋아…" → "일정을 미리 정해두는 게 편한 편이야…"
> - `decision`: "서운한 일이 있을 때 어떤 반응을…" → "중요한 걸 정할 때, 어떻게 하는 게 맞는지를…"
>   (후자는 P3-1에서 Bridge 매핑으로는 이미 기각한 T/F ↔ 갈등 대응을 질문에서는 그대로 하고 있었다)
> **id · 개수 · 순서 · tag · 저장/공유 동작은 그대로다** — 바뀐 것은 문장뿐이다.

#### MBTI × Relationship Signal Bridge (`lib/logic/mbtiBridge.ts`)

**새 점수를 만들지 않는다.** 이미 계산이 끝난 `CompatibilityResult`(tone 판정)와
`MbtiLensReport`(축별 같음/다름)를 **읽기만** 하는 presentation layer 비교다.

3개 층으로만 말한다: `MBTI 렌즈` → `네가 답한 관계 신호` → `해석`.
출처 표현을 섞지 않는다 — MBTI는 '성향 렌즈에서는', 신호는 '네가 답한 / 실제 관계 답변에서는'.

**의미적 매핑 (4축 중 1축만 사용)**

| MBTI 축 | 후보 관계 신호 | 연결 이유 | 위험 | 사용 |
|---|---|---|---|---|
| ENERGY (E/I) | 개인 시간(`alone`) | ENERGY 축 문구 자체가 이미 "혼자 있는 시간으로 회복 / 함께 활동하며 회복"이고, `alone`은 "혼자 있는 시간이 얼마나 필요한가"를 1~5로 저장한 값 — **같은 대상**을 말한다 | I가 곧 '개인 시간을 많이 원함'은 아니다 | **사용** (예측 아닌 비교로만) |
| INFORMATION (S/N) | 없음 | 구체·추상 정보 처리에 대응하는 관계 질문이 없다 | 억지 연결 | **사용 안 함 → UNKNOWN** |
| DECISION (T/F) | 갈등 해결(`conflict`) | 표면적으로만 관련 | **높음.** `conflict`는 이야기를 꺼내는 **시점**, T/F는 판단 **기준** — 서로 다른 것을 잰다. 연결하면 'T라서 공감 못 함' stereotype | **사용 안 함 → UNKNOWN** |
| LIFESTYLE (J/P) | 없음 | 계획·구조에 대응하는 질문이 없다 | 'J/P = 데이트 계획' 하드코딩 | **사용 안 함 → UNKNOWN** |

**상태 판정** — `성향에서 비슷함 === 신호에서 비슷함` 이면 `ALIGNS`, 아니면 `DIFFERS`.
tone이 `unknown`(한쪽 모름)이면 `UNKNOWN/no-signal`, `neutral`이면 `UNKNOWN/inconclusive`.
**ALIGNS는 '잘 맞음'이 아니다** — 두 관점이 같은 방향을 가리킨다는 뜻뿐이다(둘 다 '다름'을
가리켜도 ALIGNS).

**전체 그림 비교** — MBTI `sameCount`와 관계 신호 `goodSignals/frictionSignals` 개수를
**나란히 읽는다.** 더하거나 평균 내지 않는다 — 합산하는 순간 MBTI가 점수에 들어간 것이 된다.

**동일시 방지 장치 (Release Checkpoint에서 추가)** — 화면에서 두 라벨이 붙어 있으면
`ENERGY = 개인 시간`으로 읽힐 수 있어 사이에 비교 기호(`↔`)를 두고, 축 비교 블록 아래에
한 줄을 고정한다: *"두 줄은 같은 것을 잰 값이 아니야. 성향 렌즈의 축과 네가 직접 답한 관계
질문을 나란히 놓고 본 거라, 한쪽이 다른 쪽을 결정한다는 뜻은 아니야."*
해석 문장에도 인과 표현(`~라서` · `~때문에` · `예측`)을 쓰지 않는다 — 전수 grep으로 확인.

**Surprise 선정 (랜덤 없음 · 고정 우선순위)**

| 순위 | 조건 | 예시 (실측) |
|---|---|---|
| 1 | 축: MBTI 같음 + 실제 답 다름 | "성향 렌즈에서는 둘 다 I로 묶이는데, 개인 시간에 대한 기대는 오히려 다르게 답했네." |
| 2 | 축: MBTI 다름 + 실제 답 비슷 | "성향 렌즈에서는 서로 다른 쪽으로 나뉬는데, 개인 시간에서는 비슷한 걸 원한다고 답했네." |
| 3 | 전체 그림이 다른 방향 | "MBTI에서는 비슷한 축이 더 많은데, 실제로 답한 신호에서는 다른 쪽이 더 많네." |
| 4~5 | 같은 방향(축 → 전체) | "두 관점이 같은 방향을 가리켰네. 그래도 같은 방향이라고 같은 뜻인지는 더 봐야겠어." |
| 6 | 비교 가능한 것 없음 | `null` — 없는 Surprise를 만들지 않는다 |

근거(Evidence)는 Surprise **바로 아래 블록**이 보고서 톤으로 말한다. 실제 저장된 answer
label만 인용한다(`나: 혼자 있는 시간 5/5 · 상대: 거의 안 챙김`) — 자유서술 원문은 쓰지 않는다.

#### 상태별 동작

| 상태 | 동작 |
|---|---|
| 둘 다 있음 | Full Lens — 01 · 02 · 03 + Core Bridge |
| 내 것만 있음 | `MbtiSelfAxisField`(비교 판정 없음) + 상대 입력 **Optional CTA**. Bridge 없음 |
| 상대만 있음 | "상대 정보는 남아 있어. 먼저 네 정보를 입력하면…" + 내 입력 CTA |
| 둘 다 없음 | 내 입력 CTA + **"MBTI를 몰라도 괜찮아. 이건 관계를 보는 여러 렌즈 중 하나니까."** |
| MBTI는 있고 관계 답변 부족 | **01·02는 그대로 보여준다.** 03만 "아직 나란히 놓을 답변이 부족해."로 정직하게 제한 |

MBTI 미입력이 Core Compatibility 접근을 막지 않는다.

| 항목 | 내용 |
|---|---|
| 러비 화법 | 흥미·질문·혼잣말만. 유형 해설·전문가 설명 금지 |
| 보고서 화법 | 정돈·근거·구조. 실제 관계 답변은 **항상 보고서 톤**으로 — MBTI보다 높은 신뢰 위계 |
| UI 금지 | 16유형별 색상·캐릭터·궁합표·하트 궁합·matrix·퍼센트·게이지·progress ring·별점 |
| 같음/다름 표시 | 두 상태 모두 **같은 중립 chip**. 색으로 좋고 나쁨을 칠하지 않고 텍스트로만 구분 |
| marker | 색·위치만으로 구분하지 않는다 — 항상 `나` / `상대` 텍스트를 함께 표시 |
| Motion | `.report-reveal` 재사용(70ms stagger). reduced-motion에서 **즉시 최종 상태**(v1.24에서 `animation-delay`까지 끄도록 수정) |
| Analytics | `mbti_lens_view` `{ mode, has_self, has_target, same_axes?, different_axes? }` · `mbti_relationship_bridge_view` `{ bridge_state, source }` — **MBTI 유형 원문은 보내지 않는다**(v1.24) |

---

### X2 · Birth Profile 입력 — `/lens/birth`

| 항목 | 내용 |
|---|---|
| 목적 | 사주·Astrology가 쓸 출생정보를 **한 번만** 입력 |
| Route | `?from=saju` / `?from=astrology`로 돌아갈 렌즈 지정 |
| 구성 | 러비 인트로 → **나** 섹션(생년월일 · 양/음력 · 출생시간 + `태어난 시간을 몰라` · 출생지역) → **상대** 섹션(접이식, 기본 접힘) → 프라이버시 |
| 입력 유연성 | `.` `/` `-` 어떤 구분자로 입력해도 `YYYY-MM-DD`/`HH:MM`으로 정규화. 자릿수가 안 맞으면 저장하지 않고 입력값을 남겨 고칠 수 있게 한다 |
| `시간 모름` 상호배타 | 켜면 이전 시간을 비운다 — 두 상태가 동시에 참일 수 없다 |
| 상대 정보 Copy | "네가 알고 있는 상대 정보를 입력해줘. 모르면 비워둬도 돼." — 상대가 직접 입력한 것처럼 표현하지 않는다 |
| Validation | Main Funnel을 막지 않는다. 인라인 오류만 — 형식 · 없는 날짜(2월 30일) · 미래 날짜 · 1900년 이전 · 없는 시간 |
| 개별 삭제 | 나/상대 각각 `출생정보 삭제` |
| Analytics | `birth_profile_edit` `{ subject, has_date, has_time, time_unknown, calendar_type, has_location }` · `birth_profile_complete` `{ subject }` |

---

### X1-b · Astrology 렌즈 — `/lens/astrology` (Entertainment)

| 항목 | 내용 |
|---|---|
| 범위 | **Simple Sun Sign** (`getSunSign(date)`). Month/Day 경계 규칙만 |
| **미구현** | Full Natal Chart(Moon·Rising·Aspect·House·Synastry). `calculateNatalChart()`는 **계약만 있고 항상 미지원을 알린다** |
| Self Lens | `SUN SIGN` + 태양궁 + 성향 한 줄 + 이야기해볼 주제. **내 생년월일만 있으면 항상 표시**(Self First) |
| Target Lens | 상대 생년월일이 있으면 Couple과 별개로 **상대 단독 SUN SIGN**도 표시 |
| Couple Lens | 두 사람 생년월일이 **모두** 있을 때만. `비슷하게 읽힐 수 있는 부분`(같은 4원소) / `다르게 나타날 수 있는 부분` → 이야기해볼 주제 |
| 한계 표시 | Self·Target·Couple 한계를 **합쳐서**(중복 제거) 보여준다 |
| 경계일 | 별자리 전환 ±1일이면 "경계 날짜에 태어났다면 실제와 다를 수 있어. 연도별 태양 진입 시각은 반영하지 않았어." |
| Legacy 안내 | `legacyZodiac`이 있고 계산값이 없으면 "예전에 직접 고른 별자리가 남아 있어, 생년월일을 넣어주면 다시 볼 수 있어" |
| 금지 표현 | 천생연분 · 최악의 조합 · 헤어질/결혼 확률 · 운명 · N% 궁합 |
| Analytics | `astrology_lens_view` `{ mode, has_self, has_target }` |

---

### X1-c · 사주 렌즈 — `/lens/saju` (Entertainment, **엔진 미연결**)

> ⚠️ **이 화면은 사주 명식을 계산하지 않는다.**
> 사주 명식에는 절입(節入) 시각·진태양시·출생지 경도 보정·연월주 세우기 규칙이 필요하다.
> 검증된 엔진 없이 생년월일 숫자 더하기·띠·랜덤 테이블로 명식을 만들면 그건 사주가 아니라
> **거짓**이다. **정확도 없는 Fake Saju보다 Incomplete but Honest 상태가 낫다.**

| 항목 | 내용 |
|---|---|
| 엔진 플래그 | `SAJU_ENGINE_READY` (`NEXT_PUBLIC_SAJU_ENGINE_READY`) — **현재 `false`** |
| 현재 화면 | 엔진이 꺼져 있어도 Self First 유지. 내 정보 없음 → `BirthMissingBlock` · **내 것만** → "네 정보는 준비됐어. 엔진이 연결되면 먼저 네 사주 렌즈부터 볼 수 있어." · **둘 다** → "두 사람의 정보는 준비됐어. 엔진이 연결되면 각각의 결과와 둘의 비교까지 볼 수 있어." |
| 서비스 경계 | `sajuService.ts`의 `calculateSaju()` / `calculateSajuCompatibility()`. **화면은 계산 규칙을 직접 쓰지 않는다** — 엔진이 붙으면 이 두 함수 구현만 교체하면 화면 코드는 그대로 |
| 타입 분리 | `SajuProfileResult`(self)와 `SajuCompatibilityResult`(couple)가 이미 분리 — 내 사주 계산이 Couple 전용 함수에 묶여 있지 않다 |
| 엔진 연결 시 표현 제한 | ① 전통 해석에서 보는 주요 성향 ② 관계에서 참고해볼 주제 ③ 주의해서 볼 해석 ④ 한계 **만** |
| 금지 표현 | '너는 반드시 이런 사람' · 결혼운 확정 · 몇 살에 결혼 · 올해 헤어진다 · 결혼하면 안 된다 · 바람기 · 이혼 가능성 · 질병/사망 · 투자운 |
| 항상 고지 | "사주는 전통적인 해석 체계야. 실제 관계가 잘될 확률을 예측하는 결과로는 사용하지 않을게." |
| 시간 모름 | **시주를 세우지 않는다** — 엔진이 붙어도 남는 한계로 명시 |
| Analytics | `saju_lens_view` `{ mode, engine_available, has_self, has_target }` |

---

### S22a · 궁합 렌즈 허브 — `/compatibility/lenses`

S21R이 렌즈 뱃지로 뒤덮이지 않도록 진입점을 한 곳에 모았다. **화면 순서 자체가 위계다.**

| 순서 | 계층 | 내용 |
|---|---|---|
| ① | CORE | `동기화율 {score}` + 4축 (Purple 강조) |
| ② | SUPPORTING | MBTI Lens |
| ③ | ENTERTAINMENT | 사주 Lens · Astrology Lens (중립 톤) |

출생정보 상태에 따라 뱃지가 `우리 둘 보기 →` / `상대 정보 필요` / `정보 입력하기`로 갈린다.
`backHref`는 `/compatibility#lenses` — 앵커로 제자리 복귀한다.

---

### SH1/SH2 · 공유 카드 — `/share/compatibility` · `/share/mirror`

| 항목 | 내용 |
|---|---|
| 생성 | `shareCard.ts`가 Canvas 2D로 직접 그려 PNG 다운로드 |
| SH1 옵션 | `includeTargetInfo` · `includeDimensionScores` (기본 off) |
| SH2 규칙 | '말한 나' 막대 **하나만** 실제 값으로 그리고, '관계 속 나'는 `focus.relationshipSignal` **문장으로만** 보여준다 — 존재하지 않는 숫자를 막대로 그리지 않는다 |
| 방어 | `mirror.available === false`면 조용히 `/mirror`로 되돌린다 |
| 프라이버시 | "개인정보는 카드에 포함되지 않아." / "상대 정보와 개인 답변은 카드에 포함되지 않아." |
| ⚠️ 러비 배율 | 이 경로는 `Lovy` 컴포넌트를 거치지 않으므로 `lovyPose`로 같은 레지스트리에서 경로·배율을 가져온다(§4.10) |

---

## 7. 핵심 계산 로직

> ⚠️ **심리 검사나 과학적 진단이 아니다.** 입력값의 공통점·차이를 사용자가 직관적으로 읽을 수
> 있게 만든 **규칙 기반 로직**이다. 전부 `src/lib/logic/**`의 **순수 함수**이고, 화면은 계산을
> 직접 하지 않는다.

### 7.1 동기화율 (Sync Rate) — `logic/compatibility.ts`

**하한선(Floor) 없음. 관계 행동 4축(`contact`/`conflict`/`alone`/`affection`)만 사용.**

```
difference     = |나의 값 - 상대의 값|          // 둘 다 1~5 척도
axisSimilarity = 1 - (difference / 4)            // 0~1
score          = round( mean(비교 가능한 축의 axisSimilarity) × 100 )
```

| 차이 | 동기화율 |
|---|---|
| 완전히 동일 | **100** |
| 1단계 차이 | 75 |
| 2단계 차이 | 50 |
| 3단계 차이 | 25 |
| 완전 반대(4단계) | **0** |

| 규칙 | 내용 |
|---|---|
| E3 조건 | 비교 가능한 축이 **3개(`TARGET_MIN_KNOWN`) 미만**이면 `score = null` |
| '모름'(`x`) | 계산에서 **완전히 빠진다**. `comparedCount`에도 안 들어간다 |
| 항상 함께 표시 | "비교 가능한 N개 신호 기준" + **"연애 성공확률이 아니야"** |
| UI 게이지 | `alignment = round(axisSimilarity × 5)` — 완전 반대일 때 **0칸** |

> **과거 문제(고쳐짐)** — v1.0은 `score = 40 + floor(평균비율 × 52)`라는 인위적 하한선이 있어
> 축이 다 어긋나도 최소 40점이 나왔다. 그리고 `talk`/`rhythm` 두 축의 '나의 값'을 사진 관찰
> 고정값(4, 3)으로 하드코딩해서 계산에 넣었는데 **사용자가 준 데이터가 아니었다.**

#### tone 판정과 문장 선택

```
tone(similarity) =
  null      → 'unknown'
  >= 0.75   → 'good'
  <= 0.25   → 'watch'
  그 외      → 'neutral'
```

`evidence`/`scene` 문장은 **tone별로 따로** 정의돼 있다(`AxisCopyByTone`). 축당 문장 하나를
tone과 무관하게 붙이면 **판정과 반대되는 문장**이 나간다 — 실제로 있었던 문제다.

| 실제 발생했던 모순 | 원인 |
|---|---|
| 개인 시간 1/5 vs 상대 '중요해 보임' → 배지는 '관찰 필요'인데 "…거절로 받아들일 가능성이 **비교적 낮아** 보여" | 축당 문장 1개 고정 |
| 갈등 해결이 양쪽 다 '바로 대화'(일치)인데 "한쪽은 대화를 피한다고 느끼고…" | 〃 |
| 연락 중요도를 5/5로 답해도 "연락 중요도는 **낮게** 답했지만…" | 데모 페르소나 기준으로 쓴 문장 |

계산(`similarityOf`·`toneOf`·`alignment`·`score`)은 건드리지 않고 **문구 테이블만** tone별로 나눴다.

### 7.2 MBTI Lens (Supporting) — `logic/mbtiLens.ts`

**점수가 아니라 대화 포인트를 만든다.** 4개 선호 지표를 각각 **독립적으로** 비교하고 합산하지 않는다.

```
buildMbtiLens(mine, theirs)   // 둘 중 하나라도 없으면 null → TOGETHER 비교만 안 만든다
  → axes[4] : { key, eyebrow, label, mineLetter, theirsLetter, same, note }
  → sameCount / differentCount

buildMbtiSelfLens(type)       // 한 사람 유형만으로 항상 만들 수 있다 (Self First)
  → axes[4] : { key, eyebrow, label, letter }
  → note
```

| 축 | key | 글자 |
|---|---|---|
| 에너지를 회복하는 방식 | `energy` | I / E |
| 정보를 받아들이는 방식 | `information` | S / N |
| 결정할 때 먼저 보는 기준 | `decision` | T / F |
| 계획과 유연함 사이 | `lifestyle` | J / P |

> ⚠️ **표현 규칙.** 같은 글자를 '좋음', 다른 글자를 '나쁨'으로 표현하지 않는다.
> 화면 라벨은 **`비슷한 성향` / `다르게 나타날 수 있는 성향`** 두 가지만.
> **금지:** GOOD/BAD MATCH · 최고의 궁합 · 천생연분 · 상극 · 완벽한 조합 · MBTI 궁합 N점 · 퍼센트
> **톤:** `~할 수 있어` / `~로 이야기되기도 해` / `실제로 그런지는 확인해봐`

**관계 신호와의 관계** — MBTI로 실제 관계 신호를 *설명해버리지 않는다.* 갈등 방식에 실제 차이가
있고 MBTI `decision` 축도 다를 때, **두 현상이 같은 원인이라고 단정하지 않는다.**
Relationship Evidence가 우선이고 MBTI는 Supporting이다.

#### 왜 한 번 넣었다가 되돌렸는가

MBTI 4글자 중 같은 글자 수를 `matches / 4`로 similarity화해서 관계 행동 축과 **동일한 가중치**로
score 평균에 넣은 구현이 있었다. E/I·S/N·T/F·J/P가 많이 같다고 더 좋은 관계라는 근거가 없어
**'근거 없이 판정하지 않는다'** 원칙과 정면 충돌했다. `buildMbtiDimension()`과 `mbtiSimilarity()`를
삭제했다.

### 7.3 무엇이 점수에 들어가지 않는가 — 한눈에

| 항목 | MBTI | 사주 | Astrology | History | Observed(사진) |
|---|---|---|---|---|---|
| `score` 계산의 mean | ❌ | ❌ | ❌ | ❌ | ❌ |
| `comparedCount` (분모) | ❌ | ❌ | ❌ | ❌ | ❌ |
| `TARGET_MIN_KNOWN`(E3) 판단 | ❌ | ❌ | ❌ | ❌ | ❌ |
| `known_count` (S19) | ❌ | ❌ | ❌ | ❌ | ❌ |
| `goodSignals`/`frictionSignals` | ❌ | ❌ | ❌ | ❌ | ❌ |
| Relationship Mirror 판정 | ❌ | ❌ | ❌ | Past Observation만 | ❌ |
| History Change 판정 | ❌ | ❌ | ❌ | — | ❌ |

> ⚠️ `Main Sync Score + Saju Score = 최종 궁합 87점` 같은 합산을 **절대 만들지 않는다.**
>
> **구조적 보장:** `buildCompatibility(declared, target)`는 `birthProfile`을 **인자로 받지도
> 않는다.** `analysisFingerprint()`도 `birthProfile`을 포함하지 않으므로 출생정보를 바꿔도
> History에 SHIFT/NEW가 생기지 않는다.

### 7.4 Astrology Simple Sun Sign — `services/astrologyService.ts`

```
getSunSign(date)   // 'YYYY-MM-DD' → ZodiacSign | null
  SUN_SIGN_RANGES를 시작월 내림차순으로 훑어 처음 만족하는 구간을 고른다.
  1/1~1/19는 어디에도 안 걸리므로 마지막에 capricorn으로 떨어진다
  (12/22 시작 구간이 연말을 넘어 이어지기 때문)
```

| 입력 | 결과 | 비고 |
|---|---|---|
| 유효하지 않은 날짜 | `null` | **임의의 별자리를 돌려주지 않는다** |
| 1995-03-21 | 양자리 | aries 시작일 |
| 1995-03-20 | 물고기자리 | 하루 전 |
| 1995-01-19 | 염소자리 | fall-through |
| 1995-01-20 | 물병자리 | aquarius 시작일 |

두 사람 비교는 **4원소**(fire/earth/air/water)로 '비슷/다름'만 만든다. 점수·확률 없음.

### 7.5 Relationship Mirror 판정 — `logic/mirror.ts`

**숫자를 빼지 않고, '근거의 종류'로 카테고리 판정한다.**

```
evidenceStrength(axis) =
  hardest(가장 힘들었던 순간)가 이 축과 강하게 연결됨  → 'hardest'    (가장 강한 근거)
  important(중요했던 요소)에 이 축이 포함됨             → 'important'  (중간 근거)
  둘 다 아님                                            → 'absent'     (근거 없음)

state(declaredValue, strength) =
  'hardest'   && declared >= 4   → MATCH
  'hardest'   && declared <  4   → GAP
  'important' && declared <= 2   → GAP
  'important' && declared >  2   → MATCH
  'absent'    && declared >= 4   → CHANGE    (말했지만 근거가 안 보임)
  'absent'    && declared <  4   → UNKNOWN   (판정하지 않음)
```

| 규칙 | 내용 |
|---|---|
| `hardest` 대응 | `contact_drop→contact` · `fight_silence→conflict` · `no_time→alone`. `value_gap`은 5축 어디에도 명확히 대응하지 않아 **대응표에서 뺐다** |
| **UNKNOWN** | `insights` 배열에 **아예 포함되지 않는다** — 근거 없는 축을 억지로 MATCH로 채우지 않는다 |
| 헤더 표시 | "비교 가능한 N개 기준에서" = 실제 판정 개수 |

**화면 표시 규칙**

| 값 | 표시 |
|---|---|
| Declared | **실제 1~5로 수집된 축만**(`alone`·`contact`) `N/5`로 표시. `hobby`·`conflict`·`affection`은 3지선다를 내부 계산용으로 매핑한 값이라 **숫자를 보여주지 않고 문구만**(`declaredHasScale` 플래그) |
| Relationship | 숫자가 아니라 `relationshipSignalText()`가 만든 **문장** ("이전 관계에서 연락 감소가 가장 힘든 순간으로 선택") |

> **과거 문제(고쳐짐)** — 과거 관계 질문은 전부 선택형이라 1~5로 수집된 적이 없는데
> `important.includes('contact') ? 4 : 2` 같은 규칙으로 **억지로 숫자화**했다. Declared(실제
> 척도)와 이 가짜 숫자를 빼서 판정하니 실제보다 정밀하게 측정된 것처럼 보였다.
> Declared 미응답을 중립값 3으로 채운 것도 문제였다(Missing data ≠ 중립).

### 7.6 Focus 축 선정 (Teaser · Core Insight)

```
1순위: hardest 근거의 GAP 축
2순위: 그 외 GAP 축
3순위: CHANGE 축
4순위: 첫 번째 판정된 축 (모두 MATCH인 경우)
```

`contact`가 GAP으로 뽑히면 고정 헤드라인("너는 연락 자체보다…"), 그 외에는
`"너는 {label}에서 말한 기준보다 실제 관계에서 더 크게 반응하는 사람일지도 몰라."` 템플릿.

### 7.7 Adaptive Follow-up 트리거

```
pickAdaptiveTriggerAxis(declared, experience) =
  experience.skipped                       → null
  buildInsights()의 focus 축이 GAP이면      → 그 축
  그 외                                     → null
```

**Teaser/Core Insight의 focus 축과 Adaptive가 묻는 축은 항상 같다** — 그 축의 이유를 미리 하나
더 받아서 Core Insight의 근거를 풍부하게 만든다.

### 7.8 Relationship Profile Summary (S18) — `logic/profile.ts`

```
declaredHighlight(declared)       // Declared 중 가장 두드러진 특징 1개 (우선순위 표)
relationshipHighlight(experience) // 관계 경험 중 신호 1개 (hardest 우선, 없으면 important[0])

summary =
  둘 다 있으면  → "{declared 특징}, {relationship 신호}."
  하나만 있으면 → 있는 쪽만
  둘 다 없으면  → "아직 뚜렷한 특징을 관찰하기엔 정보가 조금 더 필요해."
```

두 문장은 **판정 없이 나란히 붙인다.** "~라고 답했지만 실제로는" 같은 비교·모순 표현은 이 함수
안 어디에도 없다 — Mirror와 코드가 완전히 분리돼 있어 **애초에 그런 문장을 만들 수 없다.**

### 7.9 History Change — `logic/history.ts`

> ⚠️ **Mirror State와 History Change State는 완전히 다른 축이다.**
>
> | | 비교 대상 |
> |---|---|
> | **Mirror** (MATCH/GAP/CHANGE) | 하나의 Snapshot 안에서 Declared vs Relationship Evidence |
> | **History** (STABLE/SHIFT/NEW/INSUFFICIENT) | 과거 Snapshot vs 현재 Snapshot |

UI에서 비교를 계산하지 않는다 — 전부 이 파일을 거친다.

⚠️ **`buildHistoryReport`와 `findRepeatedRelationshipSignals`는 커플 기록만 본다.**
Solo 관찰이 섞이면 커플 변화 리포트가 Solo 기록과 비교된다. Solo 시간축 비교는
`logic/soloHistory.ts`의 `buildSoloHistoryReport`가 따로 하고, 두 비교는 주어도 시점도
다르므로 화면에서도 합치지 않는다([§9.8.5](#985-mixed-history-ia--하나의-archive)).

⚠️ **v1.35 — `HistoryReport.compared`가 비교에 참여한 두 기록을 알려준다.** 화면과
Cross-source Engine이 `useHistory().latest`/`previous`(전체 History의 마지막 두 항목)를
대신 쓰면, Mixed History에서 **비교한 기록과 화면에 적힌 날짜가 서로 다른 기록을 가리킨다.**
읽는 지점은 `useComparedHistoryEntries()` 하나다.

| 함수 | 역할 |
|---|---|
| `analysisFingerprint()` | 중복 저장 방지용 분석 지문 |
| `buildHistoryEntry()` | 세션 + Mirror → 저장할 Entry (불가하면 `null`) |
| `compareDeclaredSnapshots()` | **직접 1~5로 수집한 축만**(contact/alone) 값 비교 |
| `compareMirrorSnapshots()` | 축별 과거/현재 Mirror 상태 |
| `buildHistoryChanges()` | 축별 `HistoryAxisChange` |
| `getMostMeaningfulChange()` | SHIFT 우선 → NEW. 없으면 `null` |
| `buildHistorySummary()` | 러비 한 줄 요약 |
| `buildHistoryReport()` | 기록 2개 미만이면 `comparable: false` |
| `findRepeatedRelationshipSignals()` | 같은 축에서 GAP/CHANGE 반복 |
| `pastObservationFor()` | 특정 축의 Past Observation 한 줄 |

**판정 규칙**

```
pastState === null   → nowState ? 'NEW' : 'INSUFFICIENT'
nowState  === null   → 'INSUFFICIENT'        // 사라졌다고 단정하지 않는다
pastState !== nowState || declared 값이 실제로 달라짐 → 'SHIFT'
그 외                 → 'STABLE'
```

| State | Copy 규칙 |
|---|---|
| STABLE | "이전 관찰과 비슷하게 유지되고 있어." — **'일관된 성격' 같은 Personality 판정 금지** |
| SHIFT | 척도 축이면 `(2/5 → 4/5)`까지 표시. "지난번보다 ~ 더/덜 중요하게 보고 있어." — **'성장했다' 금지** |
| NEW | "이번 관찰에서 처음 보였어." |
| INSUFFICIENT | "아직 ~ 비교할 정보가 부족해." — **판정하지 않는다** |

**척도 비교 제약** — `contact`/`alone`만 트랙 위 점으로 비교한다(직접 1~5로 수집한 값).
`conflict`/`affection`/`hobby`는 선택형이라 증감으로 말하지 않고 문장으로만 대조한다.
**Relationship Evidence는 절대 숫자로 만들지 않는다.**

#### 7.9.1 반복 신호 (Repeated Relationship Signal)

같은 축에서 **GAP/CHANGE가 2회 이상** 나타난 기록. **MATCH는 반복 신호로 보지 않는다** —
'말한 대로 나타났다'가 되풀이되는 건 발견이 아니다.

- 기준은 **Mirror Evidence만.** MBTI 변화는 절대 반영하지 않는다
- Copy: `이 신호… 처음 보는 게 아닌데.` / "이 기준은 이전 관찰에서도 비슷한 신호가 있었어."
- **금지:** '너는 항상 이래' / '반복되는 문제야' / '너의 연애 패턴은 이거야'

#### 7.9.2 Past Observation — Supporting Evidence

| 화면 | 위치 |
|---|---|
| S27R `/mirror` | Mirror Gap Map **아래**(반복 신호 알림) · '이렇게 생각한 이유' 마지막 항목 |
| S21R `/compatibility` | Friction Signal **뒤**, Lens **앞** (참고 섹션) |

> ⚠️ **Past Observation은 현재 판정을 바꾸지 않는다.**
> 현재 Mirror = 현재 데이터만 / Past History = Supporting Evidence.
> 현재 Evidence가 없으면 과거만으로 Gap을 만들지 않는다.
> 현재 분석이 이미 저장돼 있으면 그 항목은 과거에서 제외한다(**자기 자신을 반복으로 세지 않는다**).

**정보 위계:** `① 실제 관계 신호 → ② 근거/상황 → ③ Past Observation → ④ MBTI Lens`
History도 MBTI도 실제 관계 신호보다 위에 오지 않고, **둘 다 동기화율에 들어가지 않는다.**

### 7.10 Evidence Coverage (구 '확신도')

이름을 '확신도'에서 **'관측 정보 충분/보통/부족'**으로 바꿨다. 이 값은 'AI가 얼마나 확신하는가'가
아니라 **'얼마나 많은 입력 근거가 확보됐는가'**를 뜻한다.

```
experience.skipped                              → 부족
!(important.length >= 3 && selfGap !== null)     → 부족
note(서술)가 비어있지 않음                        → 충분
그 외                                            → 보통
```

자유서술을 많이 했다고 분석이 사실일 확률이 높아지는 건 아니다 — 그래서 이름을 **근거량**으로
한정했다.

사진 관찰 쪽 Coverage는 **코드가** 판정한다(AI가 '충분하다'고 말하지 못한다).

```
evidenceCoverageLevel(usableImageCount, traitCount) =
  usable >= 5 && traits >= 3   → 'high'
  usable >= 3 && traits >= 2   → 'medium'
  그 외                         → 'low'
```

### 7.11 Approach Hints — `logic/approachHints.ts`

> ⚠️ **호감도 예측이나 공략법이 아니다.** '네가 알려준 상대의 모습'을 기준으로, 관계 속도를
> 맞출 때 참고할 것을 규칙으로 만든다. 최대 3개.

| 종류 | 조건 |
|---|---|
| 연락 힌트 | 상대 `contact` 값 기준 |
| 표현 힌트 | 상대 `affection` 값 기준 |
| 갈등 힌트 | **실제 friction이 있을 때만** 만든다 |
| 취향 힌트 | `target.preferences.interests`가 있을 때 |

- 항상 하단에 "이건 공략법은 아니야. 실제론 직접 물어보는 게 가장 정확해."
- 힌트가 0개면 "아직 이 사람이 좋아하는 걸 많이 알진 못하네." + 질문 섹션으로 유도
- **AI는 의도적으로 미연결** — 규칙만으로 만든다
- Analytics에 **raw interest 텍스트·힌트 문장 원문을 절대 보내지 않는다**

### 7.12 대화 질문 — `logic/compatibility.ts`

**완전히 결정론적이다.**

```
buildConversationQuestions(result)
  keys = frictionSignals의 축들
  부족하면 QUESTION_FALLBACK_ORDER(['contact','conflict','alone'])로 채운다
  최대 CONVERSATION_QUESTION_COUNT(3)개

buildMbtiQuestions(report)
  선호가 **다른** MBTI 축마다 1개씩. 항상 관계 신호 질문 뒤에 붙는다
```

질문 id는 관계 축 4개 + MBTI 축 4개 = **8종 고정값**(`"conflict"`, `"mbti_energy"` 등).
`savedQuestions`는 `resetTargetContext()`가 항상 함께 초기화한다 — 모든 id가 상대 축/상대 MBTI
비교에서 나온 값이기 때문이다.

### 7.13 러비 관찰 노트 · FIRST SURPRISE — `data/lovyNotes.ts`

감성 문구를 화면 곳곳에 하드코딩해 흩뿌리지 않는다. **한 곳에 `trigger × axis`로 등록하고
현재 결과에서 결정론적으로 고른다** — 같은 결과면 언제나 같은 문장이다(**랜덤 생성 금지**).

```
selectCompatibilityNote(result)
  score === null                  → null   (데이터가 없는데 생각을 지어내지 않는다)
  frictionSignals[0]이 있으면       → 그 축의 gap 노트
  goodSignals[0]이 있으면           → 그 축의 alignment 노트
  그 외                            → align-default
```

> ⚠️ 이 문장들은 **결과를 만들지 않는다.** 이미 계산이 끝난 `CompatibilityResult`를 보고 러비가
> 떠올린 '생각/질문'일 뿐이다 — Compatibility Score·4축 계산·tone 판정은 이 파일을 **읽지도
> 않는다.**

**화법 규칙** — 인간을 처음 보는 외계인의 호기심. 짧은 혼잣말. `~일 수도 있겠네` / `~인가 봐`.
금지: `당신은 ~한 사람입니다` · `이 관계는 성공할 확률이 ~` · `심리학적으로 ~`.

`selectFirstSurprise(result)`는 §6 S21R 참고.

---

## 8. AI 분석 파이프라인

### 8.1 가장 중요한 원칙 — 역할 분리

> **AI는 럽유럽미의 '판정자'가 아니다.**
>
> ```
> Evidence  →  Rule  →  AI Explanation  →  User Verification
> ```

| | 담당 |
|---|---|
| **Rule Logic** (deterministic) | 동기화율 · Mirror MATCH/GAP/CHANGE · History STABLE/SHIFT/NEW · MBTI 축 비교 · Sun Sign · Evidence Coverage |
| **AI** | 관찰 · 정리 · 설명 · 개인화 문장 · 질문 생성 |

**AI가 생성할 수 없는 것** — Compatibility Score · Mirror State · History State · MBTI 결과 ·
Sun Sign · 사주 계산 · Premium 가격/가용성.

**구조적 보장**

- `buildCompatibility()`는 **AI 결과를 인자로 받지 않는다**
- Mirror state는 서버에서 **규칙 값으로 덮어쓴다**(`attachRuleStates()`) — AI가 `state`를
  바꿔 보내도 무시된다
- `focusAxis`는 규칙이 고른다. AI가 고르지 않는다
- **실측:** AI 분석 결과를 통째로 조작해도 동기화율 55 · 신호 2/2 · Mirror 판정이 불변이었다

### 8.2 Architecture

```
UI (client)
  ↓  services/ai/aiClient.ts     — in-flight dedup · cache · stale 폐기 · analytics
POST /api/ai/{task}               — Route Handler (server-only)
  ↓  app/api/ai/_shared.ts       — size limit · task allowlist · rate limit · requestId · 안전 로그
  ↓  services/ai/handlers.ts     — task 실행
  ↓  services/ai/provider.ts     — Provider 추상화 (OpenAI 호환, fetch 직접 호출)
  ↓  schemas.ts → safety.ts       — Schema Parse → Business Validation → 금지 추론 스캔
UI
```

| 파일 | 역할 |
|---|---|
| `serverEnv.ts` | **server-only.** API Key · 모델 · 타임아웃 · 요청 상한 |
| `provider.ts` | Provider 인터페이스 + OpenAI 호환 구현. **SDK 의존성 0** |
| `promptTemplates.ts` | 버전 관리된 프롬프트 + 공통 System 원칙 |
| `contextBuilders.ts` | Task별 **최소** 컨텍스트 (Data Minimization) |
| `schemas.ts` | 손으로 쓴 검증 (프로젝트 관례 — zod 미도입) |
| `safety.ts` | injection delimit + 금지 추론 스캔 |
| `fallback.ts` | demo/fallback 결과 + Evidence Coverage 판정 |
| `aiClient.ts` | 화면과 내부 API 사이의 **유일한 통로** |
| `imagePrep.ts` | 이미지 축소·검증 + 입력 지문 |

> **API Key는 서버에만 있다.** `NEXT_PUBLIC_` prefix를 쓰지 않고, `serverEnv.ts`는
> `server-only`를 import하므로 **클라이언트에서 import하면 빌드가 실패한다.**

### 8.3 AI Task 6종

| Task | Route | 하는 일 |
|---|---|---|
| `observed-profile` | `/api/ai/observed-profile` | 사진 → 관찰 (Vision) |
| `relationship-insight` | `/api/ai/relationship-insight` | Mirror 판정 **설명** |
| `compatibility-narrative` | `/api/ai/compatibility-narrative` | 축별 궁합 설명 + 대화 질문 |
| `history-insight` | `/api/ai/history-insight` | 변화 **설명** |
| `deep-report-narrative` | `/api/ai/deep-report-narrative` | Premium Deep Report 문장 |
| (dev) `contract-test` | `/api/ai/contract-test` | 픽스처 검증 실행기. **Production 404** |

### 8.4 AI Mode

| mode | 의미 | trait |
|---|---|---|
| `real` | 실제 Provider 응답 | 실제 관찰 |
| `mock` | 개발 전용. Provider를 부르지 않지만 **검증 단계는 그대로 통과**. Production에서 강제로 demo로 내려간다 | mock 관찰 |
| `demo` | Provider 미호출 (기본값 · 포트폴리오/로컬) | **0개** |
| `fallback` | real 시도 후 설정/서버 오류 → **화면에 표시한다** | **0개** |
| `legacy-demo` | v1.5 이전 세션에서 복원 | **0개** |

```
resolveMode() =
  AI_MODE === 'real'                              → 'real'
  AI_MODE === 'mock' && NODE_ENV !== 'production' → 'mock'
  그 외                                            → 'demo'

canCallProvider(config) =
  mode === 'mock'                    → true
  mode === 'real' && apiKey 있음      → true
  그 외                               → false
```

**`AI_MODE=real`인데 키가 없으면 real이라고 우기지 않는다** → `CONFIG_ERROR`.
실측: 잘못된 키로 호출하면 `{"ok":false,"reason":"CONFIG_ERROR"}` — **가짜 결과를 만들지 않는다.**

#### 모드가 결정되는 경로 (v1.39)

```
ENV (배포 플랫폼 · .env.local)
  │
  ├─ AI_MODE · AI_API_KEY · AI_PROVIDER · AI_BASE_URL · AI_MODEL · AI_VISION_MODEL
  │      ↓  server-only (import 'server-only')
  │  services/ai/serverEnv.ts   readAiConfig() → resolveMode() → canCallProvider()
  │      ↓
  │  services/ai/provider.ts    resolveProvider(useVision)
  │      │                        null → 호출부가 demo/CONFIG_ERROR로 분기
  │      ↓
  │  services/ai/handlers.ts    runObservedTask() 등 6 Task
  │      ↓
  │  /api/ai/*                  응답 data.meta.mode  ← **결과의 진실은 이 값 하나다**
  │      ↓
  │  화면 배지 (S09 `AI OBSERVATION` / `DEMO AI` / `MOCK AI` / `규칙 기반 대체`)
  │
  └─ NEXT_PUBLIC_AI_MODE
         ↓  클라이언트 번들에 build time 인라인
     lib/env.ts  AI_MODE_HINT  →  S07 `/profile/photos`의 안내 문구 **한 곳에서만** 쓴다
```

> **핵심 경계.** `NEXT_PUBLIC_AI_MODE=real`만으로는 **아무것도 real이 되지 않는다.**
> Provider 선택은 server-only 값만 본다. 이 힌트가 필요한 이유는 사진을 고르는 시점에는
> 아직 응답(`meta.mode`)이 없어서 "전송된다/전송하지 않는다"를 골라야 하기 때문이다.
> 그래서 **UI hint와 server truth는 분리돼 있고, 두 값이 어긋나면 S07 문구만 어긋난다** —
> 결과 배지·관찰 개수·limitations는 서버 응답을 따른다.

#### 환경변수 역할 (v1.39)

| 변수 | 범위 | 필수 | 기본값 | 없으면 | Production 영향 |
|---|---|---|---|---|---|
| `AI_MODE` | **서버** | real 쓰려면 필수 | `demo` | `demo`로 내려감 (조용히) | 사진 관찰 **0개** · `DEMO AI` 배지 |
| `AI_API_KEY` | **서버** | `AI_MODE=real`이면 필수 | `null` | `canCallProvider` false → **`CONFIG_ERROR`** | 실패를 사실대로 표시(`규칙 기반 대체`) |
| `AI_PROVIDER` | 서버 | ✕ | `openai-compatible` | 기본값 사용 | 없음 |
| `AI_BASE_URL` | 서버 | ✕ | `https://api.openai.com/v1` | 기본값 사용 | 오설정 시 `SERVER_ERROR`/`TIMEOUT` |
| `AI_MODEL` | 서버 | ✕ | `gpt-4o-mini` | 기본값 사용 | 없음 |
| `AI_VISION_MODEL` | 서버 | ✕ | `gpt-4o-mini` | 기본값 사용 | Vision 품질 |
| `AI_TIMEOUT_MS` | 서버 | ✕ | `45000` | 기본값 사용 | 없음 |
| `AI_MAX_REQUEST_BYTES` | 서버 | ✕ | `12MB` | 기본값 사용 | 큰 사진 거절 임계 |
| `NEXT_PUBLIC_AI_MODE` | **클라이언트** (build time) | ✕ | `demo` 힌트 | S07이 데모 문구 표시 | **Provider 선택에 영향 없음** |
| `NEXT_PUBLIC_AI_DEBUG` | 클라이언트 | ✕ | `false` | 디버그 패널 미표시 | dev에서만 의미 있음 |

⚠️ 세 변수를 혼동하지 않는다 — **`AI_MODE`가 동작을 정하고, `AI_API_KEY`가 그것을 가능하게
하고, `NEXT_PUBLIC_AI_MODE`는 문구만 고른다.** 배포에서는 셋을 함께 맞춘다.

#### Silent Demo Fallback — 현재 정책이 약속하는 것 (v1.39)

env가 빠졌을 때 무엇이 조용하고 무엇이 시끄러운지는 **의도된 비대칭**이다.

| 상황 | 결과 | 조용한가 | 근거 |
|---|---|---|---|
| `AI_MODE` 미설정 / `real` 아님 | `demo` · `traits: []` | **조용하다** — 에러가 아니다 | demo는 오류 상태가 아니라 **문서화된 기본값**이다. 앱은 정상 동작하고 화면이 `DEMO AI` 배지 + `실제 사진 내용을 분석하지 않았어`를 표시한다 |
| `AI_MODE=real` · Key 없음 | **`CONFIG_ERROR`** | 시끄럽다 | 사용자가 real을 요청했으므로 실패를 실패로 말한다 |
| `AI_MODE=real` · Key 있음 · 호출 실패 | `fallback` · `provider_failed` | 시끄럽다 | `규칙 기반 대체` 배지 + 실패 사유 안내 |
| `AI_MODE=mock` · Production | `demo` | **조용하다** | mock이 Production에 새는 것을 막는 안전장치 |

> **왜 demo를 CONFIG_ERROR로 올리지 않는가.** demo는 "설정이 잘못됐다"가 아니라 "Provider를
> 붙이지 않고 쓰는 모드"다(포트폴리오·로컬·Key 없는 리뷰어). 이걸 오류로 만들면 Key 없는
> 환경에서 앱이 못 뜨고, §1.5 규칙 6("무료에서 Core Value가 완성된다")과 충돌한다 —
> 사진 관찰은 Core의 보강 근거이고 없으면 그 섹션만 없다(§0).
>
> **그래서 위험은 '조용한 demo' 자체가 아니라 '조용한 demo인데 관찰을 만들어내는 것'이었다.**
> v1.22가 고친 결함이 정확히 그것이고(고정 영화 문구), 현재는 demo가 **관찰을 0개**로 둔다.
> ⚠️ 이 정책을 바꾸려면 §0·§1.5부터 다시 정해야 한다. v1.39는 **정책을 바꾸지 않았고**,
> Production이 실제로 이 약속을 지키는지 실측만 했다(§14.1 v1.39 항목).

#### demo / fallback은 관찰을 만들지 않는다 (v1.22)

`buildDemoObservedResult()`는 **어떤 trait도 만들지 않는다.**

| 호출부 | 언제 | 결과 |
|---|---|---|
| `runObservedTask` (서버) | `AI_MODE !== 'real'` | `traits: []` · `observedState: 'demo'` |
| `analyzeObservedProfile` (클라) | real 시도 후 `CONFIG_ERROR`/`SERVER_ERROR` | `traits: []` · `observedState: 'provider_failed'` |
| `SessionProvider` 복원 | v1.5 이전 세션 | `traits: []` (사용자 확인·수정 기록은 보존) |

> **왜 고쳤는가** — v1.21까지 이 함수는 `OBSERVED_TRAITS`(ob1~ob4 고정 문장)를 사진 **개수만**
> 보고 돌려줬다. 그래서 Provider가 붙지 않은 배포에서 **음식 사진만 올린 사용자에게
> "영화관·상영 시간표가 담긴 사진이 반복적으로 관찰됐어"가 자기 관찰로 보였다.**
> 화면에는 이미 "실제 사진 내용을 분석하지 않았어. 그래서 관찰 결과도 만들지 않았어."라는
> 정직한 문구가 있었고 **코드만 그 약속을 어기고 있었다.**
> 문구를 지우는 대신 **코드를 문구에 맞췄다.**

**샘플 세션은 분리돼 있다** — `buildSampleObservedResult()`가 고정 문장을 쓰는 유일한 함수이고,
`createSampleAnswers()`만 이 함수를 호출한다. 진입점은 dev 전용 `PrototypePanel`과
S06 `/profile/intro`의 '샘플 답변으로 결과부터 볼게' 두 곳이다(§6 S06 참고).
**사진을 업로드한 사용자의 결과 경로와는 완전히 분리돼 있다.**

### 8.5 Observed Me — 사진 분석 (Vision)

#### 4계층 분리 — 한 장의 사진에서 바로 '생활 패턴'을 만들지 않는다

| 계층 | 내용 | 만드는 주체 |
|---|---|---|
| LEVEL 1 Observation | 사진 1장에서 직접 보이는 것 (`야구장으로 보이는 장소`, `유니폼`) | **AI** (사진 1장씩) |
| LEVEL 2 Activity Signal | 관찰 라벨 → 활동 범주 (`야구장 + 유니폼` → `스포츠 관람·운동`) | 규칙 |
| LEVEL 3 Repeated Signal | 서로 다른 장면 몇 개에서 반복됐는가 | 규칙 |
| LEVEL 4 Hypothesis | `평소 즐기는 활동 중 하나일 가능성이 있어 보여` | 규칙 문장 템플릿 |

```
analyzePhoto(photo)              → PhotoObservation    (사진 1장 = Provider 1회)
aggregatePhotoObservations(...)  → ObservedSignal[]    (반복 판정은 여기서만)
```

> ⚠️ **Provider에게 반복 여부·`occurrenceCount`를 묻지 않는다.** Provider는 사진을 한 장씩
> 보므로 애초에 알 수 없고, 물어보면 **지어낸다.** 응답에 그런 필드가 섞여 와도 **스키마가
> 읽지 않는다**(Contract Test `photo_observation_repetition_claim`으로 고정).

**비용 주의** — 사진 1장당 Vision 1회다(사진 9장 = 9회). 동시 요청은 `PHOTO_CONCURRENCY = 3`.

#### 반복 기준

| 서로 다른 장면 수 | `strength` | `confidence` | 화면 문구 |
|---|---|---|---|
| 1 | `single` | low | 사진 한 장에서만 확인했어. 아직 평소 활동이라고 보긴 어려워 |
| 2 | `repeated` | medium | 사진 2장에서 비슷한 장면이 보였어. 반복인지는 아직 조심스러워 |
| 3+ | `strong_repeated` | high | 반복해서 보였어. 평소 즐기는 활동 중 하나일 가능성이 있어 보여 |

**세는 단위는 사진 장수가 아니라 장면 그룹이다.** 관찰 라벨 집합이 **Jaccard 0.75 이상** 겹치는
사진들은 '같은 날 같은 장소에서 연속 촬영한 것 같다'고 보고 한 그룹으로 묶어 **1회로** 센다.

> 픽셀·EXIF를 보지 않는 근사치라 다른 날 같은 카페 사진도 묶일 수 있다.
> **반복을 덜 세는 방향의 오류를 일부러 택했다** — 반대 방향은 없는 생활 근거를 만들어내는 것이다.

#### 절대 추론 금지

| 범주 | 내용 |
|---|---|
| **민감 범주** | 성적 지향 · 정치 · 종교 · 정신건강 · 질병 · 장애 · 경제 수준 · 범죄성 · 성생활 · 임신 · 민족/인종 · 연애 상태 · MBTI · 애착유형 |
| **관계 규정** | 사진 속 사람을 `친구`·`연인`·`가족`·`동료`라고 부르지 않는다. `다른 사람과 함께 있는 장면`까지만 |
| **성격 변환** | 여행 → 자유로운 성격, 책 → 지적, 운동 → 자기관리 철저 같은 변환 금지 |
| **사람 분석** | 얼굴 점수 · 매력도 · 나이 추정 · 체형 평가 · 표정에서 감정 읽기 |

> Observed Me는 **Behavior / Activity / Environment**까지이고, Personality는 Declared Me와
> MBTI Supporting Lens의 영역이다.

프롬프트가 1차 방어, `safety.ts`의 `scanPhotoObservation`이 **최후 방어선**이다.

**검사는 라벨 단위로 돈다** — `카페 테이블`과 `친구들`이 같은 사진에서 나왔다면 앞의 것은 쓸 수
있는 관찰이므로 **사진 전체를 버리지 않는다.** 걸린 라벨은 **고쳐 쓰지 않고 버리고**,
"사진에서 추론하지 않기로 한 항목이 있어서 일부 관찰은 제외했어"를 한계에 남긴다.

#### Evidence Provenance

모든 신호가 `photoIds`를 들고 다니고, '근거 보기'는 사진 단위로 무엇을 봤는지 보여준다
(`사진 1 · 야구장으로 보이는 장소 · 경기 관람 · 유니폼`).
사용자 수정은 `signal.id`를 키로 저장되므로 이후 Cross-source 분석에서 **AI 원본이 아니라
사용자 확인/수정 결과**를 쓴다.

**신호 0개는 실패가 아니다** — 개수를 채우려고 약한 관찰을 만들지 않는다. 최대 6개.

#### 이미지 준비 — `imagePrep.ts`

| 항목 | 값 |
|---|---|
| 장변 상한 | 1024px |
| JPEG 품질 | 0.8 |
| 개당 상한 | 1,200,000 bytes (초과 시 skip) |
| 대상 | **업로드 사진만.** 샘플 타일은 실제 이미지가 아니라 전송 대상이 아니다 |
| **EXIF** | 캔버스 재인코딩으로 **자동 제거된다** — GPS 좌표·촬영 시각·기기 정보가 결과에 남지 않는다. 부수효과가 아니라 이 방식을 택한 이유 중 하나다 |
| 파일명 | **보내지 않는다.** 세션 내부 id만 사용 |
| 실패 처리 | 손상 파일·0바이트·디코드 실패는 조용히 skip하고 `skipped` 수를 화면에 알린다 |

```
photoFingerprint(photos) = photos.map(p => `${p.source}:${p.id}`).join('|') || 'empty'
```

사진 id와 **순서**를 모두 반영하므로 한 장만 바꿔도 지문이 달라진다.

### 8.6 Structured Output + 2단 검증

자유 텍스트 하나로 받지 않는다. 모든 Task가 JSON 스키마를 강제하고 **2단**으로 검증한다.

**① Schema Parse** — 타입·길이·enum·허용 id 검사
**② Business Validation** — 스키마는 맞지만 제품 규칙을 위반한 응답 처리

| 위반 | 처리 |
|---|---|
| Evidence 없는 Trait | **버린다** |
| 입력에 없는 `imageId` 참조 | 그 evidence를 버린다 |
| `confidence: high`인데 evidence 1개 | `medium`으로 **강등** (사진 1장 단정 금지) |
| 규칙이 판정하지 않은 축 | 버린다 |
| 현재 궁합에 없는 dimension | 버린다 |
| 강한 결론 + `evidenceRefs` 0개 + uncertainty 없음 | 버린다 |
| AI가 보낸 `state`/`kind` | **규칙 값으로 덮어쓴다** |
| Core 설명에 MBTI·사주·별자리 언급 | 버린다 — Core Task에 그 데이터를 **보내지도 않았다** |
| History에 성장·극복·좋아졌다 서사 | 버린다 |
| 표시 길이 초과 | **버리지 않고** 문장 경계에서 자른다 |
| 상대 마음 읽기 | 버린다 |

> **한 절 안에서 감정·의도 동사를 찾는다.** 주어와 동사가 붙어 있을 때만 걸리면
> '상대는 너를 **더** 좋아하고 있어서'가 통과한다.
>
> 단 **'상대가 어떻게 느낄지는 알 수 없어'처럼 모른다고 말하는 문장은 의도적으로 남긴다** —
> 그건 마음 읽기가 아니라 **정직한 한계 진술**이다.

### 8.7 실패 분류 — `AiFailureReason`

| reason | 원인 | 화면 처리 |
|---|---|---|
| `CONFIG_ERROR` | 키 없음·401/403 | 규칙 결과로 진행(`fallback`) |
| `SERVER_ERROR` | 5xx | 〃 |
| `INVALID_OUTPUT` | 파싱 실패·400/422 | 재시도 가능 실패 |
| `POLICY_BLOCK` | Provider 안전 정책 거절 | 〃 |
| `RATE_LIMIT` | 429 | 〃 |
| `TIMEOUT` | AbortController | 〃 |
| `NETWORK_ERROR` | fetch 실패 | 〃 |
| `NO_USABLE_IMAGE` | 전송 가능한 사진 0장 | 〃 |

**Provider 원문 에러를 클라이언트로 올리지 않는다** — 분류만 전달한다.
오류 코드를 그대로 보여주지 않고 사람 말로 바꾼다.

### 8.8 Cache · Fingerprint

각 Narrative는 **자기 입력이 바뀔 때만** 다시 생성된다. 지문이 같으면 캐시를 쓰고, 달라지면
이전 응답은 stale로 버린다.

> ⚠️ **지문에 절대 넣지 않는 것** — 넣으면 위계가 무너진다.
>
> | 제외 | 이유 |
> |---|---|
> | MBTI | Supporting Lens가 Core 설명을 무효화시키면 안 된다 |
> | Birth Profile | Entertainment Lens |
> | Premium Intent / 가격 variant | **결제 의향이 분석을 흔들지 않는다** |
> | 사진 원본 | Observed Task는 `photoFingerprint`가 따로 만든다 |

즉 MBTI를 입력하거나 Premium을 눌러도 **Core AI 설명은 재호출되지 않는다.**

자유서술은 **원문이 아니라 길이만** 지문에 넣는다 — 사용자 문장을 지문에 남기지 않는다.

### 8.9 안전 경계 요약

| 경계 | 규칙 |
|---|---|
| Key 노출 | `server-only` import로 빌드 단계에서 차단 |
| Prompt Injection | 사용자 데이터는 system instruction과 섞지 않고 `wrapUserData()`로 delimit |
| Data Minimization | Task별 최소 컨텍스트만 전송. 상대 관계 맥락(`targetRelation`)은 **항상 `null`** |
| 요청 상한 | `AI_MAX_REQUEST_BYTES` (기본 12MB) |
| Rate Limit | 인스턴스 메모리 기반. `RateLimitStore` 인터페이스 경계만 준비됨 (**공유 저장소 미연결**) |
| 로깅 | `requestId`·task·duration·reason만. **사용자 원문·사진 미기록** |

---

### 8.10 Compatibility Evidence Vocabulary (v1.30)

> **Prompt · Schema · Resolver · UI가 같은 어휘를 써야 한다.**
> **하나만 달라도 문장은 조용히 사라진다.**

무료 Compatibility의 AI 설명이 화면에 한 문장도 닿지 않던 결함을 고친 뒤의 계약이다.

| 단계 | 값 | 예시 |
|---|---|---|
| `buildCompatibilityContext` | `dimensions[].ref` | `{source:'compatibility', field:'contact'}` |
| prompt | 지시 | "각 dimension의 `ref`를 그대로(수정 없이) 복사" |
| model | `evidenceRefs[]` | 위 ref를 그대로 |
| `parseCompatibilityResponse` | `allowedMap` 검증 | 축이 아니면 그 ref를 버린다 |
| `resolveEvidenceRef` | `case 'compatibility'` | `연락 — 나: … · 상대: …` |
| `narrativeIsShowable` | boolean | 근거 0개면 렌더 안 함 |

예전에는 프롬프트가 `"field": "필드명"` 자유 서술을 허용했고, 모델은 입력 JSON에
보이는 이름(`minePhrase` · `"contact importance"`)을 매번 다르게 지어냈다. 파싱은
통과하지만 resolver가 풀지 못해 근거 0개가 되고, API는 200이라 실패 안내도 뜨지 않았다.

⚠️ resolver에 화면용 별칭을 늘리지 않는다. **모델이 받는 어휘를 canonical key로 맞춘다.**
⚠️ 파서는 `compatibility` source만 검사한다 — 다른 source의 field 어휘는 모르므로
판단하지 않는다.

⚠️ E3(관측 정보 부족)에서는 AI를 **호출하지 않는다**. `LowConfidenceView`에는 narrative를
그릴 자리가 없어서, 예전에는 쓸 수 없는 응답을 사 오고 있었다(`score !== null` 게이트).

⚠️ narrative는 **localStorage에 저장되지 않는다**(`aiClient`의 세션 범위 in-memory Map).
그래서 프롬프트를 바꿔도 stale narrative가 남지 않는다 — fingerprint에 promptVersion을
넣을 필요가 없다.
## 9. Premium

### 9.0 무료 / Premium 경계 (v1.26 · P3-3)

> **무료에서는 하나의 관찰을 충분히 이해하게 하고,**
> **Premium에서는 서로 다른 관찰 사이의 연결을 해석한다.**

| | FREE | PREMIUM |
|---|---|---|
| 단위 | 한 Lens · 한 Signal | Signal **사이의 연결** |
| 질문 | 무엇이 보이는가 | 왜 함께 나타나는가 |
| 범위 | 한 장면 | 여러 장면의 패턴 |
| 깊이의 출처 | 그 관찰을 끝까지 설명 | Cross-source Interpretation |

**왜 재설계했나** — v1.25에서 무료 MBTI가 깊어지면서 "왜 ₩1,900을 내야 하지?"가 실제
제품 질문이 됐다. Audit 결과 Deep Report의 두 섹션이 무료 문장을 **글자 그대로** 다시
보여주고 있었다:

| 제거한 섹션 | 실체 |
|---|---|
| `궁합 심화 비교` | `buildCompatibilityDetail`이 무료 `SignalCard`와 같은 `dimension.evidence` / `dimension.scene`를 그대로 씀 (v1.23부터 남아 있던 Remaining Risk) |
| `실제로 일어날 수 있는 상황` | `situationFor`가 같은 `AXIS_DEFINITIONS.scene.watch` / `evidence.watch`를 재출력 |
| `이야기해볼 질문` | 무료 질문 텍스트를 그대로 옮김 → 연결을 검증하는 질문으로 대체 |
| `러비의 최종 관찰` | `insights[0].ruleSummary`를 리포트 맨 아래에 다시 적음(맨 위 CORE PATTERN과 동일) |

#### Cross-source 조합 (`lib/logic/crossSourceInsights.ts`)

| # | 조합 | 만들어지는 조건 | 비고 |
|---|---|---|---|
| ① | Declared ↔ Relationship (+Observed 보강) | Mirror 판정이 UNKNOWN이 아닐 때 | ④가 같은 축을 이으면 **억제**(중복) |
| ② | Relationship(hardest) ↔ Target | 과거 통증 축과 상대 특성이 겹칠 때 | |
| ③ | Current ↔ History | SHIFT/NEW · 반복 신호 | |
| **④** | **Compatibility ↔ Relationship / Mirror / History** | **v1.26 신설.** 동기화율에서 판정된 축이 Mirror·History와 같은 축을 가리킬 때 | 이 Engine이 v1.25까지 `CompatibilityResult`를 **입력으로 받지도 않았다** |
| **⑤** | **MBTI Lens ↔ Relationship Signal** | **v1.26 신설.** v1.24 Bridge가 DIFFERS로 판정한 축이 관계 경험과 겹칠 때 | 무료 4축 설명을 **재출력하지 않는다** |

④가 v1.26의 핵심이다. `buildCompatibility`는 관계 경험을 읽지 않고 `buildMirrorReport`는
상대 정보를 읽지 않는다 — **서로 독립적으로 계산된 두 판정이 같은 축을 가리키는 것**이
연결의 정보값이다.

#### 인과 vs 연관 (절대 경계)

| 금지 | 허용 |
|---|---|
| 과거 관계 **때문에** 지금 연락에 민감해졌다 | 과거에 비슷한 장면이 있었고, 지금 신호도 **같은 방향으로 보인다** |

그래서 모든 `DeepConnection`은 `limitation`을 **하나 갖는다** — 없으면 만들지 않는다.
`limitation`은 type이 아니라 **source 조합**에서 나온다(무엇을 이었는지가 무엇을 말할 수
없는지를 결정한다).

#### Deterministic vs AI

```
DETERMINISTIC  어떤 signal을 연결할지 선택        → ruleSummary (규칙)
AI             그 연결을 자연스럽게 설명          → narrativeText (문장)
```

AI는 **판정을 바꾸지 못한다.** `ruleSummary`가 항상 남고 `narrativeText`는 그 옆에 붙는다 —
AI가 실패하면 `narrativeText`가 null이고 리포트는 규칙 문장만으로 완결된다(§50 Retry는
AI 설명만 재시도하고 리포트는 사라지지 않는다).

#### AI Narrative Contract (v1.27)

```
AI_OUTPUT ⊆ DETERMINISTIC_EVIDENCE
```

v1.26 실측에서 **안전 검사를 전부 통과한** 두 종류의 실패 문장이 나왔다.

| 실패 | 규칙이 확인한 것 | AI가 쓴 것 |
|---|---|---|
| 인과 창작 | 두 관찰이 같은 축을 가리킨다 | `이 차이가 소통 방식에 영향을 미칠 수 있을 것 같아` |
| 되풀이 | `… 같은 축을 가리키고 있어.` | `… 같은 축을 가리키고 있어.` (글자 그대로) |

첫 번째는 헤지(`~수 있을 것 같아`)가 붙어 `hasUnsupportedCertainty`를 통과했다 —
**문제는 확신의 강도가 아니라 주장의 종류다.** 두 번째는 위험한 주장이 없어서 통과했지만
사용자는 같은 말을 두 번 읽는다.

**모델이 받는 세 칸** (`deep-report-v2-bounded`)

| 칸 | 내용 | 출처 |
|---|---|---|
| `OBSERVED FACTS` | 실제 세션 데이터로 만든 근거 | `aiEvidenceResolver` |
| `ALLOWED CONNECTION` | 규칙이 확인한 주장. **이것만 참이다** | `insight.ruleSummary` |
| `LIMITATION` | 이 연결이 말할 수 없는 것 | `limitationFor(sources)` |

v1.26까지는 evidence와 type 라벨만 보냈다 — 모델은 **규칙이 어디까지 확인했는지 몰랐다.**
`LIMITATION`은 화면에 보이는 것과 **같은 문자열**이고, **AI가 쓰지 않는다**(v1.26까지는
`narrative.uncertainty`가 규칙 경계를 덮어썼다).

**Quality Gate**

| 게이트 | 무엇을 막는가 | 위치 |
|---|---|---|
| (A) | 근거 2개 미만은 AI에 보내지 않는다 | `buildDeepReportContext` |
| (B)(C) | 일반론 · 근거 없는 확정 표현 | `scanDeepNarrative` |
| (D) | 마음 읽기 · 진단 · 민감 추론 | `scanForForbiddenInference` |
| (E) | 원래 Insight에 없던 `evidenceRef` | `evidenceRefsAreSubsetOf` |
| **(F)** | 규칙 문장 되풀이 (bigram novelty < 0.35 또는 글자 그대로 재사용) | `isRedundantNarrative` |
| **claim** | 인과 · 예측 · 의도 · 가치판정 | `scanClaimBoundary` |

⚠️ (F)의 임계값 `0.35`는 실측 3건으로 잡은 **초기 캘리브레이션 값**이다 — **NOT VALIDATED**.
상수는 `safety.ts` 한 곳에 있고, 관측치는 dev 로그의 `novel=`로 모은다.
`reference`(=`ruleSummary`)가 비어 있으면 판정하지 않는다(통과).

`scanClaimBoundary`는 **한계 문장을 예외로 둔다** — `원인이라고 말할 수 없어`를 막으면
경계 자체를 쓸 수 없다. 금지 대상은 **주장**이다.

걸리면 그 narrative만 버린다. 화면은 `ruleSummary`로 완결되므로 **AI가 하나도 안 남아도
리포트는 사라지지 않는다.**

그래서 **`AI 설명` 배지는 `mode`가 아니라 화면에 실제로 그려진 문장을 보고 붙인다**
(v1.28). Premium은 `narrativeText`가 붙은 연결이 있는지를, 무료 Compatibility는
`hasShowableNarrative`(렌더러와 같은 술어)를 본다 — 없는 것을 있다고 표시하지 않는다.
`규칙 기반 대체` 태그만 예외로 무조건 표시한다. AI가 있다는 주장이 아니라 AI 없이
규칙으로 채웠다는 고지라서, 문장이 없을 때 오히려 필요하다.

**실측**

```
[ai] deep-report filter parsed=5 refChecked=5 safe=5 novel=1
```

5개 전부 안전했지만 규칙에 뭔가를 더한 것은 1개다. AI narrative는 `모든 연결을 더 잘
설명하는 층`이 아니라 `추상적인 규칙 문장에 실제 값을 채워 넣을 수 있을 때만 값을 더하는
층`이다 — 분량을 억지로 늘리지 않는다.

#### 근거의 React key (v1.31)

`DeepConnection.evidence`는 `{ key, sourceLabel, text }`다. `key`는
`resolveEvidenceRefs`가 만든 canonical 식별자(`source:id:axis`)이고 **화면이 이것을
React key로 쓴다.**

⚠️ **문장을 key로 쓰지 않는다.** 서로 다른 근거가 같은 문장으로 풀리는 조합이 실제로
있다 — 같은 날 저장된 기록 두 건(날짜 접두어까지 같다), `declared:contact`와
`declared:contactImportance`(`resolveDeclared`가 같은 문장을 돌려준다).

⚠️ React key에 자유서술·생년월일·MBTI·사진 내용을 넣지 않는다.
#### Report IA

```
 00 HEADER        PRECISION REPORT (Unlock → Report layout continuity 유지)
 01 CORE PATTERN  정보 N종 · 연결 M개 + 가장 중요한 연결 1개   ← 첫 viewport
 02 CONNECTIONS   나머지 연결 (source 2개 이상)
 03 SINGLE NOTES  근거 하나뿐인 관찰 — 더 낮은 위계 (cross-source 우선)
 04 HISTORY       과거 관찰과 지금 (기록 2개 이상일 때만)
 05 TRY THIS      TRY / CHECK / NOTICE + 연결을 검증하는 질문
 06 LOVY DEEP     러비의 깊은 관찰 + 관계 철학 질문
    한계
```

`corePattern`은 `sourceCount >= 2`인 연결 중 **가장 많이 이은 것**이다. 하나도 없으면
null이고 리포트는 `available: false`로 정직하게 끝난다(억지 리포트 금지 · view event 분모
오염 금지).

#### Connection Visual

```
동기화율 비교 ─┐
내가 답한 기준 ├─ 연결
관계 경험    ─┘
```

CSS/SVG만 쓴다(chart library 없음). 연결선은 `aria-hidden`이고, 같은 정보가 source 라벨과
ruleSummary에 텍스트로 이미 있다. gold/black/glow/gradient 금지 — Premium은 *더 화려함*이
아니라 **더 촘촘하고 연결된 문서**다.

#### Availability Gate — 무엇이 있으면 팔 수 있는가

`hasDeepConnection(insights)` 한 곳에서만 판정한다. Paywall 게이트(`premiumFeatureState`)와
리포트의 `available`이 **같은 기준**을 쓴다 — 다르면 "Paywall은 열리는데 리포트는 없다"가 된다.

| 조건 | 이유 |
|---|---|
| 서로 다른 source 종류 **2개 이상** | 이어야만 보이는 신호여야 한다 |
| 근거 ref **2개 이상** | 사용자가 확인할 수 있어야 한다 |

> **v1.26 Audit에서 고친 것** — 예전 게이트는 `insights.length > 0`이었다. 그래서 Mirror
> CHANGE 하나(`sources: [declared]`)만 있는 세션에서도 Paywall이 열리고, 열린 리포트에는
> 연결이 없어 단일 관찰 한 줄만 남았다. **실측 확인**: 수정 후 그 세션은 가격·CTA 없이
> "아직 서로 연결해서 볼 수 있는 신호가 부족해"로 정직하게 닫힌다.

#### Connection Type ↔ Rank ↔ Copy

| INPUT TONE | CONNECTION TYPE | RANK | 사용자에게 보이는 문장 |
|---|---|---|---|
| `watch`(차이) | `GAP` | TYPE_RANK 1 | "지금 상대와 {축}에서 **차이가 보이는데**, 이 축은 …" |
| `good`(비슷) | `MATCH` | TYPE_RANK 4 | "지금 상대와 {축}에 대한 **기대는 비슷한데**, 이 축은 …" |
| `neutral` | 생성 안 함 | — | — |
| `unknown`(모름) | 생성 안 함 | — | — |

⚠️ **`GAP`/`MATCH` 문자열은 화면에 노출되지 않는다.** v1.26에서 구 `DeepInsightCard`의
type 배지를 없앴으므로, type은 **순위 계산**과 **러비 관찰 선택**에만 쓰인다. 그래서
"GAP = 나쁨 / MATCH = 좋음"으로 읽힐 경로가 UI에 없다.

TYPE_RANK 순서는 v1.9부터의 기존 설계이고 '나쁨 우선'이 아니라 **'예상과 어긋난 것 우선'**
이다(CONTRADICTION → GAP → REPEATED → CHANGE → MATCH). 이 순서를 바꾸면 Mirror·History
화면의 기존 정렬까지 바뀌므로 v1.26에서 **변경하지 않았다.**

#### History 연결 — v1.26 실측으로 고친 3건

| 발견 | 조치 |
|---|---|
| `buildHistoryDetail`이 `previousText`/`currentText`를 `mine`/`theirs`에 담아 화면이 **나 / 상대**로 렌더 — 사실이 아닌 라벨 | `PremiumDetailSection.mineLabel`/`theirsLabel` 신설. History는 **이전 기록 / 최근 기록**. 다른 상세 4종은 기본값(나/상대) 그대로 |
| ④가 "이전 관찰에서도 **반복해서** 나온 축"이라고 말하면서 근거는 `entryIds[0]` **1건**만 걸었다 | 반복 판정에 쓰인 기록 **전부**를 근거로 건다 |
| ③ history CHANGE의 note는 **저장된 두 기록** 비교인데 두 번째 근거가 `declared`(현재값)라 화면에서 모순 — note "(2/5 → 3/5)" vs 근거 "5점 중 5" | 비교에 실제로 참여한 **두 기록**을 근거로 건다. source가 `history` 하나뿐이 되어 연결이 아닌 단일 관찰로 내려간다 — 그게 사실에 맞는 위계다 |
| 과거 근거 2건의 텍스트가 완전히 동일해 **'2번'을 확인할 수 없었다** | `resolveHistory`에 기록 날짜를 붙인다 (`2026.06.01 기록에서도 …`) |

**실측 결과**

| CASE | 기록 | 결과 |
|---|---|---|
| H1 | 0건 | HISTORY 섹션 미표시 · `과거 관찰` source 없음 · 억지 변화 서술 0 |
| H2 | 1건 | `comparable: false` — CHANGE/STABLE 선언 없음 · '반복' 표현 0 · 정직한 한계 문구 |
| H3 | 2건(contact GAP ×2) | ④ 반복 연결 발화 · 근거 5개(과거 2건 포함) · HISTORY 섹션 렌더 · 인과 부정 문장 3개 |
| H4 | 2건 동일, 현재만 다름 | '항상/원래/본래' **0건** · 반복 주장 없음(과거가 MATCH라 반복 신호 미생성) |

'반복'이라는 단어가 화면에 나오는 유일한 자리는 NOTICE 액션(**앞으로** 관찰해볼 것)이다.

#### Analytics · Privacy

**새 이벤트 0개.** 근거 펼치기는 기존 `deep_insight_evidence_expand`를 재사용하고
(`source_count`만 property로 추가), `deep_report_view`/`deep_report_complete`의 의미(분모·
분자)는 그대로다 — `insight_count`가 세는 대상만 새 구조에서 다시 센다.
Hook Attribution key(`friction_why`/`mirror_why`/`history_change`)도 불변이다.

---


> ⚠️ **실제 결제(PG)를 붙이지 않았다.** PG SDK · 결제 서버 · webhook · 주문 DB · 환불 · 구독
> 전부 미구현이다. 목적은 '사용자가 무료 핵심 결과를 확인한 뒤, 더 깊은 결과를 보기 위해
> **실제로 결제 행동까지 시도하는가**'를 측정하는 것이다.

### 9.1 Fake Door — 무엇을 하지 않는가

| 금지 | 이 구현에서 |
|---|---|
| 카드번호 입력 유도 후 중단 | ❌ **결제 수단을 아예 받지 않는다** |
| 결제가 가능한 것처럼 허위 표시 | ❌ '결제됩니다' 문구 없음. 구매 시도 **직후** 준비 중 공개 |
| 결제 완료 Fake 화면 | ❌ 없음 |
| 환불·구독이 있는 것처럼 표현 | ❌ `상세 분석 1회 · 구독 아님` 명시 |
| 이메일·전화번호 수집 | ❌ 알림 의향은 `notifyIntent: true` **로컬 기록만** |

**흐름:** Premium CTA → 가치/가격 안내 → `[상세 분석 열기]` → **'준비 중' 안내** → 관심 표시(선택)

**Production 문구**

> 정밀 관찰 리포트는 지금 준비 중이야
> 아직 결제도, 리포트 연결도 전이야. 방금 누른 건 결제가 아니라 관심 표시로만 기록했어.

### 9.2 가격

| 항목 | 값 |
|---|---|
| 가격 | **₩1,900** (MVP 단일 가격 · HYPOTHESIS) |
| variant | A/B 실험 골격만 남아 있고 **두 variant 모두 1900** — 사용자는 항상 같은 가격을 본다 |
| 과거 | 3,900/4,900원 A/B는 종료됨. 그 시절 설문 데이터는 **검증된 가격이 아니다** |
| 고정 | 같은 세션에서 가격이 바뀌지 않도록 sessionStorage에 고정 |

### 9.3 무료 / Premium 가치 경계

**무료에서 Core Value가 완성된다.** Premium은 무료 결과를 잘라내는 기능이 아니다.

| | 답하는 질문 | 형태 |
|---|---|---|
| **FREE** | 무슨 신호가 있고, 왜 그게 흥미로운지 | **하나의 의미 있는 관찰** |
| **PREMIUM** | 여러 신호가 왜 함께 나타나는지, 어떤 상황으로 이어질 수 있는지, 다른 Evidence와 어떻게 연결되는지 | **관찰 사이의 이유와 연결** |

| 영역 | FREE | PREMIUM | 중복 | 조치 |
|---|---|---|---|---|
| 축별 차이 값 | ○ (PRIMARY 1개 + compact 나머지) | ○ (4축 전체) | **있음** | Premium에서 **같은 문장을 다시 쓰지 않는다** — 무료가 '무엇이 다른가', Premium은 '왜 함께 나타나는가' |
| 핵심 한 문장 | ○ | ✕ | 없음 | — |
| YOUR SIGNAL (실제 답변 인용) | ○ | ✕ | 없음 | — |
| LOVY OBSERVATION (심리·철학) | ○ **1개 보장** | ✕ | 없음 | 무료 Aha의 핵심 |
| Cross-source 연결 | ✕ | ○ | 없음 | Premium 고유 가치 |
| History 연결 | 제한적(반복 신호 알림) | ○ (축별 변화 + 연결 설명) | 부분 | 무료는 '있다'만, Premium은 '어떻게 이어지는가' |
| 행동 제안 | ○ Short (다가갈 때 · 질문) | ○ Why 연결 | 부분 | Premium은 제안이 아니라 **그 제안이 나온 이유의 연결** |
| Deep Question | ✕ | ○ | 없음 | — |

| 영역 | FREE | PREMIUM |
|---|---|---|
| Compatibility | 동기화율 · 비교 신호 수 · 대표 Good 1개 · 대표 Friction 1개 · **핵심 한 문장** · **FIRST SURPRISE + YOUR SIGNAL** · **LOVY OBSERVATION 1개** | 4축 전체 상세 · 실제 상황 예시 · 대화 질문 · 과거 관찰 연결 |
| Mirror | 핵심 GAP/MATCH/CHANGE · Core Insight · 기본 근거 | 판정된 모든 축 상세 · Adaptive 응답 포함 심화 근거 · 과거 연결 |
| History | **저장 · 타임라인 · 기본 비교 · 삭제** | 축별 변화 상세 · 반복 신호 연결 설명 · 다음 관계 확인 기준 |
| MBTI | 4축 비교 요약 | 4축 상세 · 관계 신호와 나란히 비교할 질문 |
| Astrology | Sun Sign 기본 Lens | 태양궁 상세 · 원소 비교 확장 · 대화 주제 확장 |
| 사주 | UI·한계 안내 | **없음 (unavailable)** — 엔진이 없으므로 **팔지 않는다** |

> **Guardrail 1.** 사용자 **본인의 저장 기록은 무료다.** Premium으로 잠가서 자기 과거 기록을
> 못 보게 만들지 않는다 — 유료는 '해석의 깊이'만이다.
>
> **Guardrail 2.** Premium에서도 **수집하지 않은 데이터를 새로 추론하지 않는다.**
> 새 점수를 만들지 않는다 — `buildCompatibilityDetail()`은 이미 계산된 `CompatibilityResult`를
> 그대로 읽는다.
>
> **Guardrail 3.** 무료가 **'Preview만 보여주고 결제하라는 화면'**처럼 느껴져도 안 된다.
> 무료 결과 자체로 적어도 하나의 작은 Aha가 있어야 한다.

### 9.3.1 잠금 표시 정책 (v1.23 §16)

무료 결과 영역에 **locked teaser를 남발하지 않는다.** 🔒가 반복되면 '무료는 아무것도 못 보네'가
학습된다. 잠긴 목차는 **Premium Entry 블록 하나 안에서만** 보여준다.

현재 무료 Compatibility Result의 잠금 표시: **Premium Entry 1개**(friction이 있을 때만).

### 9.4 Data Model

```
ResultLevel = 'summary' | 'detail'      // UI에서 잘라내지 않고 계약으로 분리

PremiumFeatureId = compatibility_detail | mirror_detail | history_detail
                 | mbti_detail | astrology_detail | saju_detail
                 | relationship_deep_report        // flagship 통합

PremiumFeature
├─ id · source · title · description · additions[]
├─ price   number | null      ← unavailable이면 null (가격을 붙이지 않는다)
└─ status  'available' | 'fake-door' | 'unavailable'    ← 기본은 fake-door

PremiumIntent                 // lym.premium-intent.v1 (세션 저장소와 분리)
└─ feature · source · price · variant · clickedAt · notifyIntent
```

> `PremiumIntent`를 `SessionAnswers`에 섞지 않은 이유 — **결제 상태가 분석 결과에 영향을 줄 수
> 없게 구조적으로 떼어놓기 위해서다.** Compatibility·Mirror·History 로직은 이 저장소를
> **import하지 않는다.**

`resetTargetContext()`(새 상대 시작)가 `clearPremiumIntents()`도 함께 호출한다 —
`PremiumIntent`가 `feature`로만 키를 잡고 Target을 구분하지 않아서, 지우지 않으면 이전
상대에게서 나온 '출시되면 알려줘' 클릭이 새 상대의 Paywall에 남아 있는 것처럼 보인다.

### 9.5 Relationship Deep Report (flagship)

`buildRelationshipDeepReport()`가 **8개 섹션**을 IA 순서대로 만든다. 데이터가 있는 섹션만 렌더한다.

```
Overview → Relationship Self → Cross-source Insight → Compatibility Deep Dive
        → Situations → Questions → History → Final Observation
```

| 항목 | 내용 |
|---|---|
| 가치 정의 | '더 긴 설명'이 아니라 **'따로 있던 결과를 연결했을 때만 보이는 것'** |
| Cross-source Insight | Declared · Relationship Evidence · Observed(사진) · History를 교차 |
| Evidence Hierarchy | Core > Supporting > Entertainment. 위계를 섞지 않는다 |
| AI 호출 시점 | **결제(또는 Preview Unlock) 이전에는 AI를 호출하지 않는다.** 규칙 기반 리포트 자체는 항상 준비돼 있다 |
| 규칙 기반 고지 | 일부 상세는 원래부터 100% 규칙 기반이다 — "데모라서 규칙 기반"이 아니라 **"원래부터 규칙 기반"**이라 AI_MODE와 무관하게 항상 표시한다 |

### 9.6 Paywall → Report 전환

`/premium` **한 Route**가 5 stage를 갖는다 (새 Route를 만들지 않았다 — Route를 나누면
layout continuity가 깨진다).

```
paywall → leaving → success → revealing → report
```

| 항목 | 내용 |
|---|---|
| 전환 시간 | 약 300ms — **상태 변화를 인지시키는 presentation layer 전용**. 실제 처리를 늦추지 않는다 |
| 연출 금지 | 자물쇠 · 금색 효과 · confetti · 폭죽 ❌ |
| 대신 쓰는 표현 | `LOCKED` → `추가 관찰 완료` → `REPORT AVAILABLE` |
| 연속성 | shared-element 라이브러리 없이 `PRECISION REPORT` 라벨의 **layout continuity**로 만든다 |
| **Production 경계** | `NEXT_PUBLIC_PREMIUM_PREVIEW=false`(Production)는 **Fake Door BottomSheet만** 본다 |
| Preview/UT에서도 | **'결제가 완료됐어'라고 말하지 않는다** — "미리보기로 리포트를 열었어 · 실제 결제는 아니야" |
| 가격 노출 | 실제 `payment` mode에서만 |

---

### 9.7 Solo / First Contact (v1.29 · P4)

> **COUPLE — 나와 상대 사이의 차이**
> **SOLO — 나 안에서 함께 나타나는 기준**

기존 리포트는 전부 '두 사람'을 전제로 한다. 그래서 상대가 없고 경험도 없는 사용자는
동기화율 `?`와 "Mirror를 만들 수 없어"를 지나 홈으로 돌아갔다(P4 Audit 실측).

#### 세 상태 (`lib/logic/soloMode.ts`)

| mode | 뜻 | 판정 | 목적지 |
|---|---|---|---|
| `couple` | 비교할 만큼 안다 | `targetKnownCount >= TARGET_MIN_KNOWN(3)` | `/compatibility` |
| `unknown_target` | 사람은 있는데 아는 게 적다 | `relation !== null` 또는 known > 0 | `/first-contact` |
| `no_target` | 지금 특정 상대가 없다 | 그 외 | `/first-contact` |

⚠️ **새 점수를 만들지 않는다.** 이미 있는 `targetKnownCount`만 읽고, 동기화율·Mirror·
History 계산에는 들어가지 않는다.

⚠️ `unknown_target`이면 **상대를 추론하지 않는다** — 아직 `모름`으로 남긴 축에 대해서만
'물어볼 것'을 준다(`GETTING_TO_KNOW`).

⚠️ 프로필 결과 화면의 분기는 `soloModeOf`가 아니라 **`answers.status`**로 한다.
그 화면은 target 입력 **전**이라 모든 사용자가 `no_target`으로 읽힌다.

#### First Contact Report (`/first-contact` · `lib/logic/firstContact.ts`)

```
핵심 한 문장            두 답을 규칙으로 이어 붙인다(단계가 다르면 '지만', 같으면 '고')
01 WHAT YOU VALUE       내가 고른 값 그대로. 해석을 섞지 않는다
02 WHAT TO NOTICE       내 답변 두 개가 같은 자리를 가리킨다는 관찰 ← SOLO의 정보값
03 LOVY OBSERVATION     질문으로 끝난다(진단 아님)
04 GETTING TO KNOW      unknown_target에서만
05 FIRST CONTACT        TRY / ASK / NOTICE
06 OTHER LENSES         MBTI 있을 때만
```

- 랜덤이 없다. 같은 답에는 항상 같은 문장·같은 순서가 나온다.
- 섹션 번호는 **실제로 렌더되는 것만** 세어 붙인다. 데이터가 없으면 섹션이 아예 없다.
- 내 기준이 3개 미만이면 `available: false` — 빈 섹션을 늘어놓지 않는다.
- `firstContact.ts`는 `CompatibilityResult`도 `MirrorReport`도 **import하지 않는다.**

#### `WHAT TO NOTICE`의 규칙

`SELF_PAIR_RULES`에 등록된 조합만 나온다(전수 조합을 만들지 않는다). 각 항목은
**`limitation`을 반드시 갖는다** — Premium 연결과 같은 규칙이다. 하나도 안 맞으면
이 섹션은 비운다.

#### `해볼 수 있는 것`의 3칸 구조

```
SELF SIGNAL → WHY → ACTION
```

첫 칸이 없으면 일반 연애 팁이다. `FIRST_CONTACT_ACTIONS`가 축·단계로 걸러지므로
**목록 길이가 사람마다 다르다** — 분량을 맞추려고 채우지 않는다.

#### Solo Premium — 생성기 ⑥ · ⑦ · ⑧ (v1.32 → v1.35)

> **사진은 보강 근거이지 Solo Premium의 필수 입장권이 아니다.**

| | 조합 | 여는 조건 |
|---|---|---|
| ⑥ | `declared × observed` | 사진 관찰(반복 신호 또는 축 키워드) |
| ⑦ | `declared × mbti_lens(self)` | 내 MBTI + 개인 시간 답변(보통 제외) |
| ⑧ | `history × declared` (v1.35) | Solo 관찰 1건 이상 + **두 축 이상이 함께 CHANGE** |

⑧은 상대도 사진도 MBTI도 없이 열린다 — 상세는 [§9.8.10](#9810--solo-history--current-declared-v135).
실행 순서상 **⑦보다 먼저** 만든다: ⑦의 `covered` 중복 회피가 ⑧이 이미 다룬 축까지 보게 하기 위해서다.

⑦은 **`audience === 'solo'`일 때만 만들어진다**(v1.33). 커플에서 MBTI를 잇는 것은
⑤(P3-1 MBTI ↔ Relationship Bridge)의 몫이다. audience는 화면과 **같은 함수**
(`soloModeOfTarget`)로 판정한다 — 새 boolean을 만들지 않는다.

⚠️ v1.32까지는 `covered.has('alone')`로만 억제됐는데, 그건 축 중복 회피 장치이지
audience 판정이 아니다. `alone`이 덮이지 않는 커플 세션에서는 **실제로 Solo 연결이
커플 리포트에 들어갔다**(실측). 지금 `covered`는 중복 회피 역할만 한다.

⑦은 **`energy ↔ alone` 한 축만** 쓴다 — v1.24가 4축 중 의미가 겹치는 하나만 연결하고
나머지는 stereotype 위험으로 기각했고, **Premium이라고 더 많이 연결하지 않는다.**

```
❌  I라서 혼자 있는 시간이 필요하다
⭕  성향 렌즈에서는 내향(I) 쪽이고, 실제로 답한 개인 시간 기준도
    혼자 있는 시간을 중요하게 답했어. 두 답이 같은 방향으로 나타났어.
```

⚠️ `mbtiLens`는 양쪽 MBTI가 있어야 만들어지므로 Solo에게는 null이다. 그래서
`EvidenceResolverContext`가 `mbtiSelfLens`를 함께 받아 self-only 경로를 연다 —
**새 source를 만들지 않는다.** 상대 MBTI가 없으면 상대 쪽은 문장에도 없다.

⚠️ **같은 questionnaire의 두 답을 source 2개로 세지 않는다.** 무료의
`WHAT TO NOTICE`(declared × declared)가 아무리 많아도 Premium은 열리지 않는다.
`experience.skipped`도 플래그일 뿐 source가 아니다.

⚠️ 판정 기준은 **커플과 동일**하다(`hasDeepConnection`) — 새 기준을 만들지 않았다.
#### (v1.29) ⑥ 상세

Solo가 만들 수 있는 Cross-source 조합이 **하나도 없어서** Premium이 구조적으로
영구 unavailable이었다. 그래서 조합 하나를 신설했다.

```
declared   사용자가 답한 관계 기준
observed   사진에서 반복해서 나타난 활동(사용자가 확인·수정한 것만)
```

| | FREE | PREMIUM |
|---|---|---|
| Solo | 내 기준 하나씩 · 답변끼리 함께 나타난 것 | 내 기준 ↔ **사진 관찰**이 같은 축을 가리키는지 |

- **판정 기준은 커플과 동일**하다(`hasDeepConnection`: 서로 다른 source 2종 + 근거 2개).
  사진이 없으면 열리지 않는다.
- **이미 다른 조합이 덮은 축에는 만들지 않는다** — ①의 CONTRADICTION 승격이 같은
  observed 근거를 쓰므로, 커플 리포트가 바뀌지 않게 ⑥은 빈 자리만 채운다.
- 사진 한 장짜리 관찰과 반복 관찰을 같은 강도로 세지 않는다(`repeated` 분기).
- 사진으로 성격을 판단하지 않는다 — `limitationFor`의 `observed` 분기가 항상 붙는다.
- Premium unavailable 문구는 Solo에게 다르다: "사진을 올려서 관찰을 확인해두면" —
  **갈 수 없는 길을 알려주지 않는다.**

#### Navigation — 새 탭 0

`홈 / 나 / 분석 / 관찰기록` 유지. '분석' 탭이 Solo도 담당한다.

| | 목적지 | 열리는 조건 |
|---|---|---|
| couple | `/compatibility` | `completed.compatibility` |
| solo | `/first-contact` | `completed.profile` |

`couple`이 `/first-contact`로 직접 들어오면 `/compatibility`로 보낸다 — 빈 화면은 거짓이다.

#### 금지 (§19 · §29 · §47 · §48)

연애 성공 확률 · 연애 준비도 점수 · '솔로인 이유' 진단 · 애착 유형 · 자존감 ·
연애 초보/모솔 호칭 · 하트/커플 실루엣/pink gradient/match animation.

**관계 경험 없음은 결함이 아니다.** 그래서 문구도 '데이터가 부족해'로 끝내지 않는다:
"과거 관계에서 확인할 기록은 아직 없어. 그래서 이번에는 네가 지금 중요하게 생각하는
기준을 중심으로 볼게."

#### Analytics

`solo_report_view` · `solo_action_view` **2개만**. Primary KPI(Compatibility)를 오염시키지
않는 별도 secondary funnel이다. property는 categorical count/state뿐 —
raw answer·headline 원문·축 값은 보내지 않는다.

### 9.8 Solo History / Retention (v1.34 → v1.35)

> **CURRENT** = 지금의 기준 (live interpretation)
> **HISTORY** = 당시의 frozen observation
> **CHANGE** = 두 snapshot의 비교
>
> **Retention Principle** — 러비는 한 번의 결과를 기억하는 게 아니라,
> 시간이 지나며 달라지는 기준을 관찰한다.

#### 9.8.1 Reset 정책 — History는 Target lifecycle과 분리된다

| DATA | SELF/TARGET | NEW TARGET에서 초기화? | 근거 |
|---|---|---|---|
| Solo History 전체 | SELF | **아니오** | 다른 저장소(`lym.history.v1`). `resetTargetContext()`가 구조적으로 닿지 않는다 |
| Couple History 전체 | SELF | **아니오** | 같음 |
| `declared` (내 기준) | SELF | 아니오 | |
| `mbti` (내 유형) | SELF | 아니오 | |
| `experience` (관계 경험) | SELF | 아니오 | |
| `photos` · `observedAnalysis` · `observations` | SELF | 아니오 | |
| `birthProfile` (내 출생정보) | SELF | 아니오 | |
| `completed.profile` · `completed.mirror` | SELF | 아니오 | Mirror는 Target을 계산에 쓰지 않는다 |
| `currentAnalysisMeta.mirrorViewedAt` | SELF | 아니오 | |
| `target` (4축 · 상대 MBTI · 상대 출생정보 · preferences) | TARGET | **예** | `createEmptyTargetProfile()` |
| `savedQuestions` | TARGET | **예** | 전부 상대 궁합 축/MBTI 비교에서 나온 질문 id |
| `completed.compatibility` | TARGET | **예** | |
| `currentAnalysisMeta.compatibilityViewedAt` | TARGET | **예** | |
| `funnelAnalysisId` | TARGET | **예** (재발급) | 새 상대 = 새 funnel 단위 |
| Premium Intent (`lym.premium-intent.v1`) | TARGET | **예** | feature 키만 쓰고 Target을 구분하지 않아 누수됐다 |
| Preview Unlock | TARGET | **예** | 분석 단위 상태 |

**NEW TARGET** = 새 분석 context만 초기화 · **HISTORY** = 사용자가 별도로 삭제하지 않는 한 유지.

History가 지워지는 경로는 **사용자가 명시적으로 고른 두 곳뿐**이다 —
Home '내 관찰 데이터 삭제'에서 '기록도 함께 삭제'를 체크한 경우, UT_MODE의 '다음 참가자를 위해 초기화'.
둘 다 화면이 `useHistory().clearAll()`을 직접 부른다.

#### 9.8.2 저장 구조

`RelationshipHistoryEntry`에 두 필드가 **optional로** 있다.

| 필드 | 성격 |
|---|---|
| `audience?` | 없으면 `couple`로 읽는다 — 기존 기록을 다시 쓰지 않는다 |
| `soloSnapshot?` | 축+단계 · headline · pairIds · sources · **observed(v1.35)** |

⚠️ **해석 문장을 통째로 저장하지 않는다.** 값과 규칙 id만 얼려두고 문장은 표시할 때
다시 만든다 — 문구를 고쳐도 과거가 깨지지 않고 저장소에 자유서술이 쌓이지 않는다.

⚠️ **자동 저장하지 않는다.** 결과를 볼 때마다 쌓이면 관찰 횟수가 부풀고, 그건 이
제품이 하지 않기로 한 것이다. 중복은 기존 `analysisId` 지문이 막는다.

#### 9.8.3 Observed 시간축 비교 (v1.35)

`soloSnapshot.observed`에 **활동 범주만** 남긴다 — `category` · `occurrences`(서로 다른
장면 수) · `strength`. 사진 원본·base64·AI 서술 원문은 저장하지 않고, 값은 전부 이미
계산돼 있던 `ObservedSignal`에서 그대로 옮긴다(새 판정 없음).

| 상태 | 의미 | 문장 |
|---|---|---|
| `NEW` | 지난 관찰에 없고 이번에 있음 | `야외 활동 장면이 이번 관찰에 새로 나타났어.` |
| `STABLE` | 둘 다 있음 (관찰 3회 미만) | `지난 관찰에도, 이번 관찰에도 카페 방문 장면이 있었어.` |
| `STABLE` | 둘 다 있음 (관찰 3회 이상) | `3번의 관찰에서 카페 방문 장면이 있었어.` |
| `ABSENT` | 지난 관찰에 있고 이번에 없음 | `지난 관찰에는 카페 방문 장면이 있었고, 이번 관찰에서는 보이지 않았어.` |
| `INSUFFICIENT` | **범주 단위가 아니라 리포트 단위** (`comparable === false`) | 섹션 자체가 없다 |

⚠️ **사진은 장면 observation이고 성격·취향 진단이 아니다.** 허용되는 말은
'있었다 / 없었다 / 새로 나타났다'까지다. `취향이 바뀌었다` · `요즘 카페를 싫어하게 됐다` ·
`독립적인 성향이 강해졌다`는 금지다. 화면 caption도 그 경계를 사용자에게 직접 말한다.

⚠️ **1회 → 1회 없음을 '사라졌다'로 과장하지 않는다.** `ABSENT` 문구는
"이번 관찰에서는 보이지 않았어"에서 멈춘다.

⚠️ **사진이 없어졌을 때 전부 ABSENT로 만들지 않는다.** 지금 사진 근거가 아예 없으면
그건 '장면이 사라졌다'가 아니라 '이번엔 비교할 사진이 없다'다. `undefined`(그때는 저장하지
않았음)와 `[]`(사진 근거가 없는 관찰이었음)도 구분한다.

⚠️ 사용자가 제외(`excluded`)한 관찰은 snapshot에 넣지 않는다 — 화면에서 지운 근거가
기록에 남아 다음 관찰과 비교되면 취소한 관찰이 되살아난다.

#### 9.8.4 반복 임계값 — 하나의 정책

`SOLO_REPEAT_MIN_OBSERVATIONS = 3`. **2시점은 반복의 증거가 아니다.**
Solo self signal · Observed 시간축 · Observed 세션 내 집계가 **모두 같은 기준**을 쓴다.

| 판정 | 관찰 2회 | 관찰 3회 이상 |
|---|---|---|
| Solo self signal | `repeatable: false` — 반복 어휘 없음 | `3번의 관찰에서 같은 방향이었어.` |
| Observed 시간축 | `지난 관찰에도, 이번 관찰에도 …` | `3번의 관찰에서 … 장면이 있었어.` |
| Observed 세션 내 (`describeSignal`) | `반복인지는 아직 조심스러워.` | `비슷한 장면이 반복해서 보였어.` |
| Cross-source ⑥ `ruleSummary` (v1.35 수정) | `사진에서 보인 활동이 같은 축을 …` | `사진 기록에서 반복해서 보인 활동이 …` |

⚠️ 값이 **바뀐** 경우(`CHANGE`)는 반복 횟수를 세지 않는다 — 그 자체가 '이번엔 달라졌다'다.

#### 9.8.5 Mixed History IA — 하나의 Archive

`/history`는 **탭을 나누지 않는다.** 하나의 timeline을 유지하고 항목마다 작은 metadata로만
구분한다: `나의 관찰` / `Relationship Mirror`.

변화 요약은 **주어가 다른 두 줄**로 둔다 — 커플 요약(`buildHistoryReport`)과 Solo
요약(`buildSoloHistoryReport`)을 한 문장으로 합치지 않는다.

**v1.35에서 고친 Mixed History 버그 3건**(실측):

| 증상 | 원인 | 수정 |
|---|---|---|
| Solo 관찰 2건만 있는데 변화 요약이 "아직 저장된 관찰이 없어" | 커플 요약을 audience 무관하게 렌더 | 커플 요약은 커플 기록이 있을 때만, Solo 요약은 별도 줄 |
| 변화 리포트 캡션이 비교하지 않은 기록의 날짜를 적음 | 화면이 `useHistory().latest`/`previous`(전체의 마지막 두 항목)로 캡션 생성 | `HistoryReport.compared`가 **비교에 참여한 두 기록**을 알려준다 (`useComparedHistoryEntries()`) |
| 커플 기록 상세가 "이때와 지금 사이에 크게 달라진 기준은 없었어"만 표시 | Mirror 판정이 없는 Solo 기록과 비교되어 전 축 INSUFFICIENT | 상세는 **같은 audience의 최신 기록**과 비교 |

#### 9.8.6 History Detail — Solo

| 섹션 | 출처 |
|---|---|
| 이때의 나의 기준 | `soloSnapshot.signals` → `SELF_VALUE_TEXT`로 문장 복원 (단계를 못 찾으면 declared 칩으로 대체) |
| 이때 함께 나타난 신호 | `soloSnapshot.pairIds` → `SELF_PAIR_RULES` |
| 이때 러비가 본 것 | `soloSnapshot.headline` |
| 당시 쓸 수 있던 정보 | `soloSnapshot.sources` + `observed` 범주 이름 |
| 이때의 참고 정보 | `evidenceCoverage` · 당시 MBTI (해석하지 않는다) |

⚠️ Solo 상세에 **빈 Mirror 판정 · 빈 관계 경험 · target placeholder는 0건**이다.
없는 것은 자리도 만들지 않는다.

#### 9.8.7 Snapshot Integrity

History Entry는 **snapshot만** 읽는다. Current Report는 live answers로 계산한다.
과거를 저장한 뒤 현재 답을 바꿔도 History detail은 바뀌지 않는다.

`analysisId`(= `analysisFingerprint`) 지문이 같은 분석의 중복 저장을 막고, 답이 실제로
달라지면 새 항목이 된다.

#### 9.8.8 근거 묶음 (Duplicate Evidence Aggregation)

같은 문장이 여러 줄 나오지 않게 묶고, `(관찰 N회)`로 개수 정보를 보존한다.
**묶는 기준은 날짜가 아니라 `문장 + variant`다.**

| 케이스 | 결과 |
|---|---|
| 같은 날짜 + 같은 값 + 같은 판정 | 1줄 + `(관찰 2회)` |
| 같은 날짜 + 같은 값 + **다른 판정** | **2줄** (v1.35 수정) |
| 같은 날짜 + 다른 값 | 2줄 |
| 다른 날짜 + 같은 값 | 2줄 (문장에 날짜가 들어 있다) |

⚠️ v1.35 실측으로 고쳤다 — History 근거 문장에 **Mirror 판정이 들어 있지 않아서**, 같은 날
저장된 두 기록이 같은 관계 경험 근거를 갖고 판정만 다를 때 실제로 반복된 적 없는 것이
"(관찰 2회)" 한 줄로 합쳐졌다. 판정이 갈리는 그룹에서만 라벨(`말한 기준보다 크게 반응함`
등)을 덧붙이므로, 판정이 하나뿐인 대부분의 근거 문장은 글자 하나도 달라지지 않는다.

#### 9.8.9 FREE / PREMIUM 경계

| CONTENT | FREE | PREMIUM | 같은가 | 새 연결 | 판정 |
|---|---|---|---|---|---|
| 과거 snapshot 자체 | ✅ `/history/[id]` | — | — | — | 무료가 전부 보여준다 |
| 축별 CHANGE (지난 관찰 → 지금) | ✅ WHAT CHANGED | — | — | — | **paywall 뒤로 숨기지 않는다** |
| 축별 STABLE / 유지 | ✅ | — | — | — | 무료 |
| 새로 나타난 signal (NEW) | ✅ | — | — | — | 무료 |
| Observed 장면 NEW/STABLE/ABSENT | ✅ PHOTO SCENES | — | — | — | 무료 |
| **여러 축이 함께 움직였다** | ❌ | ✅ ⑧ 연결 | 아니오 | **예** — source 2종(history×declared) | Premium |
| 커플 축별 변화 상세 | ✅ 변화 리포트 | ✅ 축별 상세 | 일부 | ④ 동기화율 연결 | 기존 v1.26 경계 유지 |

**Premium은 '무엇이 변했다'가 아니라 '그 변화가 다른 source와 어떻게 함께 나타나는가'다.**

#### 9.8.10 ⑧ Solo History ↔ Current Declared (v1.35)

Solo History를 **독립 source**로 쓰는 생성기. `history`(그때 얼려둔 값)와
`declared`(지금 답한 값)는 서로를 참조하지 않고 서로 다른 시점에 입력됐다.

| 조건 | 값 |
|---|---|
| audience | `solo`만 (커플의 시간축 비교는 ③이 담당) |
| 최소 기록 | Solo snapshot 1건 이상 + 현재 self signal |
| 게이트 | **두 축 이상이 함께 `CHANGE`** — 한 축만 달라진 것은 무료 문장의 반복이라 만들지 않는다 |
| sources | `history` + `declared` (2종) |
| evidenceRefs | 움직인 축마다 history ref + declared ref (4개 이상) |
| strength | `medium` — `strong`을 주지 않는다 |
| limitation | `과거 관찰과 지금이 같은 축을 가리킨다는 것까지야. 과거가 지금의 원인이라고는 말할 수 없어.` |

⚠️ **과거 → 현재 인과 금지.** `과거 경험 때문에 지금 바뀌었다`가 아니라
`지난 기록과 지금 답이 다른 방향을 가리키고 있어`까지다.

⚠️ **MBTI를 변화의 원인으로 끌어오지 않는다** — 이 조합에는 `mbti_lens`가 아예 없다.

⚠️ **기록이 한 건 있다는 이유만으로 Premium이 열리지 않는다.** 게이트는 v1.26과 같은
`hasDeepConnection`(서로 다른 source 2종 + 근거 2개 이상)이고, ⑧은 그 조건을 실제 움직임이
있을 때만 만족시킨다. Eligibility 영향: **사진도 MBTI도 없는 Solo 사용자**가 관찰을 2회
이상 쌓고 두 축 이상이 달라진 경우에만 새로 열린다.

`resolveHistory`가 v1.35부터 **Solo 기록의 근거도 푼다** — Solo에는 Mirror 판정이 없어
예전에는 항상 null이었다. 그때 얼려둔 단계 값을 문장으로 되돌린다
(`2026.08.01 기록에서는 혼자 있는 시간이 중요하다고 답했어`).

Solo Premium 진입 문구는 **특정 source를 약속하지 않는다**(v1.35 수정) — Solo가 이 리포트를
여는 경로는 ⑥ 사진 관찰 · ⑦ 성향 렌즈 · ⑧ 과거 관찰 셋이고, 사진이 없는 사용자에게
사진을 약속하면 결제 후 열리는 리포트에 약속한 것이 없다.

#### 9.8.11 Home Solo Retention (v1.35)

**새 대형 카드를 만들지 않는다.** 이미 있는 Solo 입구 행의 caption 한 줄만 실제 데이터로
바꾼다 — Home의 정보량과 시각적 균형을 그대로 둔다.

| Solo 기록 | 문장 |
|---|---|
| 0건 | `상대가 없어도 네가 관계를 어떻게 생각하는지는 관찰할 수 있어. 첫 관찰을 남겨볼까?` |
| 1건 (변화 없음) | `이때의 나를 기록해뒀어. 전에 남긴 기준과 비교해볼 수 있어.` |
| 2건 이상 + 실제 CHANGE | `지난 관찰과 달라진 기준이 N개 있어.` |
| 3건 이상 + `repeatable` STABLE | `몇 번의 관찰에서 계속 보인 기준이 있어.` |
| 그 외 | `생각이 조금 달라졌다면 다시 관찰해볼까?` |

⚠️ **매일 오게 만드는 앱이 아니다.** `오늘도 기록` · `연속 기록` · `streak` ·
`매일 체크` 어휘를 쓰지 않는다.

⚠️ Home의 `RELATIONSHIP HISTORY` 행과 `/history` 빈 화면은 **갈 수 없는 길을 알려주지
않는다**(v1.35 수정) — 상대도 관계 경험도 없는 사용자에게 Mirror는 만들어지지 않으므로
"Relationship Mirror를 저장하면"이 아니라 "첫 관찰을 남기면"이라고 말하고 `/first-contact`로 보낸다.

#### 9.8.12 Lovy Learning Experience

| 관찰 | 문장 |
|---|---|
| 1회 | `전에 기록한 너랑 지금을 나란히 놓아봤어.` |
| 2회 이상 | `몇 번의 관찰이 쌓였어. 여전히 다 아는 건 아니지만, 전보다는 조금 알 것 같아.` |

⚠️ `너를 이제 완전히 이해했어`는 금지다. 어떤 단계에서도 '이해했다'고 말하지 않는다.

#### 9.8.13 Analytics

**새 이벤트를 0개 만들었다** — 기존 이벤트의 property만 늘렸다.

| 이벤트 | 추가 property |
|---|---|
| `relationship_history_entry_created` | `audience`(solo/couple) · `history_bucket` |
| `relationship_history_view` | `history_bucket` · `audience_mix`(none/solo/couple/mixed) |
| `relationship_history_change_report_view` | `history_bucket` |

`historyCountBucket(n)` → `'0' | '1' | '2' | '3_plus'`.
경계가 제품 정책과 같다 — `2`부터 비교가 되고, `3_plus`부터 반복 어휘가 허용된다.

**Retention KPI** (기존 이벤트로 계산 가능):

| 지표 | 정의 |
|---|---|
| First Contact → History Save | `relationship_history_entry_created{audience=solo}` / `solo_report_view` |
| 두 번째 Solo 관찰 | `relationship_history_entry_created{audience=solo, history_bucket≥2}` / `{history_bucket=1}` |

**Privacy** — 보내지 않는 것: raw signal text · MBTI 원문 · 사진 범주 raw text · 자유서술 ·
생년월일 · history narrative. 보내는 것: `audience` · `history_bucket` · `change_count` ·
`source_count` 같은 categorical/count뿐이다. `sanitizeForExternal()`이 코드로 한 번 더 막는다.

#### 9.8.14 Delete / Legacy / Observed Missing

| 항목 | 동작 |
|---|---|
| 삭제 후 재계산 | 비교·반복 횟수·Home 요약·Premium 연결이 전부 `entries`에서 파생되므로 즉시 다시 계산된다. 삭제된 기록을 가리키는 근거 ref는 resolver에서 조용히 빠진다 |
| legacy (audience 없음) | `couple`로 읽는다. `soloSnapshot`이 없어도 runtime error 0건 |
| 사진 없는 Solo | History 정상 동작. Observed는 **optional source**이고 사진은 History의 입장권이 아니다 |

---

## 10. Analytics

`src/lib/analytics.ts`의 `trackEvent(name, properties)` 하나로 통일한다.

| 헬퍼 | dedup 기준 |
|---|---|
| `trackEvent` | 없음 |
| `trackOnce` | `sessionStorage` — **세션(탭)당 1회** |
| `trackOncePerAnalysis` | `funnelAnalysisId` — **분석 단위 1회** (새 상대마다 재발생) |

### 10.1 Primary KPI

```
relationship_mirror_entry_click / compatibility_result_view
= '궁합 결과를 본 사용자 중 Relationship Mirror에 진입한 비율'
```

| 항목 | 내용 |
|---|---|
| 분모 | `compatibility_result_view` — S21R **정상 결과와 E3 둘 다**에서 발생, `trackOnce` |
| 분자 | `relationship_mirror_entry_click` — S26 Teaser CTA **버튼에만** 있다 |
| Revisit 오염 | **없다.** Revisit은 `*_result_revisit` 별도 이벤트로만 기록되고 `getPrimaryKpi()`는 그 이름을 읽지 않는다. Revisit 경로는 Teaser를 거치지 않아 분자가 재발생할 방법이 없다 |

> **분모를 바꾼 이유** — 예전 분모는 `compatibility_complete`(정상 계산된 궁합만)였는데,
> E3(관측 정보 부족) 사용자는 결과 화면을 분명히 봤는데도 분모에서 빠졌다.

> **`score: 0`과 '계산 불가'를 구분한다** — E3에서 `score: 0`을 보내면 실제 0점(4축 모두
> 최대 차이)과 Analytics에서 구분되지 않는다. 지금은 `result_state`를 함께 보낸다.
>
> | 상태 | properties |
> |---|---|
> | 정상 계산 | `{ score: <number>, result_state: 'scored' }` |
> | E3 | `{ score: null, result_state: 'insufficient' }` |

> ⚠️ **알려진 한계 (미수정)** — `trackOnce`는 **브라우저 탭 단위**이지 분석 단위가 아니다.
> 한 탭에서 두 번째 분석을 해도 분모는 다시 발생하지 않지만, 분자는 Teaser CTA를 다시
> 누를 때마다 발생한다 — **반복 분석이 많은 세션일수록 비율이 부풀 수 있다.**
> User ID가 없어 '세션당 1회'가 지금 쓸 수 있는 최선의 근사치다.
> Primary KPI 정의를 analysis 단위로 바꾸면 과거 로그와 비교가 끊기므로 그대로 뒀다.

### 10.2 Funnel 단계별 주요 이벤트

| 단계 | 이벤트 | properties |
|---|---|---|
| 온보딩 | `onboarding_complete` | `skipped`, `last_step` |
| S05 | `relationship_status_select` | `status`, `supported` |
| S06 | `profile_building_start` | — |
| S07 | `photo_input_complete` | `count`, `uploads` |
| S08 | `photo_analysis_start` / `_complete` / `_failure` | `photo_count`, `duration_ms`, `reason` |
| S09 | `photo_observation_confirm` | `category`, `strength`, `ai_mode` |
| S09 | `observed_result_edit` · `observation_excluded` | `trait` |
| S09 | `observed_profile_complete` | `confirmed`, `corrected`, `excluded` |
| S13 | `declared_me_complete` · **`self_mbti_select`** | 5개 필드 + `mbti` |
| S16a | `relationship_adaptive_answer` | `axis`, `option` |
| S17 | `relationship_experience_complete` | `important_count`, `hardest`, `self_gap`, `has_note` |
| S18 | `profile_complete` · `profile_feedback_positive`/`_edit` | `confidence`, `observed_items` |
| S19 | `target_profile_complete` · **`target_mbti_select`** · `target_preference_add` | `relation`, `known_count` |
| S20 | `compatibility_complete` / `compatibility_low_confidence` | `score`, `compared` |
| **S21R** | **`compatibility_result_view`** | `score`, `result_state`, `compared` |
| S21R | `compatibility_analysis_result_view` | `score`, `result_state` (분석 단위 dedup) |
| S21R | `lovy_surprise_view` | `surprise_variant` (**opaque**) |
| S21R | `result_section_expand` · `result_anchor_navigation` | `section` |
| S21R | `both_mbti_available` · `mbti_conversation_question_view` | `self`/`target`, `count` |
| S21R | `approach_hint_view` / `_expand` / `_question_click` | `hint_count`, `kind` |
| S21R | `conversation_question_save` / `_share` · `saved_question_view` | `question`, `count` |
| **S26** | **`relationship_mirror_entry_click`** | `axis`, `score` |
| S26 | `relationship_mirror_teaser_view` · `relationship_mirror_postpone` | `axis`, `gap_count`, `reason` |
| S27R | `relationship_mirror_view` · **`relationship_mirror_complete`** | `gap_count`, `compared_axes`, `axis` |
| S27R | `mirror_feedback_positive` / `_edit` | `axis` |
| S27R | `relationship_history_entry_created` | `entry_id`, `focus_axis`, `gap_count`, `history_count` |
| S27R | `history_based_insight_view` | `axis`, `previous_occurrences` |
| F1/F1-a/F2 | `relationship_history_view` · `_entry_view` · `_change_report_view` · `_repeated_signal_view` · `_entry_delete` | `entry_count`, `entry_id`, `axis` |
| Lens | `mbti_lens_view` · `astrology_lens_view` · `saju_lens_view` | `mode`, `has_self`, `has_target`, `engine_available` |
| Lens | `birth_profile_edit` / `_complete` · `entertainment_lens_entry_click` / `_birth_missing` | `subject`, `lens`, `missing` |
| Premium | `premium_entry_view` · `premium_purchase_intent` · `premium_fake_door_reveal` · `premium_notify_intent` | `feature`, `source`, `price`, `variant` |
| Share | `share_card_open` · `share_card_action` | `kind`, `result` |
| AI | `ai_analysis_request` / `_success` / `_failure` | `task`, `duration_ms`, `mode`, `result_items`, `reason` |
| Revisit | `compatibility_result_revisit` · `mirror_result_revisit` · `profile_result_revisit` | `funnel_analysis_id`, `source` |
| 삭제 | `session_data_deleted`(사용자) / `session_reset`(dev 패널) | — |

### 10.3 Deprecated

| 이벤트 | 사유 | Migration |
|---|---|---|
| `compatibility_reason_view` | '왜 N인지 보기' 버튼이 Section Nav 앵커로 대체됨 | `result_anchor_navigation` |
| `good_signal_view` | S23 Route 소멸 | `result_section_expand { section:'good' }` |
| `friction_signal_view` | S24 Route 소멸 | `result_section_expand { section:'friction' }` |
| `result_reanalysis_start`/`_complete` | 처음부터 미사용 — 재분석이 별도 비동기 프로세스가 아니라 발생시킬 실제 액션이 없다 | — |

### 10.4 외부 전송 경계 — `sanitizeForExternal()`

GA4로 나가기 전 반드시 통과한다.

| 규칙 | 내용 |
|---|---|
| 금지 키 | `EXTERNAL_FORBIDDEN_KEYS`에 등록된 키는 **드롭** |
| 지문 차단 | `looksLikeFingerprint(value)`에 걸리면 드롭 — 답변 파생 해시는 입력 공간이 작아 **전수 대조로 되돌릴 수 있다** |
| 절대 미전송 | 사용자 자유서술 원문 · 사진 · 사진 분석 원문 · 러비 문구 원문 · raw interest 텍스트 · birth data |
| opaque만 전송 | `surprise_variant` · `funnel_analysis_id`(랜덤 UUID) |
| GA4 로딩 조건 | ① production 빌드 ② Consent 배너에서 **동의(granted)** — 둘 다 만족해야 `gtag.js`가 로드된다 |
| Key 없으면 | local store에만 남고 외부로 나가지 않는다. **Analytics 때문에 Core Funnel이 막히지 않는다** |

> **v1.36 §58 Audit — Declared 답변 원본을 막았다.**
>
> `declared_me_complete`가 `contact`/`alone`/`conflict`/`affection`/`hobby`를 **값 그대로**
> 싣고 있었다. 한 키에 담은 지문(`analysis_id`)은 v1.19에서 막았지만, 같은 정보를
> **필드 5개로 나눠 보내면** 키 이름 필터도 `looksLikeFingerprint`도 걸러내지 못한다 —
> GA4 이벤트는 client id에 묶이므로 결과는 같다: 사용자의 관계 응답 프로필이 외부에
> 복원된다. 다섯 키를 `EXTERNAL_FORBIDDEN_KEYS`에 추가했다.
>
> **local store에는 그대로 남는다** — UT 결과 회수(`utExport`)와 디버깅이 그 값을 쓰고,
> 기기 밖으로 나가지 않는다. 완료 여부는 **이벤트가 발생했다는 사실**이 말해준다.

### 10.5 새 이벤트를 만들지 않는 원칙

| 상황 | 판단 |
|---|---|
| Bottom Nav 클릭 | `post_analysis_nav_click`을 **추가하지 않았다** — 목적지가 이미 `*_result_revisit`/`relationship_history_view`를 발생시켜 재구성 가능 |
| AI latency/failure | 기존 `ai_analysis_*`가 이미 task·duration·mode·reason을 전부 갖고 있다 — 이름만 다른 이벤트를 만들면 같은 사실을 두 이름으로 중복 기록하게 된다 |
| 관계 옵션 2개 추가 (v1.22) | 기존 `target_profile_complete`의 `relation` **값만 확장** |
| Solo History Retention (v1.35) | **새 이벤트 0개.** `relationship_history_entry_created`에 `audience`·`history_bucket`, `relationship_history_view`에 `history_bucket`·`audience_mix`, `relationship_history_change_report_view`에 `history_bucket`만 더했다 — Retention KPI 두 개(First Contact → Save, Save → 두 번째 관찰)가 이 property로 계산된다 |
| Home Solo Retention 행 노출 (v1.35) | 이벤트를 **만들지 않았다** — 이 행은 기존 Solo 입구의 caption이고, 클릭은 이미 `solo_report_view`로 도달이 측정된다 |

**Low-cardinality bucket** — `historyCountBucket(n)` → `'0' | '1' | '2' | '3_plus'`.
정확한 개수를 외부로 보내지 않는다. 관찰을 많이 쌓은 사용자는 그 숫자 자체가 준식별자에
가까워지고, 알고 싶은 것은 애초에 '몇 개인가'가 아니라 **'없음 / 하나 / 비교 가능 /
반복 판정 가능'** 네 구간이다 — 경계가 제품 정책(`SOLO_REPEAT_MIN_OBSERVATIONS = 3`)과 같다.

---

## 11. Validation 규칙

`src/lib/validation.ts` — 화면은 이 함수들만 부른다.

| 함수 | 규칙 |
|---|---|
| `isDeclaredStepComplete(declared, step)` | 1:`contact` · 2:`conflict` · 3:`alone` · 4:`affection`+`hobby`. **MBTI는 검사하지 않는다** |
| `isDeclaredComplete(declared)` | 4스텝 전부 완료 |
| `firstIncompleteDeclaredStep(declared)` | 첫 미완료 스텝 (딥링크 방어) |
| `isPhotoSelectionValid(answers)` | `photos.length >= PHOTO_MIN_COUNT` |
| `isObservedReviewComplete(answers)` | **최소 1개** 항목에 `verdict !== null` 또는 `excluded` |
| `isExperienceComplete(experience)` | `skipped`면 통과. 아니면 `important.length > 0 && hardest !== null && selfGap !== null` |
| `targetKnownCount(target)` | 4축 중 `'x'`가 아닌 개수 |
| `isTargetComparable(target)` | `targetKnownCount >= TARGET_MIN_KNOWN(3)` |
| `canBuildProfile(answers)` | `isDeclaredComplete && isExperienceComplete` |
| `isLowData(answers)` | 관계 경험 얇음 · Declared 미완 중 하나라도 (E1). **사진 장수를 보지 않는다 (v1.37)** — `buildMirrorReport()`가 `(declared, experience)`만 받으므로, 판정에 쓰지 않는 데이터로 판정을 막지 않는다 |

### 11.1 Validation이 막지 않는 것

| 항목 | 이유 |
|---|---|
| 내 MBTI · 상대 MBTI | **Optional** — Supporting Lens |
| Birth Profile | **Optional** — Entertainment Lens. Main Funnel을 막지 않는다 |
| S16a Adaptive 답변 | **완전히 선택** |
| S17 자유서술(`note`) | 선택 (300자) |
| 지원하지 않는 관계 상태(S05) | 토스트만 띄우고 진행 |
| **사진 분석 실패** | **Core Funnel을 막지 않는다** — '질문으로 계속하기'를 항상 준다 |
| S19 관계 맥락(`relation`) | 선택 — 계산에 쓰이지 않는다 |

### 11.2 S19 단계별 안내

| `known_count` | 동작 |
|---|---|
| 0 | 진행 차단 — "아는 항목 하나라도 알려줘. 그거라도 있으면 비교해볼게." |
| 1~2 | 진행 허용 + "N개 더 알려주면 동기화율을 계산할 수 있어" |
| 3+ | 정상 |

---

## 12. 접근성

| 항목 | 규칙 |
|---|---|
| 선택 요소 | 실제 `<input type="radio">` / `<input type="checkbox">` 기반 또는 `role="radio"` + `aria-checked` |
| 터치 타깃 | 최소 44px (관계 칩 46px 실측) |
| 아이콘 전용 버튼 | `aria-label` 필수 |
| 토스트·로딩 | `aria-live="polite"` |
| 에러 | `role="alert"` |
| 색 단독 금지 | MATCH/GAP/CHANGE · 잘맞음/관찰필요 · 진행 단계 — **항상 텍스트를 함께 노출** |
| 포커스 | `:focus-visible` 2px outline — 색이 아니라 **형태로** 드러낸다 |
| Reduced Motion | `prefers-reduced-motion: reduce`에서 모든 CSS keyframe 정지. **꺼져도 각 단계의 최종 상태는 그대로 보인다**(정보가 사라지지 않는다) |
| 관찰 시퀀스 | 모션이 '무슨 일이 일어나는지'를 이해하는 **유일한 수단이 되면 안 된다** — 4단계를 목록으로 보여준다 |
| 승인된 줄바꿈 | `<br>`이 아니라 문장 단위 `span`(block) — 스크린리더가 한 문단을 억지로 끊어 읽지 않게 |
| 스크롤 복원 | **포커스를 옮기지 않는다.** `smooth` 미사용 |
| 앵커 이동 | 임시 `tabindex="-1"` 부여 후 포커스, blur 시 제거 |
| 한국어 줄바꿈 | `word-break: keep-all` + `text-wrap: pretty` |

### 12.1 시각 높이와 터치 영역의 분리 (v1.23)

`ResultSectionNav` 칩은 **보이는 높이 32px · 실제 터치 타깃 44px**이다.

```css
after:absolute after:inset-x-0 after:top-1/2 after:h-11 after:-translate-y-1/2
```

칩을 실제로 44px로 키우면 2줄 × 44px + gap으로 nav 한 덩어리가 100px을 넘어 LEVEL 1을
첫 viewport 밖으로 밀어낸다. 투명 `::after`로 hit area만 넓히면 시각 리듬을 유지하면서
접근성 목표(≥44px)를 만족한다. **가로는 칩 폭 그대로다** — 세로만 늘린다(gap 6px보다 넓히면
인접 칩과 겹쳐 오터치가 난다).

**실측(브라우저):** 시각 높이 32px × 5개, `::after` 높이 44px × 5개.

### 12.2 알려진 미달 항목

| 항목 | 현재 | 판단 |
|---|---|---|
| `PremiumEntryRow`의 `PRECISION REPORT`가 `h2` | 섹션이 아닌데 h2 | 이 컴포넌트는 Compatibility·Mirror·History·Lens 5개 화면 공용이라 heading level 변경의 파급이 크다 — **결정 대기** |

---

## 13. 참조표 (컴포넌트 ↔ 로직)

| 영역 | 화면 | 로직 | 데이터 |
|---|---|---|---|
| 동기화율 | `app/compatibility/page.tsx` | `logic/compatibility.ts` | `data/axes.ts` |
| Mirror | `app/mirror/page.tsx` | `logic/mirror.ts` | `data/axes.ts` |
| History (커플) | `app/history/**` | `logic/history.ts` | `lib/historyRepository.ts` |
| History (Solo · 시간축) | `app/first-contact/page.tsx`(WHAT CHANGED) · `app/history/[id]`(Solo 상세) | `logic/soloHistory.ts` | `lib/historyRepository.ts` |
| Profile | `app/profile/result/page.tsx` | `logic/profile.ts` | — |
| Observed 집계 | `app/profile/observed/page.tsx` | `logic/observedSignals.ts` · `logic/observed.ts` | `data/observations.ts`(샘플 전용) |
| MBTI Lens | `app/lens/mbti/page.tsx` · `components/compatibility/MbtiLensPanel.tsx`(4축 관찰표) · `components/lens/MbtiBridgeSection.tsx`(v1.24) | `logic/mbtiLens.ts` · `logic/mbtiBridge.ts`(v1.24) · `logic/mbtiPattern.ts`(v1.25) | `data/mbti.ts` |
| Astrology | `app/lens/astrology/page.tsx` | `services/astrologyService.ts` | `data/zodiac.ts` |
| 사주 | `app/lens/saju/page.tsx` | `services/sajuService.ts` (**미연결**) | — |
| Approach Hints | `components/compatibility/ApproachHintCard.tsx` | `logic/approachHints.ts` | `data/targetPreferences.ts` |
| Cross-source | `app/premium/page.tsx` | `logic/crossSourceInsights.ts` | — |
| Deep Report | `app/premium/page.tsx` · `premium-preview/[feature]` | `services/premiumService.ts` | `data/premium.ts` |
| AI 파이프라인 | `hooks/useAiNarrative.ts` | `services/ai/**` | `services/ai/promptTemplates.ts` |
| 러비 노트 | `components/lovy/LovyNote.tsx` · `components/compatibility/FirstSurprise.tsx` | — | `data/lovyNotes.ts` |
| 스크롤 복원 | `components/common/ScreenLayout.tsx` | `hooks/useScrollRestore.ts` | `lib/scrollRestore.ts` |
| 앵커 이동 | `components/common/ResultSectionNav.tsx` | `hooks/useAnchorScroll.ts` | `lib/routes.ts` |
| 카피 | 전 화면 | — | `data/copy.ts` · `data/labels.ts` |
| 세션 | 전 화면 | `state/SessionProvider.tsx` | `state/defaultAnswers.ts` |
| Analytics | 전 화면 | `lib/analytics.ts` | `lib/analyticsAdapter.ts` · `lib/analyticsConsent.ts` |
| UT | `components/ut/**` | `lib/deepReportUtStore.ts` · `lib/utExport.ts` | — |

### 13.1 개발 전용 (Production 미노출)

| 도구 | 게이트 |
|---|---|
| `PrototypePanel` (샘플 세션 · 화면 점프 · 답변 초기화) | `NODE_ENV !== 'production'` |
| `AiDebugPanel` | `NEXT_PUBLIC_AI_DEBUG=true` **&&** `NODE_ENV !== 'production'` |
| `/api/ai/contract-test` | Production **404** |
| `/api/dev/history-test` (History Fixture 실행기) | Production **404** |
| `/premium-preview/[feature]` | `NEXT_PUBLIC_PREMIUM_PREVIEW=true` |
| UT Rating UI | `NEXT_PUBLIC_UT_MODE=true` |
| `AI_MODE=mock` | Production에서 강제로 `demo`로 내려감 |

### 13.2 테스트

| 명령 | 내용 | 현재 |
|---|---|---|
| `npm run typecheck` | `tsc --noEmit` | PASS |
| `npm run lint` | `next lint` | PASS |
| `npm run build` | production build | PASS (55/55 static pages) |
| `npm run test:ai` | Contract Test — 픽스처를 **실제 파싱·Business Validation·Safety Scan에 그대로** 통과시킨다 (검증 로직을 테스트용으로 복제하지 않는다) | **PASS 143건** |
| `npm run test:observed` | Observed E2E — dev 서버에 실제 요청 | **PASS 10건** |
| `npm run test:history` | Logic Fixture — H0~H10 + Observed 시간축 + 근거 묶음 + ⑧ Premium 게이트 + `네가 말한 너` 문구 무결성(v1.36) + 샘플 근거·S07 게이트(v1.37) + **관찰 시퀀스 시간 예산**(v1.38). 개발 전용 라우트(`/api/dev/history-test`)가 **화면과 같은 판정 함수**를 호출하고, 스크립트는 fixture 조립·검증만 한다 | **PASS 100건** |
| `npm run test:ai:e2e` | Real Provider E2E — **`meta.mode`가 `real`이 아니면 무조건 SKIPPED로 보고한다** (검증 완료를 주장하지 않는다) | 별도 실행 |

> ⚠️ **네 스크립트 모두 dev 서버가 떠 있어야 한다** (v1.39 확인). `tests/*.mjs`는 서버를
> 직접 띄우지 않고 `LYM_BASE_URL ?? 'http://localhost:3000'`으로 **fetch만** 한다 —
> 서버가 없으면 `ECONNREFUSED`로 즉시 실패한다. 실행 순서는 터미널 A `npm run dev`,
> 터미널 B에서 테스트다. `test:ai`·`test:history`가 쓰는 두 Route는 Production 404이므로
> `LYM_BASE_URL`을 배포 URL로 돌릴 수 없다(§3.5).
>
> ⚠️ `npm run lint`는 통과해도 이 환경에서 exit code가 0이 아니다 —
> `@next/swc-win32-x64-msvc` DLL 초기화 실패 경고 때문이다. 출력에
> `✔ No ESLint warnings or errors`가 있으면 통과이고, exit code가 필요하면
> `npx eslint src tests --ext .ts,.tsx,.mjs`를 직접 돌린다.

---

## 14. 검증 상태

### 14.1 VERIFIED (실측 확인)

| 항목 |
|---|
| AI 결과를 통째로 조작해도 동기화율·신호 수·Mirror 판정이 **불변** |
| `AI_MODE=real` + 잘못된 키 → `CONFIG_ERROR` 반환. **가짜 결과를 만들지 않는다** |
| demo 모드 사진 분석 → `traits: 0` · `observedState: 'demo'` · 영화 문구 **0건** (Production 빌드) |
| 사진 지문 불일치 시 stale 안내로 전환, trait·예전 limitations 모두 숨김 |
| 스크롤 복원: 1500 → 하위 화면 → Back → **1507 복원**. `#good` 앵커 진입 시 복원이 앵커에 양보 |
| 관계 옵션 6개 `role="radio"` 단일 선택 · `aria-checked` 정상 · 터치 타깃 46px |
| 360px에서 History 제목 1줄(높이 32px, 폭 312px) · 승인 줄바꿈 4곳 `span.block` |
| 페이지 배경 `rgb(255,255,255)` (360/375/393/430/768/1280 전부) · 가로 오버플로 0 |
| 결과 섹션 칩 2줄 배치 (360/375/393px) |
| Rate Limit 12회/분 (13번째부터 `RATE_LIMIT`) |
| **New Target reset이 History를 지우지 않는다** — Solo 2건 + Couple 2건 mixed 상태에서 '새로운 사람과 궁합 보기' 전/후 localStorage 대조: 기록 4건·id·audience·`soloSnapshot.observed` 전부 동일. SELF(declared·MBTI·experience·photos·`completed.profile`·`mirrorViewedAt`) 유지, TARGET(4축·상대 MBTI·상대 출생정보·preferences·savedQuestions·`completed.compatibility`·`compatibilityViewedAt`·Premium Intent) 초기화, `funnelAnalysisId` 재발급 (v1.35) |
| **H0~H10 fixture 전수** — `npm run test:history` PASS (v1.35 기준 71건 · v1.36에서 82건으로 확장). 판정은 화면과 같은 함수를 통과한다 |
| **Solo 저장이 사진 원본·base64·AI 서술 원문을 남기지 않는다** — 실제 저장 버튼으로 만든 entry에 `data:image`·`base64`·관찰 원문 0건, `mirrorSnapshot.insights` 0건, `soloSnapshot.observed`는 `category`/`occurrences`/`strength`만 (v1.35) |
| **Mixed History 3건 수정 실측** — Solo만 2건일 때 변화 요약 오문구 · 변화 리포트 캡션이 비교하지 않은 기록의 날짜 · 커플 상세가 Solo 기록과 비교되어 "달라진 기준 없음" — 전부 재현 후 수정 확인 (v1.35) |
| **근거 묶음이 판정이 다른 관찰을 합치지 않는다** — 같은 날짜·같은 문장·다른 Mirror 판정이 `(관찰 2회)`로 합쳐지던 문제 재현 후 수정 확인 (v1.35) |
| **Observed 시간축** — NEW / STABLE(2회·3회) / ABSENT 문장, 사진 제거 시 ABSENT 대신 비교 불가, `observed` 미저장 legacy 기록은 조용히 제외 (v1.35) |
| **⑧ Solo History Premium 연결** — 한 축만 달라지면 미생성, 두 축 이상이면 source 2종·해석된 근거 10개·`hasDeepConnection` true. Deep Report 화면에서 '과거 관찰 + 내가 답한 기준' 2종과 인과 금지 한계 문장 렌더 확인 (v1.35) |
| **v1.35 반응형** — `/history` · `/history/[id]` · `/history/report` · `/first-contact` · `/home` · Premium Deep Report에서 360/375/393/430/768/1280 전부 가로 오버플로 0 · 뷰포트 초과 요소 0 |
| **v1.35 heading 순서** — `/history`(H1→H2→H3) · `/history/[id]`(H1→H2) · `/first-contact`(H1→H2) 역전 0건. Timeline은 `<ol>`, 항목 라벨은 `2026.09.01 · 나의 관찰` 형태로 색이 아닌 텍스트로 구분 |
| **v1.35 Analytics** — `relationship_history_entry_created{audience:'solo', history_bucket:'1'}` · `relationship_history_view{history_bucket, audience_mix}`. raw signal text·MBTI·자유서술·생년월일 0건 |
| **v1.35 커플 회귀** — 동기화율 65 · Mirror 차이 4개/일치 1개 · 변화 리포트 · 커플 History 상세 · ₩1,900 Fake Door 정상. 새 탭에서 console error 0건 |
| **v1.36 — `네가 말한 너` 문구가 실제 답을 따라간다.** 연락 5/5 사용자의 Premium Mirror 상세가 `연락은 별로 중요하지 않음 (5/5)` → `연락을 중요하게 생각함 (5/5)`. 답을 정반대로 바꾸면 5축 문구가 모두 달라짐을 test로 고정(`test:history` 8건) |
| **v1.36 — AI narrative가 판정과 같은 방향으로 말한다.** 같은 세션에서 `연락이 별로 중요하지 않다고 생각했지만…`(MATCH 축에 GAP 프레이밍) → `연락을 중요하게 생각한다고 했고, 연락 감소가 힘들었던 경험이 있어.` AI 환각이 아니라 프롬프트에 들어간 거짓 값이 원인이었음을 확인 |
| **v1.36 — Solo 퍼널 Dead End 해소.** `solo_none`으로 퍼널을 끝까지 진행: `past/none` → `/profile/result`(Solo 분기 노출) → `/first-contact` 도달. 수정 전에는 `/target`에서 `아는 항목 하나라도 알려줘`에 막혀 도달 불가(BottomNav 없음) |
| **v1.36 — 하드코딩된 사진 관찰 주장 제거.** 사진 0장·분석 실패 세션의 `/profile/declared/3`에서 `사진에서도` 문구 0건 |
| **v1.36 — Relationship Profile 요약 문법.** 경험 없음 6케이스 전부 `-고 모습이 보여` 패턴 0건 (`test:history`로 고정) |
| **v1.36 A11y — Production 44px 미달 0건.** 375px `/compatibility` 재실측: `<summary>` 17px×5 · `상대 정보 수정` 17px · 질문 탭 36px×2 · `ResultSectionNav` 32px×5 → 전부 해소 |
| **v1.36 Heading — 역전 0건.** `/mirror` 본문 `H1>H2>H3…` · `/mirror` 정보부족 상태 `H1` 단독 · `/history` `H1>H2>H2` · `/first-contact` `H1>H2×10` |
| **v1.36 — Solo First Contact에 Mirror 입구.** `solo_exp` 세션에서 `RELATIONSHIP MIRROR · 차이 3개 · 일치 1개` 렌더, 경험 없는 세션에서는 섹션 0건 |
| **v1.36 Paywall/Price** — 가격 `top:1186px`(1st viewport 밖) · 19px/600. Premium 첫 viewport에 `정보 4종 · 연결 2개` + core 연결 + 한계 모두 존재 |
| **v1.36 Content Integrity** — 금지 어휘 12종 전수: 근거 없는 사용자 노출 0건 |
| **v1.36 반응형** — `/first-contact`·`/compatibility`·`/mirror` × 360/375/430/768/1280 가로 오버플로 0 · 뷰포트 초과 요소 0 |
| **v1.37 — 사진 없이 퍼널을 끝까지 통과한다.** 사진 0장으로 `/profile/photos` → `사진 없이 질문부터 시작` → declared 1~4 → past 1~3 → `/profile/result` → `/mirror` 본문(`차이 1개 · 일치 2개`, 항목별 대조 3축, AI narrative 렌더) 도달. 수정 전에는 ① S07에서 진행 자체가 불가 ② 설령 도달해도 `isLowData`가 `/mirror`를 빈 상태로 막았다 |
| **v1.37 — 샘플 타일만으로는 분석이 시작되지 않는다.** 샘플 6장 선택 후 `러비에게 보여주기 · 0장`을 누르면 S07에서 `샘플 타일은 색 타일이라 러비가 볼 수 있는 게 없어…`로 멈춘다. 수정 전에는 `canAnalyze: true / usable: 0`으로 통과시켜 6.1초 연출 뒤 `NO_USABLE_IMAGE` 실패 화면으로 보냈다(fixture로 재현·고정) |
| **v1.37 — 샘플 세션 근거가 샘플 타일과 맞는다.** `/profile/observed`(샘플 세션) 근거 4문장 전수: `영화관 좌석이 찍힌 사진이 1장 있었어…` · `집 책상·카페처럼 혼자 있는 장면이 2장 있었어.` · `사람이 함께 찍힌 사진은 1장이고, 거기 2명이 있었어…` · `산책·등산처럼 밖에서 찍은 사진이 2장 있었어.` 금지 어휘(`반복적으로`·`여러 장`·`절반 이상`·`많은 편`) 0건 |
| **v1.37 — 빈 OBSERVED 레이어가 사라진다.** 사진 0장 `/profile/result`에서 `OBSERVED ME` 0건 · 요약 라벨 `두 관찰을 합친 결과`. 샘플 세션(사진 6장)은 3레이어 · `세 관찰을 합친 결과` 그대로 |
| **v1.37 커플 회귀** — 같은 샘플 세션의 동기화율이 수정 전후 **55로 동일**(git stash 대조 실측). 관찰한 신호 4개 · 비교한 신호 4개도 동일 |
| **v1.37 회귀 방지** — `test:history` 82 → **93건**(+11). 새 SO 절은 v1.36 데이터·규칙으로 되돌리면 실제로 실패한다(근거 5건 · 게이트 2건 재현 확인) |
| **v1.38 — 궁합 관찰 6.1초를 실측으로 재현하고 줄였다.** 같은 세션 5회 반복: 수정 전 median **6,158ms**(min 6,153 · max 6,165). Release Gate 재측정 — 첫 관찰 median **2,633ms**(min 2,612 · max 3,045, 최대값은 dev 첫 라우트 컴파일) · 재관찰 median **1,443ms**(1,415~1,448) · Reduced Motion median **776ms**(760~829, 수정 전 1,322ms). 4단계 문구·토큰·caveat은 그대로다 |
| **v1.38 — 단계가 쌓이는지 프레임 샘플링으로 확인.** 200ms 간격 캡처에서 `01/04 OBSERVING` → `02/04`(2줄) → `03/04`(3줄) → `04/04`(4줄 + caveat) → `REPORT READY`로 누적. 마지막 줄만 강조·앞 줄은 낮춤. 375px 오버플로 0 |
| **v1.38 — 계산이 늦어도 0을 결과로 보내지 않는다.** 이동 조건이 `연출 완료 && 계산 완료`로 바뀌어, E3(비교 1/4) 세션에서도 `?` 점수 + `관측 정보 부족 · 입력 1/4`로 정상 도달. Analytics는 `compatibility_complete {score:55, compared:4}` 실제 값 |
| **v1.38 — MBTI Lens 도달 거리 실측(375px).** Bridge 1,480 → **1,056px**(2.37 → 1.69 화면) · Core CTA 2,438 → **2,014px**(3.91 → 3.23 화면) · 전체 2,903 → **2,626px** · 카드 7 → **6**. 첫 viewport에 조합·한 문장·4축 같음/다름·패턴 이름이 함께 들어간다 |
| **v1.38 — Back/스크롤 복원(M12) 실측.** `/compatibility`에서 1,200px까지 스크롤 → MBTI 렌즈 진입 → 4축 펼침 → 브라우저 Back: **1,208px 복원**(초기화 아님, 오차 8px) · BottomNavigation 5항목 · 동기화율 55 유지. MBTI 펼침 상태는 설계상 복원하지 않는다 |
| **v1.38 — 관찰 필드 연출이 한 단계 안에서 끝난다.** 110ms 프레임 샘플링: CONNECT 선이 1,168ms에 `strokeDashoffset: 0px` 완주 · 이후 재애니메이션 0 · REPORT 진입 후 값 변화 0 · `opacity: 0`으로 남는 요소 0. 누적 로그가 자라며 관찰 필드가 328 → 261px 상승해 **CLS 0.0321**(good 임계 0.1 미만, 이 화면에는 조작 대상이 없다) |
| **v1.38 — 4축 토글 접근성.** 44px · `aria-expanded` false→true · `aria-controls`가 가리키는 패널은 펼쳤을 때만 존재 · `type="button"` · 전역 `:focus-visible`(2px brand outline) 규칙 적용 |
| **v1.38 — MBTI 상태 전수(M1~M11).** 4같음/3:1/2:2/1:3/4다름 전부 01·02·03·04 렌더 · 토글 44px · Core CTA 존재 · heading `1>2>2>3>2…` 역전 0 · 오버플로 0. 내 것만(1.71 화면) · 둘 다 없음(1.00 화면)은 비교 섹션 미생성. Bridge UNKNOWN(`아직 비교 못 함`)과 관계 답변 부족(`아직 나란히 놓을 답변이 부족해`)도 새 순서에서 정상 |
| **v1.38 반응형** — `/lens/mbti` × 360/375/393/430/768/1280 가로 오버플로 0 · 컨테이너 밖 요소 0. 회귀 sweep(`/mirror`·`/home`·`/history`·`/lens`·`/profile/result`·`/profile/photos`·`/premium`) 전부 오버플로 0 |
| **v1.38 회귀** — 동기화율 **55**·관찰한 신호 4개·비교한 신호 4개·잘 맞는 신호·확인이 필요한 신호·YOUR SIGNAL·`잠깐.` 전부 불변. ₩1,900 Fake Door 유지. v1.37 사진 퍼널(`샘플 타일 · 화면 확인용`) 유지 |
| **v1.38 Analytics** — 새 이벤트 **0**. 4축 펼침은 기존 `result_section_expand {section:'mbti_axes'}`. 로그 전수에서 MBTI 유형 원문 유출 **0건**(v1.24 방어선 유지) |
| **v1.38 Bundle** — dependency 추가 0(`package.json` 미변경). `/lens/mbti` 5.53 → 5.67 kB · `/compatibility/analyzing` 1.61 → 1.63 kB · shared 103 kB 동일 |
| Contract Test 143건 · Observed E2E 10건 · Logic Fixture 100건 (v1.39 재실측) |
| **v1.39 — Production이 실제로 무슨 모드인지 실측했다.** `https://loveyouloveme.vercel.app/api/ai/observed-profile`에 ① 사진 0장 ② 합성 PNG 1장으로 요청 → 두 번 모두 `meta.mode: "demo"` · `observedState: "demo"` · `traits: []` · `usableImageCount: 0`. `AI_MODE=real`인데 Key만 없으면 `CONFIG_ERROR`가 나야 하므로 **Production Runtime에서 `AI_MODE`가 `real`로 해석되지 않는다**(Key 유무는 이 응답으로 판별 불가) |
| **v1.39 — Production demo가 관찰을 만들어내지 않는다.** 위 ②(실제 이미지 바이트를 보낸 업로드 경로)에서도 trait **0개** · 영화/생활패턴 문구 **0건** · limitation은 `실제 사진 내용을 분석하지 않았어. 그래서 관찰 결과도 만들지 않았어.` — v1.22가 고친 결함이 **Production에서** 재발하지 않았음을 처음으로 직접 확인 |
| **v1.39 — Production 배포가 v1.38 코드를 포함한다.** v1.38에서만 도입된 문자열 `아직 보고 있어. 조금만 더.`(`git log -S` 결과 `3509692` 단독)를 Production 청크 `/_next/static/chunks/6436-1faaec3abc1a40f8.js`에서 확인. HEAD(`e5f9746`)는 문서 전용 커밋이므로 **배포는 기능상 최신** |
| **v1.39 — S07 힌트와 서버 진실이 어긋나지 않는다.** Production `/profile/photos`가 `지금은 데모 모드라 사진을 전송하지 않아`(demo 문구)를 렌더 → `NEXT_PUBLIC_AI_MODE`도 real이 아니며, 서버 상태와 **일치**한다. real 서버 + demo 힌트(또는 그 반대)로 어긋난 상태는 아니다 |
| **v1.39 — Real Provider E2E를 다시 돌렸다(이 실행 기준).** 2026-09-07 로컬 `AI_MODE=real`에서 `npm run test:ai:e2e` **PASS 6 · SKIPPED 0 · FAIL 0** — 6 Task 전부 `mode: real` · `compatibility-narrative`의 `score-leak: false` · `deep-report-narrative`의 `evidence-subset-ok: true` · `relationship-insight` state-override 0. v1.17(2026-09-04) 이후 처음 재실행이다. **상시 CI는 아니다** |
| **v1.39 — real 경로가 읽을 수 없는 입력에 관찰을 지어내지 않는다.** 로컬 real에서 캐릭터 일러스트 1장을 업로드 경로로 보냄 → `meta.mode: real` · `usableImageCount: 0` · **trait 0개** · 고정 영화 문구 0건. (실제 음식/장소 사진의 **내용 인식 품질**은 여전히 §14.2 NOT VERIFIED — 저장소에 사진 asset이 없다) |
| **v1.39 — Production 샘플 세션 근거가 v1.37 교정본과 같다.** `/profile/intro` → `샘플 답변으로 결과부터 볼게` → `/profile/observed`: `DEMO AI` 배지 · `이건 화면 확인용 샘플 세션이야. 실제 사진 내용을 분석한 결과가 아니야.` · 근거 4문장(`영화관 좌석이 찍힌 사진이 1장 있었어…` · `집 책상·카페처럼 혼자 있는 장면이 2장 있었어.` · `사람이 함께 찍힌 사진은 1장이고, 거기 2명이 있었어…` · `산책·등산처럼 밖에서 찍은 사진이 2장 있었어.`) · 금지 어휘(`반복적으로`·`절반 이상`) **0건** |
| **(v1.23)** Compatibility Result 4단계 재편 후 실측 — 3,755px / 6.1화면 / `rounded-card` 7개 (v1.22: 4,307px / 6.9화면 / 12개) |
| **(v1.23)** 첫 viewport(620px) 안에 동기화율 · 핵심 한 문장 · 러비의 의문 · YOUR SIGNAL 시작 전부 포함 |
| **(v1.23)** `ResultSectionNav` 시각 32px / 터치 44px 분리 |
| **(v1.23)** heading 계층 H1→H2→H3→H4 정상 |
| **(v1.23)** Legacy Redirect 전수 — `/compatibility/why`→`#why`(scrollTop 360) · `/good`→`#good`(785) · `/questions`→`#questions`(2826), 각 섹션이 viewport 상단 12px |
| **(v1.23)** friction 0 케이스 — 핵심 한 문장이 good 상위 2개로, Premium Entry 미표시 |
| **(v1.23)** Low Data(E3) — 핵심 한 문장·FIRST SURPRISE·YOUR SIGNAL·LOVY OBSERVATION **전부 미생성** |
| **(v1.23)** 360/375/393/430/768/1280 가로 오버플로 0 |

### 14.2 NOT VERIFIED

| 항목 | 사유 |
|---|---|
| 실제 음식/영화 사진으로 real Vision 결과 육안 확인 | 검증 환경에서 실제 파일 업로드 불가 |
| **Production 실제 사진 분석 동작** | **v1.39에서 재실측 — 여전히 `demo`다.** 2026-09-07 기준 Production 응답이 `meta.mode: 'demo'`이므로, 배포 환경에서 실제 Vision 관찰이 만들어진 적이 없다. 코드 경로는 정상이고(로컬 real 실측 PASS) 남은 것은 **Vercel Production 환경변수 + Redeploy**다 — Claude가 Dashboard에 접근할 수 없으므로 **USER ACTION REQUIRED**(절차: `기능명세서.md` §36.9). 이 항목은 사용자가 설정한 뒤 같은 probe를 다시 돌려야 VERIFIED가 된다 |
| Production에서 `meta.mode: 'real'` · `AI OBSERVATION` 배지 | 위 항목이 해결되기 전에는 확인 자체가 불가능하다. **'검증됐다'고 쓰지 않는다** |
| Production 서버에 `AI_API_KEY`가 있는지 | `AI_MODE`가 real이 아닌 동안에는 공개 응답으로 구분할 수 없다(둘 다 `demo`). Dashboard 확인 필요 |
| 궁합 관찰 단계 전환 타이밍 육안 확인 | 검증 브라우저가 `prefers-reduced-motion`을 강제하고 백그라운드 타이머를 clamp |
| Real Provider E2E 상시 검증 | CI에 연결돼 있지 않다. Key 만료·Rate Limit·모델 변경 시 재검증 필요 |
| Rate Limit 다중 인스턴스 정확도 | 인스턴스 메모리 기반. 공유 저장소 미연결 |

### 14.3 NOT VALIDATED (사용자 가치 미검증)

| 항목 |
|---|
| 4단계 읽기 구조가 실제로 '5초 안에 핵심이 읽힌다'로 이어지는가 |
| 핵심 한 문장이 점수보다 먼저 읽히는가 |
| compact 위계가 '중요한 것부터 본다'로 이어지는가 (정보를 놓쳤다고 느끼지 않는가) |
| Lens를 editorial index로 낮춘 뒤 Lens 진입률이 어떻게 변하는가 |
| YOUR SIGNAL이 실제로 '이 서비스는 다르다'는 인식을 만드는가 |
| **궁합 관찰 2.5초**가 적정한 체류 시간인가 — 너무 짧아 가벼워 보이는지, 여전히 기다린다고 느끼는지 (v1.38에서 6.1초 → 2.5초, 실사용자 미검증) |
| 4축을 접었을 때 무료 가치가 줄었다고 느끼는가 (v1.38 Progressive Disclosure, 실사용자 미검증) |
| 흰 배경이 선호되는가 |
| **실제 결제 전환율 · ₩1,900의 적정성** — PG 미연결 |
| Deep Report의 실제 지불 가치(WTP) · 반복 열람 |
| Premium 전환 연출이 완독률/만족도에 주는 영향 |
| 실사용자 AI 품질 평가 |

### 14.4 미해결 항목

| 항목 | 내용 |
|---|---|
| `ResultSectionNav` 칩 높이 | 32px (44px 미달, §12.1) |
| 사주 엔진 | 미연결. `SAJU_ENGINE_READY=false` |
| Full Natal Chart | 미구현. `calculateNatalChart()`는 계약만 존재 |
| 실제 PG 결제 | 미구현 (Fake Door 유지) |
| 공유 Rate Limit 저장소 | `RateLimitStore` 인터페이스 경계만 준비됨 |
| 전체 카피 Audit 제안 | 12건 제안 상태, 승인 대기 |
| dating / married / ended | 미지원. S05에서 `준비 중` 라벨 + 토스트 후 같은 흐름으로 진행한다 |

---

> **문서 끝.** 변경 이력·판단 근거는 [`기능명세서.md`](./기능명세서.md) 부록 A,
> 기획 의도·성공지표는 [`럽유럽미_기획서_v1.md`](./럽유럽미_기획서_v1.md)를 참고한다.
