import type { ZodiacModality } from '@/data/zodiac';
import type { PremiumLensKind, PremiumLensTheme } from '@/types';

/**
 * Premium 관계 렌즈 — 문구 (v1.46 PremiumLens · §4 · §5 · §27 · §28 · §31)
 *
 * ══ 이 파일이 지키는 품질 기준 ═════════════════════════════════════════════
 *
 * §27이 실패로 규정한 문장들이 여기 **하나도 없어야 한다.**
 *
 * ```
 * ❌ 서로 대화를 많이 해보는 게 좋아 · 서로 이해하는 게 중요해
 * ❌ 차이를 존중하면 좋아 · 솔직한 대화가 필요해
 * ```
 *
 * 그래서 이 파일의 모든 '확인' 문장은 **무엇을 · 어떤 상황에서 · 어떻게** 셋을
 * 갖는다. `tests/run-lens-fixtures.mjs`의 VALUE-05가 위 네 문장을 금칙어로 스캔한다.
 *
 * ══ §28 — 세 렌즈가 같은 말을 하면 실패다 ══════════════════════════════════
 *
 * 렌즈마다 **자기만의 질문**이 있어야 한다. 그래서 세 렌즈의 섹션 제목이 서로
 * 다르고(VALUE-01/VALUE-02가 검사한다), 각 렌즈가 보는 대상 자체를 다르게 뒀다:
 *
 * ```
 * MBTI   정보를 받아들이고 결정하는 방식이 어떻게 다른가   ← 선호 지표
 * 사주    전통 프레임에서 관계의 힘이 어느 방향으로 흐르나  ← 일간 오행
 * 별자리  관계에 대한 기대와 표현을 어떤 상징으로 보나      ← 태양궁 원소·양태
 * ```
 */

export const LENS_LABEL: Record<PremiumLensKind, string> = {
  mbti: 'MBTI 관계 렌즈',
  saju: '사주 관계 렌즈',
  zodiac: '별자리 관계 렌즈',
};

/** Home 3버튼 등 좁은 자리에서 쓰는 짧은 이름 */
export const LENS_SHORT_LABEL: Record<PremiumLensKind, string> = {
  mbti: 'MBTI 관계 분석',
  saju: '사주 관계 분석',
  zodiac: '별자리 관계 분석',
};

/** §37 — Premium 결과 안에서 이 렌즈로 바로 이동하는 anchor */
export const LENS_ANCHOR: Record<PremiumLensKind, string> = {
  mbti: 'lens-mbti',
  saju: 'lens-saju',
  zodiac: 'lens-zodiac',
};

/**
 * §31 — 짧게 한 줄. **경고문처럼 만들지 않는다.**
 * 카드마다 3줄짜리 면책 문구가 붙으면 읽는 사람은 결과보다 경고를 먼저 본다.
 */
export const LENS_DISCLAIMER: Record<PremiumLensKind, string> = {
  mbti: 'MBTI는 관계 성공을 예측하는 공식이 아니야.',
  saju: '사주는 관계를 돌아보는 전통적 해석 렌즈로 봐줘.',
  zodiac: '별자리는 관계를 생각해보는 상징적 관점이야.',
};

/* ─────────────────────────────────────────────────── Cross-Lens (§18~§20) */

export const LENS_THEME_LABEL: Record<PremiumLensTheme, string> = {
  pace: '움직이는 속도',
  alone_time: '혼자 정리하는 시간',
  expression: '마음을 표현하는 방식',
  planning: '계획과 즉흥 사이',
  closeness: '가까움과 거리',
  standard: '기준을 누가 정하는가',
};

/**
 * 테마별 **실제 관계에서 확인할 질문** (§19 C).
 *
 * ⚠️ 렌즈 일반론이 아니라 그 테마를 **행동으로** 확인하는 질문이다. 이 목록이
 * Cross-Lens의 최종 가치이므로, 전부 '무엇을 언제 어떻게'를 포함한다.
 */
export const LENS_THEME_QUESTION: Record<PremiumLensTheme, string> = {
  pace: '무언가를 정해야 할 때, 둘 중 누가 먼저 말을 꺼내고 누가 며칠 두고 보는 편인지 최근 일로 맞춰봐.',
  alone_time:
    '연락이 없는 하루가 지났을 때, 서로 그걸 혼자 있는 시간으로 읽는지 멀어진 걸로 읽는지 물어봐.',
  expression:
    '상대가 한 어떤 행동을 애정 표현으로 알아들었는지, 최근 일주일에서 하나씩 골라 서로 말해봐.',
  planning:
    '다음 약속을 언제까지 정해두면 편한지, 당일에 정하면 불편한지 각자 기준을 숫자로 맞춰봐.',
  closeness:
    '일주일에 며칠 정도 만나는 게 서로에게 편한지, 지금보다 많고 적음을 각각 말해봐.',
  standard:
    '최근에 둘이 정한 것 하나를 골라, 그걸 누가 먼저 제안했고 상대는 언제 동의했는지 되짚어봐.',
};

export const CROSS_LENS_COPY = {
  title: '세 렌즈를 겹쳐보면',
  /** 2개일 때는 '세'가 거짓말이 된다 — 호출부가 개수로 고른다 */
  titleTwo: '두 렌즈를 겹쳐보면',
  lovyIntro: '러비가 서로 다른 관점을 같이 놓아봤어.',
  repeatedLabel: '반복해서 나온 테마',
  differenceLabel: '렌즈마다 다르게 말하는 부분',
  questionLabel: '실제 관계에서 확인할 것',
  /**
   * ⚠️ §20 — **이 문장은 데이터에 필수 필드로 들어간다.** 화면 문구로만 두면
   * 다음 화면이 빼먹을 수 있고, 그러면 '3개 근거가 일치했다'로 읽힌다.
   */
  note: '세 관점은 서로 독립적인 증거가 아니야. 다른 해석 프레임에서 비슷한 테마가 반복됐다는 뜻이고, 실제로 그런지는 네 경험으로 확인해야 해.',
  /** 반복 테마가 하나도 없을 때 — 그것도 결과다 */
  noRepeatNote: '이번에는 렌즈끼리 겹치는 테마가 없었어. 세 관점이 서로 다른 곳을 보고 있다는 뜻이야.',
} as const;

/* ─────────────────────────────────────────── Bundle 문구 (§32~§35 · §58) */

/**
 * Home 하단 Premium Bundle 영역 (§32~§35).
 *
 * ⚠️ **가격은 한 번만 나온다**(§35). `price`는 여기 하드코딩하지 않고 호출부가
 * `resolvePrice()`로 넣는다 — 렌즈별로 가격이 붙는 것처럼 보이면 안 되므로,
 * 3개 버튼에는 가격 문자열이 들어갈 자리 자체가 없다.
 */
export const PREMIUM_BUNDLE_COPY = {
  eyebrow: 'PREMIUM BUNDLE',
  title: '정밀 관찰 리포트 + 관계 렌즈 3종',
  description:
    '내 답과 관계 기록을 연결해 보고, MBTI · 사주 · 별자리로도 다른 관점에서 볼 수 있어.',
  /** 가격 뒤에 붙는다 — 지금은 실제 결제가 없다는 사실을 같은 줄에서 말한다 */
  priceSuffix: '현재 테스트 무료',
  lensListLabel: '한 번 열면 아래 전부 볼 수 있어',
  allCta: '전체 상세 분석 보기',
  /** 잠긴 상태에서 렌즈 버튼을 눌렀을 때 Paywall이 강조할 문장 */
  lockedNote: '따로 파는 게 아니라 정밀 관찰 리포트와 같이 열려.',
  unlockedCta: '보기',
  lockedCta: '열기',
} as const;

/**
 * Paywall에서 '이번에 함께 열리는 렌즈' 목록 (§34).
 *
 * ⚠️ **항목 문자열을 여기 두지 않는다.** Paywall이 보여주는 목록은 실제
 * `report.lensBundle.lenses`의 상태다 — 그래서 **가짜 teaser가 구조적으로
 * 불가능하다.** 사주 양력 생년월일이 없는 사용자에게 사주 렌즈를 약속하지
 * 않고, 대신 무엇이 있으면 보이는지를 그 자리에서 말한다.
 * v1.45 `chapterListLabel`이 세운 규칙 그대로다.
 */
export const LENS_PAYWALL_COPY = {
  label: '같이 열리는 관계 렌즈',
  modePair: '둘이 함께',
  modeSelf: '나만',
  modeUnavailable: '이번엔 없음',
  /** ⚠️ 개수를 하드코딩하지 않는다 — 호출부가 실제 `availableCount`를 넣는다 */
  note: '이 목록도 네 입력에서 실제로 만들어지는 것만이야. 따로 결제하는 게 아니라 정밀 관찰 리포트와 한 번에 열려.',
} as const;

/** Premium 결과 화면의 Lens 섹션 헤더 (§36) */
export const LENS_SECTION_COPY = {
  eyebrow: 'RELATIONSHIP LENS',
  title: '관계 렌즈',
  intro:
    '여기부터는 같은 관계를 다른 프레임으로 다시 본 거야. 위의 정밀 관찰 리포트가 네 답과 기록을 연결한 결과라면, 아래는 참고로 겹쳐보는 관점이야.',
  basisLabel: '왜 이렇게 봤어?',
  checkpointLabel: '러비의 체크포인트',
  limitationLabel: '이 렌즈가 못 하는 것',
  unavailableLabel: '지금은 볼 수 없어',
} as const;

/* ══════════════════════════════════════════════════════ MBTI (§7~§9) */

/**
 * §8 MBTI-02 — **관계 리듬**(E/I × J/P).
 *
 * ⚠️ `I = 연락 싫어함` 같은 치환을 하지 않는다(§8 명시). 아래 문장 어디에도
 * 연락 빈도가 없다 — 회복 방식과 일정 감각까지만 말한다.
 */
export const MBTI_ENERGY_PAIR: Record<'both_i' | 'both_e' | 'differ', string> = {
  both_i:
    '둘 다 혼자 있는 시간으로 회복하는 쪽으로 분류됐어. 이런 조합에서는 각자 충전하는 시간이 자연스럽게 확보되는 대신, 같이 있는 시간을 늘리자고 먼저 말하는 사람이 없을 수 있다고 이야기돼.',
  both_e:
    '둘 다 함께 활동하면서 에너지를 얻는 쪽으로 분류됐어. 함께하는 시간을 만드는 건 쉬운 대신, 각자 혼자 있고 싶어지는 순간을 말하기 어려워질 수 있다고 이야기돼.',
  differ:
    '한 사람은 혼자 있는 시간으로, 다른 사람은 함께 활동하면서 회복하는 쪽으로 분류됐어. 같은 하루를 보내고 나서 필요한 게 서로 반대일 수 있다는 뜻이야.',
};

export const MBTI_LIFESTYLE_PAIR: Record<'both_j' | 'both_p' | 'differ', string> = {
  both_j:
    '일정은 둘 다 미리 정해두는 쪽이야. 약속을 잡는 건 수월한 대신, 정해둔 게 어긋났을 때 누가 다시 조정할지는 정해져 있지 않을 수 있어.',
  both_p:
    '일정은 둘 다 그때 상황을 보고 정하는 쪽이야. 여유는 있지만, 정해야 끝나는 일이 계속 뒤로 밀릴 수 있어.',
  differ:
    '한 사람은 미리 정해두는 쪽, 다른 사람은 그때 정하는 쪽이야. 약속을 며칠 전에 확정하고 싶은지가 서로 다를 수 있어.',
};

/** §8 MBTI-03 — **대화와 이해 방식**(S/N × T/F). 리듬 섹션과 층위가 겹치지 않는다 */
export const MBTI_INFORMATION_PAIR: Record<'both_s' | 'both_n' | 'differ', string> = {
  both_s:
    '둘 다 눈에 보이는 구체적인 사실을 먼저 보는 쪽이야. 무슨 일이 있었는지는 빠르게 맞춰지는 대신, 그게 서로에게 무슨 의미였는지는 말로 나오지 않을 수 있어.',
  both_n:
    '둘 다 의미와 가능성을 먼저 보는 쪽이야. 이야기가 깊어지기는 쉬운 대신, 실제로 무슨 일이 있었는지가 흐려질 수 있어.',
  differ:
    '한 사람은 무슨 일이 있었는지를, 다른 사람은 그게 무슨 뜻인지를 먼저 말할 수 있어. 같은 사건을 이야기하는데 서로 다른 층에서 말하고 있는 상태가 될 수 있다는 뜻이야.',
};

export const MBTI_DECISION_PAIR: Record<'both_t' | 'both_f' | 'differ', string> = {
  both_t:
    '둘 다 결정할 때 해결 논리를 먼저 보는 쪽이야. 결론은 빨리 나는 대신, 각자 어떤 기분이었는지는 확인되기 전에 대화가 끝날 수 있어.',
  both_f:
    '둘 다 결정할 때 감정적 맥락을 먼저 보는 쪽이야. 서로의 상태는 잘 챙겨지는 대신, 정해야 할 것이 정해지지 않은 채 넘어갈 수 있어.',
  differ:
    '한 사람은 해결 방법을, 다른 사람은 지금 어떤 기분인지를 먼저 확인하고 싶어할 수 있어. 같은 대화에서 서로 다른 것을 기다리고 있는 상태가 될 수 있어.',
};

/**
 * §8 MBTI-05 — **오해 포인트.** `행동 → 의도 단정 방지`가 핵심이다.
 *
 * ⚠️ 선호가 **다른 축에서만** 나온다. 같은 축은 오해의 재료가 아니다.
 * ⚠️ 전부 `X ≠ Y` 꼴이다 — 상대의 의도를 새로 주장하는 문장이 하나도 없다.
 */
export const MBTI_MISREAD: Record<'energy' | 'information' | 'decision' | 'lifestyle', string> = {
  energy:
    '혼자 있는 시간을 갖는 것이 마음이 식은 신호는 아니야. 회복하는 방식이 다르면 같은 침묵이 서로에게 다른 뜻으로 읽혀.',
  information:
    '구체적으로 말하지 않는 것이 숨기는 것은 아니야. 반대로 자세히 되짚는 것도 따지는 것은 아니고.',
  decision:
    '해결책부터 말하는 것이 공감하지 않는다는 뜻은 아니야. 반대로 기분부터 말하는 것도 문제를 피하는 것은 아니고.',
  lifestyle:
    '미리 정하고 싶어하는 것이 통제하려는 것은 아니야. 반대로 정하지 않고 두는 것도 관심이 없는 것은 아니고.',
};

/** 네 축이 전부 같을 때 — 오해 포인트를 비워두지 않는다 */
export const MBTI_MISREAD_ALL_SAME =
  '네 축이 전부 같은 쪽으로 분류됐어. 이런 조합에서 흔히 이야기되는 건 "말 안 해도 알겠지"로 설명을 생략하게 되는 지점이야. 같은 칸에 놓였다는 게 같은 사람이라는 뜻은 아니야.';

/** §9 — Target 없는 MBTI Self Lens. 축별로 '관계 속의 나'까지 간다 */
export const MBTI_SELF_ENERGY: Record<'I' | 'E', string> = {
  I: '혼자 있는 시간으로 에너지를 회복하는 쪽으로 분류됐어. 관계에서는 붙어 있는 시간이 길어질수록 회복이 필요해지는 형태로 나타난다고 이야기돼.',
  E: '함께 활동하면서 에너지를 얻는 쪽으로 분류됐어. 관계에서는 혼자 보내는 시간이 길어질 때 먼저 연락하게 되는 형태로 나타난다고 이야기돼.',
};

export const MBTI_SELF_INFORMATION: Record<'S' | 'N', string> = {
  S: '관계 신호도 눈에 보이는 사실로 받아들이는 쪽이야. 상대가 실제로 한 행동이 마음의 증거가 된다고 이야기돼.',
  N: '관계 신호를 의미로 받아들이는 쪽이야. 같은 행동에서도 그게 무슨 뜻인지를 먼저 읽는다고 이야기돼.',
};

export const MBTI_SELF_DECISION: Record<'T' | 'F', string> = {
  T: '갈등에서는 어떻게 하는 게 맞는지를 먼저 보는 쪽이야. 결론이 정해지면 마음이 정리된다고 이야기돼.',
  F: '갈등에서는 누가 어떤 마음인지를 먼저 보는 쪽이야. 마음이 확인되면 결론은 뒤에 와도 괜찮다고 이야기돼.',
};

export const MBTI_SELF_LIFESTYLE: Record<'J' | 'P', string> = {
  J: '약속과 일상은 미리 정해져 있을 때 편한 쪽이야. 정해진 게 갑자기 바뀌면 일정보다 마음이 먼저 흔들린다고 이야기돼.',
  P: '약속과 일상은 그때 정하는 게 편한 쪽이야. 미리 확정하는 요구가 부담으로 느껴질 수 있다고 이야기돼.',
};

/* ══════════════════════════════════════════════════ 별자리 양태 (§16) */

/** 표 자체는 `data/zodiac.ts`에 있다 — 여기 있는 것은 그 표를 읽는 문장뿐이다 */
export const MODALITY_LABEL: Record<ZodiacModality, string> = {
  cardinal: '활동',
  fixed: '고정',
  mutable: '변통',
};

/**
 * 양태(modality) — 12궁을 3개로 나누는 **전통 점성술의 고정 분류표**다.
 * 내가 만든 구분이 아니고, 계산도 아니다(태양궁이 정해지면 곧바로 정해진다).
 */
export const MODALITY_NOTE: Record<ZodiacModality, string> = {
  cardinal: '관계에서도 먼저 시작하고 방향을 내는 쪽으로 이야기돼.',
  fixed: '관계에서도 한번 정한 방식을 유지하는 쪽으로 이야기돼.',
  mutable: '관계에서도 상황에 맞춰 방식을 바꾸는 쪽으로 이야기돼.',
};

export const MODALITY_PAIR: Record<'same' | 'differ', string> = {
  same: '두 사람의 양태가 같아. 관계를 움직이는 방식이 닮은 것으로 읽히고, 그래서 둘 다 같은 지점에서 멈출 수도 있다고 이야기돼.',
  differ:
    '두 사람의 양태가 달라. 한쪽이 시작하거나 유지하거나 바꾸려 할 때 다른 쪽이 다른 리듬으로 반응할 수 있다는 뜻으로 읽혀.',
};

/** §16 ZODIAC-04 — 별자리 특성을 사람의 의도로 치환하지 않는다 */
export const ZODIAC_MISREAD =
  '별자리에서 말하는 성향은 그 사람이 그렇게 하려고 마음먹었다는 뜻이 아니야. 표현이 적은 것과 마음이 적은 것은 다른 이야기고, 이 렌즈는 둘을 구분하지 못해.';

/* ═══════════════════════════════════════════ 렌즈별 unavailable 사유 */

/**
 * ⚠️ **무엇이 있으면 볼 수 있는지까지 말한다.** '데이터가 없어'로 끝나면
 * 사용자는 무엇을 해야 할지 모른 채 빈 칸만 본다.
 *
 * ⚠️ 갈 수 없는 길을 알려주지 않는다 — 상대가 없는 사용자에게 '상대 MBTI를
 * 입력해'라고 하지 않는다(호출부가 self/unavailable을 먼저 가른다).
 */
export const LENS_UNAVAILABLE_REASON = {
  mbtiNoSelf: '네 MBTI를 입력하면 이 렌즈를 볼 수 있어. 모르면 비워둬도 괜찮아 — 없는 유형을 내가 정하진 않을게.',
  sajuNoSelf: '네 생년월일(양력)이 있어야 일주를 계산할 수 있어.',
  sajuLunar:
    '음력으로 입력했네. 음양력 환산표가 아직 연결되지 않아서 일주를 계산하지 않았어. 양력 생일을 알면 그걸로 다시 입력해줘.',
  zodiacNoSelf: '네 생년월일이 있어야 태양궁을 계산할 수 있어.',
} as const;
